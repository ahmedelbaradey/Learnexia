# Design Spec — Login Screen Pixel-Alignment Delta
**Story:** P1-11 pixel-alignment pass (Login screen only)
**Date:** 2026-05-24
**Auditor:** designer agent
**Method:** Element-by-element diff between captures, preview cards, and implementation code.

---

## Source hierarchy (highest to lowest priority)

1. Captures: `design-system/screenshots/web/02-login.png` (EN) + `design-system/screenshots/web-ar/02-login.png` (AR)
2. Preview cards cited per row below
3. `design-system/ui_kits/parent-dashboard/PagesPublic.jsx` (`LoginWebPage`)
4. `packages/design-system/src/tokens/` (concrete resolved values)

---

## Screens in scope

This pass covers the Login screen only (web split-panel, ≥768px).
Files under review:

- `apps/student-app/app/(auth)/login.tsx`
- `apps/student-app/app/(auth)/_components/LoginForm.tsx`
- `apps/student-app/app/(auth)/_components/loginParts.tsx`
- `apps/student-app/app/(auth)/_components/PersonaToggle.tsx`
- `apps/student-app/app/(auth)/_components/SocialIcons.tsx`
- `apps/student-app/app/(auth)/_components/LocaleThemeControls.tsx`
- `apps/student-app/app/(auth)/_components/LoginBrandPanel.tsx`
- `apps/student-app/src/components/FormScaffold.tsx` (`SplitFormScaffold`)

---

## Delta Table

Severity order: Blocker → Major → Minor. Within each tier, ordered by visual prominence.

---

### BLOCKER — Wrong layout or missing element that breaks the screen

| # | Element | Current implementation | Target (token + card) | Severity | Fix |
|---|---------|------------------------|----------------------|----------|-----|
| B-01 | **Eyebrow color — always primary, both breakpoints** | `login.tsx` line 72-78: eyebrow renders `color="$primary"` on desktop but the `$tablet` conditional wraps it such that on phone it falls through to `color="$fg3"` (#94A3B8). In the EN web capture the eyebrow "LOG IN" is clearly `#A5B4FC` indigo-300 (matches `$primaryLight`, the lighter indigo used for the eyebrow in `PagesPublic.jsx` line 288). In the AR capture "تسجيل الدخول" is the same tint. | `$primaryLight` = `#A5B4FC`. Proven by `PagesPublic.jsx` line 288 (`color: '#A5B4FC'`, `fontWeight: 800`, `textTransform: uppercase`, `letterSpacing: '0.12em'`). Preview card: `web-auth-split.html` line 15 confirms `color:#A5B4FC`. | Blocker | In `login.tsx` remove the breakpoint conditional and set `color="$primaryLight"` unconditionally on the eyebrow `Text`. |
| B-02 | **Eyebrow letter-spacing and font-weight** | `login.tsx` eyebrow: `letterSpacing={1.5}` (1.5px) and `fontWeight="700"`. | Capture and `PagesPublic.jsx` line 288 use `letterSpacing: '0.12em'` (~1.44px at 12px = 1.44px; effectively 1.4px) and `fontWeight: 800`. `web-auth-split.html` line 15: `font-weight:800; letter-spacing:0.12em`. Token: `--lx-letter-spacing-wide` ≈ 0.12em. | Blocker | Change eyebrow `fontWeight` from `"700"` to `"800"` and `letterSpacing` to `1.44` (0.12 × 12). |

---

### MAJOR — Wrong token, wrong measurement, wrong copy, wrong state

| # | Element | Current implementation | Target (token + card) | Severity | Fix |
|---|---------|------------------------|----------------------|----------|-----|
| M-01 | **Heading font size** | `login.tsx` line 83: `fontSize={28}`. | Capture "Welcome back" heading is H1 scale: `--lx-size-h1` = 32px / weight 800. Proven by `type-display.html` line 6: `H1 · 32 / 800` and `PagesPublic.jsx` line 289: `fontSize: 32, fontWeight: 900`. Token: `$7` (32px in the fontSize scale). | Major | Change `fontSize={28}` to `fontSize={32}` and `fontWeight="800"` (already 800 — confirm). |
| M-02 | **Heading letter-spacing** | `login.tsx` heading has no explicit `letterSpacing`. Default Poppins at 32px has no system tracking. | Capture and `PagesPublic.jsx` line 289 apply `letterSpacing: '-0.02em'` ≈ -0.64px at 32px. `type-display.html` line 6 confirms `line-height:1.15` (implies design-system expects negative tracking at H1 scale). The font config `letterSpacingScale` at key `7` = `-0.02 * 32 = -0.64`. | Major | Add `letterSpacing={-0.64}` to the "Welcome back" heading `Text`. |
| M-03 | **Persona toggle — outer container border-radius** | `PersonaToggle.tsx` line 50: `borderRadius="$pill"` (9999px) on the outer rail. | Capture shows the outer rail with a more moderate rounded-rectangle, not a full pill. `PagesPublic.jsx` line 293: `borderRadius: 14`. The inner tabs also have `borderRadius: 10` (not pill). Token: radius 14 falls between `$sm` (8) and `$button` (16). Closest compliant token is `$button` (16px). The capture's radius is between 12–16px and the `web-auth-split.html` spec does not show the toggle in detail; `PagesPublic.jsx` is the secondary authority. | Major | Change outer `borderRadius` from `"$pill"` to `"$button"` (16px). Change inner tab `borderRadius` from `"$pill"` to `12` (half of 24px, the midpoint between $sm and $button — no exact token; this is a gap, see Design Gaps). |
| M-04 | **Persona toggle — inner tab height** | `PersonaToggle.tsx` line 68: `height={52}` (already updated per P1-11-qa-pass M-correction; current code shows `height={52}`). Wait — reading the file again: `height={52}` is in the current code. The QA pass noted it should be 52. Confirmed: current code already has `height={52}`. This is **resolved**. | — | Accepted | No change needed. |
| M-05 | **Persona toggle — emoji in labels** | `resources.ts` line 111-112: `personaParent: '👨‍👩‍👧 Parent'` and `personaStudent: '🎓 Student'` — emoji baked into i18n string. `PersonaToggle.tsx` renders the full string including emoji. | Capture shows "👨‍👩‍👦 I'm a Parent" and "🎓 I'm a Student" (with full "I'm a" prefix). `PagesPublic.jsx` line 295-296: `emoji: '👨‍👩‍👦'` + `label: "I'm a Parent"` / `label: "I'm a Student"`. AR capture: "👨‍👩‍👧 أنا ولي أمر" / "🎓 أنا طالب" (also has "أنا" prefix). Current i18n values omit the "I'm a" / "أنا" prefix. | Major | Update `resources.ts`: `personaParent` EN → `"👨‍👩‍👦 I'm a Parent"` / AR → `"👨‍👩‍👧 أنا ولي أمر"`. `personaStudent` EN → `"🎓 I'm a Student"` / AR → `"🎓 أنا طالب"`. |
| M-06 | **Password field — "Show" / "Hide" as text, not emoji** | `TextField/index.tsx` uses `revealed ? '🙈' : '👁'` emoji inside a `Text` node for the reveal toggle. `resources.ts` defines `showPassword: 'Show'` / `hidePassword: 'Hide'` (EN) and `showPassword: 'إظهار'` / `hidePassword: 'إخفاء'` (AR) — but the TextField component ignores these i18n keys and uses hard-coded emoji. | Capture (EN) clearly shows the word "Show" right-aligned beside the Password label — not an emoji. AR capture shows "إظهار" right-aligned. `PagesPublic.jsx` line 317: `<button style={textBtn()}>{showPw ? 'Hide' : 'Show'}</button>`. Token: color `$primaryLight` (#A5B4FC), `fontSize: 12`, `fontWeight: 600`. | Major | In `TextField/index.tsx` replace the emoji glyph text with the i18n-resolved string. Pass a `showLabel` / `hideLabel` prop (or use locale from context). Render as `Text` with `color="$primaryLight"` `fontSize={12}` `fontWeight="600"`. |
| M-07 | **"Show"/"Hide" alignment — positioned beside the label row, not inside the input** | `TextField/index.tsx` places the reveal toggle as `position="absolute" end={4}` inside the input container — it sits inside the input field at the right edge. | Capture and `PagesPublic.jsx` show "Show" placed inside a `WebField` `right` slot — rendered in the label row to the right of "Password" label text, above the input, not overlapping the input content. The AR capture shows "إظهار" to the left of "كلمة المرور" label (RTL start). | Major | Move the reveal toggle out of the input overlay into the label row. The `TextField` component needs a `labelRight` slot (or the login form wraps it). Update `TextField`: when `secureTextEntry=true`, render the toggle inline with the label in a `flexDirection="row" justifyContent="space-between"` label row. In RTL (`dir=rtl`), the toggle moves to the label-start position. |
| M-08 | **Submit button — disabled state appearance** | `Button/index.tsx` disabled variant: `backgroundColor: '$card'` (#1E293B), `opacity: 0.4`, color `$fg4` (#64748B). With opacity 0.4 applied to `$card`, the visual result is very dark / near-invisible on the `$bg` canvas. | Capture "Log in →" in the disabled state (empty email/password) shows a mid-dark surface approximately `#2A2D3E` with `$fg4` label — **not** a 0.4-opacity card. `PagesPublic.jsx` line 331: `background: canSubmit ? '#4F46E5' : '#2A2D3E'`, `color: canSubmit ? '#fff' : '#64748B'`, `cursor: 'not-allowed'`. `#2A2D3E` is between `$card` (#1E293B) and `$cardSoft` (#334155). The token closest is `$cardSoft` at full opacity, or a new intermediate. | Major | Remove `opacity: 0.4` from the disabled Button variant. Instead set `backgroundColor` to a concrete non-transparent value. Closest existing token: use `backgroundColor: '$cardSoft'` (#334155) at `opacity: 1` for the disabled state so it reads as a distinct muted surface without the translucency artefact. Label stays `color: '$fg4'` (#64748B). See Design Gaps — no exact `#2A2D3E` token exists. |
| M-09 | **Submit button — active state shadow / inner highlight** | `Button/index.tsx` primary variant: `box-shadow: 0 4px 12px rgba(99,102,241,0.4)` (the token `primaryGlow`). No `inset` highlight. | `PagesPublic.jsx` line 335 (active state): `boxShadow: '0 4px 12px rgba(99,102,241,0.4), inset 0 1px 0 rgba(255,255,255,0.2)'`. The `inset 0 1px 0 rgba(255,255,255,0.2)` creates a subtle top-edge highlight visible in the capture. `elevation.html` line 13 (primary glow card) confirms the outer glow. | Major | Add `inset 0 1px 0 rgba(255,255,255,0.2)` to the primary Button shadow. Since RN does not support inset shadows, gate this with a web-only style (`platform: 'web'` or via `$platform-web` Tamagui prop). Combined web value: `shadows.primaryGlow + ', inset 0 1px 0 rgba(255,255,255,0.2)'`. |
| M-10 | **Social buttons — border-radius** | `loginParts.tsx` `SocialButton`: `borderRadius="$button"` (16px). | Capture and `PagesPublic.jsx` line 561: `borderRadius: 14`. `mobile-social-buttons.html` line 5: `border-radius:14px`. Token gap: 14px is between `$sm` (8) and `$button` (16). Functionally close but a measurable 2px deviation. The preview card is the authority. | Major | Change `SocialButton` `borderRadius` from `"$button"` (16px) to `14` (use the literal as a local-const like `inputRadius`). This is a second instance of the "14px radius" gap — same as TextField `inputRadius`. Both should use the same local constant `socialButtonRadius = 14`. |
| M-11 | **Social buttons — background color token** | `loginParts.tsx` `SocialButton`: `backgroundColor="$card"` (#1E293B). | Capture social buttons appear on a slightly different surface — `PagesPublic.jsx` line 560: `background: '#1E293B'` which is exactly `$card`. `mobile-social-buttons.html` line 5: `background:#15161D` (a near-black surface). The web capture matches `$card` (#1E293B). So `$card` is correct **but** the border must be `rgba(255,255,255,0.08)` not the current `$border` token. Current `$border` = `rgba(255,255,255,0.08)` — this is actually correct. Verified: no deviation here. | — | Accepted — `$card` bg + `$border` border match. |
| M-12 | **Social button hover state** | `loginParts.tsx` `SocialButton hoverStyle={{ backgroundColor: '$cardSoft' }}` (#334155). | SKILL.md rule 9: "Brighten on hover, never darken". `$cardSoft` (#334155) is lighter than `$card` (#1E293B) — this is correct brightening. The `pressStyle={{ scale: 0.97 }}` diverges from the spec: SKILL.md rule 9 says `scale: 0.95` for 80ms. | Major | Change `pressStyle={{ scale: 0.97 }}` to `pressStyle={{ scale: 0.95 }}` to match SKILL.md. |
| M-13 | **Brand panel logo-mark border-radius** | `LoginBrandPanel.tsx` line 29: `borderRadius="$button"` (16px) on the logo-mark container. | Capture logo-mark (the purple spark square in top-left) has a rounded-square radius visually closer to 20px. `PagesPublic.jsx` line 267: the logo-mark `<img>` has no explicit radius (the SVG asset itself has rounded corners). The QA pass (P1-11-qa-pass.md row L-05) confirmed: capture radius is `$card` (20px). | Major | Change logo-mark container `borderRadius` from `"$button"` (16px) to `"$card"` (20px). |
| M-14 | **Brand panel star illustration — size** | `LoginBrandPanel.tsx` lines 48-56: star container `width={140} height={140}` circle, `fontSize={72}` ⭐. | Capture shows the star illustration at approximately 96px rendered emoji size, in a ~140px circular glow bg. `PagesPublic.jsx` line 272: `fontSize: 96` for the star. `web-auth-split.html` line 8: `font-size:60px`. The star itself (emoji) should be larger. The container circle size (140px) is correct; the fontSize of the emoji should match the capture's visual weight. | Major | Change star `fontSize` from `{72}` to `{96}` to match `PagesPublic.jsx`. |
| M-15 | **Brand panel tagline — font size** | `LoginBrandPanel.tsx` line 57-66: heading `fontSize={44}` `fontWeight="800"` `lineHeight={48}`. | Capture heading "Welcome back to the adventure." appears ~48px. `PagesPublic.jsx` line 273: `fontSize: 48, fontWeight: 900`. `web-auth-split.html` line 9: `font-size:24px` (preview card is a reduced mockup — use `PagesPublic.jsx` as authority for this element). | Major | Change brand tagline heading `fontSize` from `{44}` to `{48}` and `fontWeight` from `"800"` to `"900"`. Note: Poppins weight 900 = Black; the font config `weightScale` has `9: '900'` — valid. |
| M-16 | **"240,000+ kids learning today" — social proof number format (AR)** | `resources.ts` AR `brand.socialProof`: `'أكثر من 240,000 طفل يتعلمون اليوم'`. Uses Western-Arabic numerals (240,000) inline in Arabic text. | SKILL.md rule on numerals: "use Eastern-Arabic numerals (٠١٢٣٤٥٦٧٨٩) for in-line text" but "keep Latin numerals (and dir=ltr) for technical strings." The number 240,000 in this social-proof sentence is in-line Arabic text — SKILL.md requires Eastern-Arabic: `٢٤٠٬٠٠٠`. Exception: `dir=ltr` is specified for "emails/brand/technical strings." "240,000+" is a social proof count — it is in-line text, not a technical string, so Eastern-Arabic applies. | Major | Update `resources.ts` AR `brand.socialProof` to `'أكثر من ٢٤٠٬٠٠٠ طفل يتعلمون اليوم'`. The "Learnexia" brand name stays Latin. |
| M-17 | **AR caption "سجّل دخولك لمواصلة سلسلة إنجازاتك 🔥"** | `resources.ts` AR `login.subtitle`: `'سجّل دخولك لمواصلة سلسلة إنجازاتك 🔥'` | AR capture shows the subtitle as "سجّل دخولك للحفاظ على سلسلتك 🔥". The AR capture subtitle wording differs from the resources.ts AR string. The SKILL.md cheat-sheet Greeting entry for "streak" is "🔥 سلسلة ٧ أيام"; the EN source "Log in to keep your streak alive 🔥" should translate closely to "سجّل دخولك للحفاظ على سلسلتك 🔥". | Major | Update `resources.ts` AR `login.subtitle` to `'سجّل دخولك للحفاظ على سلسلتك 🔥'` to match the AR capture exactly. |
| M-18 | **Submit button text — RTL arrow direction** | `resources.ts` AR `login.submitButton`: `'تسجيل الدخول →'` — right-pointing arrow in RTL text. | SKILL.md rule 4 for Arabic: "flip arrows (`→` becomes `←`)" in RTL. The AR capture CTA button clearly shows "تسجيل الدخول ←" with a left-pointing arrow. This is the "next" direction in RTL layout. | Major | Update `resources.ts` AR `login.submitButton` from `'تسجيل الدخول →'` to `'تسجيل الدخول ←'`. |
| M-19 | **"Or continue with" divider text — letter-spacing and transform** | `loginParts.tsx` `OrDivider` `Text`: `fontFamily="$heading"` `fontSize={12}` `fontWeight="600"` `textTransform="uppercase"` `letterSpacing={1}`. | `PagesPublic.jsx` line 340: `fontSize: 12, fontWeight: 600, color: '#64748B'` (= `$fg4`, not `$fg3`). No `textTransform` shown — but capture clearly renders "OR CONTINUE WITH" in all-caps. So uppercase is correct. The color token is `$fg4` (#64748B), not `$fg3` (#94A3B8). The divider lines use `rgba(255,255,255,0.08)` = `$border`. `letterSpacing: '0.04em'` ≈ 0.48px at 12px vs current hardcoded `1`. The preview card `web-auth-split.html` line 15 uses `letter-spacing:0.12em` for the eyebrow — the divider is a different element; use `PagesPublic.jsx` as authority. | Major | Change `OrDivider` text `color` from `"$fg3"` to `"$fg4"` (#64748B). Change `letterSpacing` from `{1}` to `{0.48}` (0.04em × 12). |

---

### MINOR — Small spacing, small typography, icon, or copy delta

| # | Element | Current implementation | Target (token + card) | Severity | Fix |
|---|---------|------------------------|----------------------|----------|-----|
| m-01 | **Heading line-height** | `login.tsx` heading has no explicit `lineHeight`. Tamagui resolves via `lineHeightScale[7]` = 40. | `PagesPublic.jsx` line 289 and capture: heading "Welcome back" at 32px has `letterSpacing: '-0.02em'` but no explicit `lineHeight` override there either. `type-display.html` line 6: `line-height:1.15` → 1.15 × 32 = 36.8px ≈ 37px. The font config `lineHeightScale[7] = 40` diverges slightly from the preview card spec of 1.15 × 32 = 36.8px. At H1 scale this is a 3px discrepancy. | Minor | Flag as design gap GAP-A: the `lineHeightScale` at step 7 (H1) is 40px but `type-display.html` specifies 1.15 × 32 = 36.8px. Until resolved, add explicit `lineHeight={37}` to the heading. |
| m-02 | **Subtitle color** | `login.tsx` subtitle: `color="$fg3"` (#94A3B8). | Capture subtitle "Log in to keep your streak alive 🔥" appears `$fg3`. `PagesPublic.jsx` line 290: `color: '#94A3B8'` = `$fg3`. Match confirmed. | — | Accepted. |
| m-03 | **Subtitle font size** | `login.tsx` subtitle: `fontSize={14}` and `fontFamily="$body"`. | `PagesPublic.jsx` line 290: `fontSize: 14`. `type-body.html` line 9: `Body sm · 14 / 400`. Match. | — | Accepted. |
| m-04 | **Label text transform and spacing (Email / Password labels)** | `TextField/index.tsx` label: `textTransform="uppercase"` `letterSpacing={0.6}` `fontSize={12}` `fontWeight="600"` `color="$fg3"`. | `PagesPublic.jsx` `WebField` label line 511: `fontSize: 12, fontWeight: 700, color: '#CBD5E1', letterSpacing: '0.04em'` (= `$fg2`, not `$fg3`; weight 700, not 600). `components-input.html` line 6: `font-weight:600; color:var(--lx-fg3); text-transform:uppercase; letter-spacing:0.06em`. The preview card (`components-input.html`) matches current implementation better than `PagesPublic.jsx`. The **preview card is the authority**. Deviation: `letterSpacing={0.6}` vs `0.06em` at 12px = 0.72px. | Minor | Change `letterSpacing` from `{0.6}` to `{0.72}` (0.06 × 12). Change `color` from `"$fg3"` to `"$fg3"` (already correct per preview card; keep). Keep `fontWeight="600"`. |
| m-05 | **Input height** | `TextField/index.tsx`: `height={52}`. | `components-input.html` line 7: `height:48px`. `ar-input.html` line 6: `height:48px`. The preview card spec is 48px, implementation uses 52px. The previous audit (P1-11-pixel-audit.md line 89) already flagged this as Minor. | Minor | Change `height={52}` to `height={48}` on the `Stack` wrapper in `TextField`. Update `TextInput` `style.height` accordingly. |
| m-06 | **Input font size** | `TextField/index.tsx` `TextInput style.fontSize: 15`. | `components-input.html` line 7: `font-size:15px`. `ar-input.html` line 6: `font-size:15px`. Match confirmed. | — | Accepted. |
| m-07 | **Input placeholder color** | `TextField/index.tsx` `placeholderTextColor="#94A3B8"` (hard-coded hex). | `#94A3B8` = `$fg3`. The value is correct but should be the token constant `colors.fg3` not a raw hex. | Minor | Replace `placeholderTextColor="#94A3B8"` with `placeholderTextColor={colors.fg3}` imported from `@learnexia/design-system`. |
| m-08 | **"Forgot password?" color** | `LoginForm.tsx` line 160: `color="$primaryLight"` = #A5B4FC (indigo-300). | `PagesPublic.jsx` `textBtn()` line 546: `color: '#A5B4FC'` = `$primaryLight`. Match. | — | Accepted. But note: the previous QA pass (L-08) asked to verify whether this should be `$primaryHover` (#6366F1). Reviewing the capture directly, the "Forgot password?" text is a lighter indigo, clearly matching `#A5B4FC`, not `#6366F1`. `$primaryLight` is confirmed correct. |
| m-09 | **"Forgot password?" font size** | `LoginForm.tsx` line 162: `fontSize={14}`. | Capture and `PagesPublic.jsx` `textBtn()` line 548: `fontSize: 12`. The QA pass did not flag this. At 14px vs 12px the "Forgot password?" text is slightly larger than the capture. | Minor | Change `fontSize` from `{14}` to `{12}` on the "Forgot password?" `Text`. |
| m-10 | **"Remember me" checkbox — visual size** | `loginParts.tsx` `Checkbox`: box `width={20} height={20}` `borderRadius="$sm"` (8px). | `PagesPublic.jsx` register form lines 448-452 shows a `22×22` box with `borderRadius: 6`. `components-input.html` has no checkbox spec. No capture close-up available; use `PagesPublic.jsx` as authority. | Minor | Change checkbox box from `20×20` to `22×22`, `borderRadius` from `"$sm"` (8px) to `{6}` (local literal). |
| m-11 | **"Remember me" label font size** | `loginParts.tsx` `Checkbox Text`: `fontSize={14}`. | `PagesPublic.jsx` line 325: checkbox label `fontSize: 13`. Capture label appears at ~13px body size. | Minor | Change checkbox label `fontSize` from `{14}` to `{13}`. |
| m-12 | **"Remember me" label color** | `loginParts.tsx` `Checkbox Text`: `color="$fg3"` (#94A3B8). | `PagesPublic.jsx` line 325: `color: '#CBD5E1'` = `$fg2`. The capture label is clearly lighter than muted — it reads as `$fg2`. | Minor | Change checkbox label `color` from `"$fg3"` to `"$fg2"`. |
| m-13 | **"New to Learnexia?" footer — font size** | `login.tsx` lines 119-134: `fontSize={14}` for both "New to Learnexia?" and "Create parent account" text. | `PagesPublic.jsx` line 352: `fontSize: 14` for the footer. Match. But the "Create parent account" link has `fontWeight: 800` in `PagesPublic.jsx`. Current code uses `fontWeight="600"`. | Minor | Change "Create parent account" link `fontWeight` from `"600"` to `"800"`. |
| m-14 | **"New to Learnexia?" footer alignment — web** | `login.tsx` footer row: `justifyContent="center"`. | Capture footer is horizontally centered on the form panel. `PagesPublic.jsx` line 351: `textAlign: 'center'`. `justifyContent="center"` is correct. Match. | — | Accepted. |
| m-15 | **Brand panel background gradient** | `SplitFormScaffold.tsx` brand panel: `backgroundColor="$primary"` (flat `#4F46E5`). | Capture brand panel clearly shows a gradient — not flat primary. `PagesPublic.jsx` line 257: `background: 'linear-gradient(165deg,#4F3FB0 0%,#3B2C8F 50%,#1E1B4B 100%)'`. This is a purple-to-dark-indigo gradient on the brand panel. Token: `$primary` is flat; the gradient is a custom 3-stop value not in the token system (`gradients.ts` has 3 named gradients but none is this 165deg panel gradient). | Minor | Apply the panel gradient as a web-platform style: `$platform-web: { background: 'linear-gradient(165deg,#4F3FB0 0%,#3B2C8F 50%,#1E1B4B 100%)' }` on the brand panel Stack. On native keep `backgroundColor="$primary"`. Flag as Design Gap GAP-B (no token for the brand-panel gradient). |
| m-16 | **Brand panel particle dots** | `SplitFormScaffold.tsx` brand panel: no decorative dot/particle layer. | Capture shows ~12 small glowing white dots scattered in the brand panel background. `PagesPublic.jsx` lines 260-264: 12 absolutely-positioned dots, each `3–5px`, `borderRadius: '50%'`, `opacity: 0.2–0.5`, with a white `boxShadow` glow. Token: `$fg1` at low opacity. | Minor | Add a decorative particle layer (web-only) inside the brand panel Stack: 12 `position="absolute"` circles, `width: 3–5`, `background: rgba(255,255,255,0.25)`, `boxShadow: '0 0 8px rgba(255,255,255,0.5)'`. Positions from `PagesPublic.jsx`. |
| m-17 | **Brand panel — "240,000+" social proof: fire emoji alignment** | `LoginBrandPanel.tsx` line 83-89: social proof row uses `flexDirection={rowDir}` (RTL-aware) with `fontSize={18}` 🔥 emoji and `fontSize={14}` text. | `PagesPublic.jsx` line 281: `gap: 12, color: 'rgba(255,255,255,0.6)', fontSize: 13`. Current `$fg2` (#CBD5E1) is close but the capture social proof text is slightly dimmer — `rgba(255,255,255,0.6)` ≈ #99A (off-white 60%). Closest token is `$fg3` (#94A3B8). The `$fg2` in current code is slightly too bright. | Minor | Change social proof text `color` from `"$fg2"` to `"$fg3"`. Change `fontSize` from `{14}` to `{13}`. |
| m-18 | **AR — eyebrow text alignment** | `login.tsx` eyebrow: `textTransform="uppercase"` always applied. | Arabic text does not use uppercase (no concept in Arabic script). When `direction === 'rtl'`, `textTransform="uppercase"` should not be applied (it has no effect on Arabic but may affect mixed strings). This is an i18n hygiene issue; the AR eyebrow copy "تسجيل الدخول" contains no Latin chars so uppercase is effectively a no-op — but it is semantically incorrect. | Minor | Conditionally apply `textTransform={direction === 'rtl' ? 'none' : 'uppercase'}` to the eyebrow `Text` in `login.tsx`. |
| m-19 | **AR — "New to Learnexia? Create parent account" — brand name** | `resources.ts` AR `login.newParent`: `'جديد على ليرنيكسيا؟'`. The brand name "ليرنيكسيا" is an Arabic transliteration. | SKILL.md: "keep Latin numerals (and `dir=ltr`) for… brand name `Learnexia`". The brand name should remain Latin "Learnexia" in AR too, wrapped in `dir="ltr"` inline. The AR capture shows "جديد على Learnexia؟" with the Latin brand name inside the RTL sentence. | Minor | Update `resources.ts` AR `login.newParent` to `'جديد على Learnexia؟'`. In the rendered `Text`, wrap "Learnexia" in a nested inline `Text` with `writingDirection="ltr"` (or use a Unicode bidi mark). |
| m-20 | **AR — social provider names** | `resources.ts` AR: `socialGoogle: 'جوجل'`, `socialApple: 'آبل'`, `socialMicrosoft: 'مايكروسوفت'`. | AR capture shows the provider names as Arabic transliterations. But SKILL.md says brand names should stay Latin. "Google", "Apple", "Microsoft" are brand names. However the AR capture buttons clearly show Arabic transliterations ("جوجل", "آبل", "مايكروسوفت"). Since the capture is the pixel-perfect target, the Arabic transliterations match the capture. | — | Accepted (capture outranks the brand-name rule here). |
| m-21 | **AR — "Or continue with" divider** | `resources.ts` AR `orContinueWith`: `'أو تابع باستخدام'`. | AR capture shows "أو واصل بـ" — a slightly different phrasing. The current AR phrasing "أو تابع باستخدام" is natural Arabic but the capture reads differently. | Minor | Update `resources.ts` AR `orContinueWith` to match the capture exactly: `'أو واصل بـ'`. |
| m-22 | **Brand panel body text — line-height** | `LoginBrandPanel.tsx` `Text` body: `lineHeight={24}` at `fontSize={16}`. | `PagesPublic.jsx` line 277: no explicit `lineHeight` on the body paragraph, relying on browser default (~1.5 × 16 = 24px). The `web-auth-split.html` line 10 shows `font-size:12px` for the body in the preview card (reduced mockup). `1.5 × 16 = 24px` matches current `lineHeight={24}`. Match. | — | Accepted. |
| m-23 | **Input border in filled/resting state** | `TextField/index.tsx`: filled non-focused state uses `borderColor="$borderStrong"` (`rgba(255,255,255,0.16)`). Empty resting uses `borderColor="$border"` (`rgba(255,255,255,0.08)`). | `components-input.html` line 8: resting input uses `border:1px solid rgba(255,255,255,0.1)` (alpha 0.10). Current token `$border` = 0.08 alpha. The preview card shows 0.10. | Minor | Design gap: `$border` is 0.08 but preview shows 0.10. Flag as Design Gap GAP-C. No change to code until token is updated. |
| m-24 | **Focus ring on inputs** | `TextField/index.tsx`: focused state uses `borderColor="$borderFocus"` + `shadowColor` / `shadowRadius=4` as a React Native shadow (not a CSS `box-shadow`). | `borders-focus.html` line 12: focused input: `outline:2px solid #4F46E5; outline-offset:0; box-shadow:0 0 0 6px rgba(99,102,241,0.35)`. The token `shadows.focusRing` = `'0 0 0 2px #4F46E5, 0 0 0 6px rgba(99,102,241,0.45)'`. On web, RN `shadowColor` does not produce a CSS `box-shadow` ring — it renders as a drop shadow. The `box-shadow` focus ring needs a web-specific style. | Minor | On web, apply `$platform-web: { boxShadow: '0 0 0 2px #4F46E5, 0 0 0 6px rgba(99,102,241,0.35)' }` to the focused input wrapper. On native keep the current shadow approach. |
| m-25 | **OR divider line color** | `loginParts.tsx` `OrDivider`: `backgroundColor="$border"` = `rgba(255,255,255,0.08)`. | `PagesPublic.jsx` line 339: `background: 'rgba(255,255,255,0.08)'` = `$border`. Match. | — | Accepted. |
| m-26 | **Social button icon size** | `SocialIcons.tsx`: `SIZE = 18`. | `PagesPublic.jsx` line 567: icon container `22×22`. `mobile-social-buttons.html` line 6: `.ico { width:20px; height:20px }`. The current `SIZE=18` is smaller than both references. | Minor | Change `SIZE` in `SocialIcons.tsx` from `18` to `20`. Resize the icon container accordingly. |
| m-27 | **Google icon — monochrome vs multicolor** | `SocialIcons.tsx` `GoogleIcon`: renders four-quadrant brand colors (red/amber/primary/secondary). | Capture social buttons show a **white circular disc with a dark "G"** letter — not multicolor quadrants. The monochrome style is the dark-theme social button convention. `PagesPublic.jsx` line 554: Google icon = `{ icon: 'G', label: 'Google', bg: '#fff', fg: '#0F172A' }` — white disc with dark "G". `mobile-social-buttons.html` line 9: white disc + `G` letter in dark. | Minor | Simplify `GoogleIcon` to white disc + dark "G": `Stack` `width={SIZE} height={SIZE}` `borderRadius={9999}` `backgroundColor="#fff"` with inner `Text color="#0F172A" fontWeight="900" fontSize={13}` "G". Remove the four-quadrant colored segments. |
| m-28 | **Apple icon — shape accuracy** | `SocialIcons.tsx` `AppleIcon`: constructed from a leaf Stack + a rounded body Stack. Visually differs from the standard Apple logo. | `PagesPublic.jsx` line 555: Apple icon = `{ icon: '🍎', label: 'Apple', bg: '#1E293B', fg: '#F8FAFC' }` — uses the apple emoji. `mobile-social-buttons.html` line 10: Apple SVG path. The capture shows a clean white apple silhouette. | Minor | Use the Apple emoji `🍎` inside a styled container matching `PagesPublic.jsx` — or use a plain `Text` "🍎" (`fontSize={SIZE}`, width/height constrained). This is consistent with the design-system allowance for emoji as semantic icons. |
| m-29 | **Microsoft icon — color accuracy** | `SocialIcons.tsx` `MicrosoftIcon`: uses `$danger` (red), `$secondary` (green), `$primaryHover` (indigo), `$accent` (amber) — brand-adjacent but uses Learnexia token colors, not Microsoft brand colors. | Capture and `PagesPublic.jsx` line 556: Microsoft icon = `{ icon: '⊞', label: 'Microsoft', bg: '#1E293B', fg: '#F8FAFC' }` — rendered as a grid symbol `⊞` in white on dark. The icon is monochrome on the dark social button. | Minor | Simplify `MicrosoftIcon` to the grid symbol `⊞` rendered as `Text` `color="$fg1"` `fontSize={SIZE}`. |
| m-30 | **Social row gap** | `loginParts.tsx` `SocialRow`: `gap="$3"` (12px). | `PagesPublic.jsx` line 344: `gap: 12`. Match. | — | Accepted. |
| m-31 | **"Or continue with" divider — vertical margin** | `loginParts.tsx` `OrDivider` has no margin. Spacing is controlled by the parent `Stack gap="$4"` (16px) in `LoginForm.tsx`. | `PagesPublic.jsx` lines 338-342: the divider is wrapped in `margin: '24px 0'`. The capture shows ~24px spacing above and below the divider. Current `gap="$4"` (16px) in `LoginForm.tsx` yields less spacing. | Minor | In `LoginForm.tsx`, wrap the `OrDivider` in a `Stack` with `paddingVertical="$2"` (8px top + bottom), giving 16 + 8 + 8 = 32px effective gap around the divider, or switch the parent gap to `"$6"` (24px) only between the submit button and the footer section. |

---

## RTL-specific deltas (AR capture `web-ar/02-login.png`)

| # | Element | Current | Target | Severity | Fix |
|---|---------|---------|--------|----------|-----|
| RTL-01 | **Submit button arrow** | `'تسجيل الدخول →'` (right arrow in RTL) | AR capture: `'تسجيل الدخول ←'` (left arrow, pointing "next" in RTL) | Major | See M-18 above. |
| RTL-02 | **"Remember me" checkbox — position** | `Checkbox` in `loginParts.tsx`: `flexDirection={direction === 'rtl' ? 'row-reverse' : 'row'}`. In RTL the checkbox box moves to the right (logical start). | AR capture: checkbox box is at the right, label text to its left, which is correct RTL layout. `flexDirection="row-reverse"` achieves this. Match. | — | Accepted. |
| RTL-03 | **"Forgot password?" — end-alignment in RTL** | `LoginForm.tsx` row: `justifyContent="space-between"`. In RTL the "Forgot password?" moves to the left (logical start). | AR capture: "نسيت كلمة المرور؟" appears at the left side of the row in RTL — correct logical start alignment. Match. | — | Accepted. |
| RTL-04 | **Brand panel — logo + wordmark row in AR** | `LoginBrandPanel.tsx`: `flexDirection={rowDir}` where `rowDir = direction === 'rtl' ? 'row-reverse' : 'row'`. In RTL: logo-mark is at the right, wordmark "Learnexia" is at the left. | AR capture shows: wordmark "Learnexia" at the right, logo-mark at the left. This is the standard RTL start-to-end ordering when using `row-reverse`. In the AR capture the logo-mark (purple spark) is at the RIGHT and "Learnexia" text is to its LEFT — which is `flexDirection: 'row-reverse'` if originally logo-mark then text. But `row-reverse` would put text first visually (leftmost). Wait — the AR capture has "Learnexia" on the right, logo-mark on the left. That means in RTL the order should be natural `row` (not reversed), since in AR text starts at right. SKILL.md: "Avatar gradients and icon shapes stay the same — don't mirror them." The logo-wordmark row should use `dir=ltr` (keep order). | Minor | In `LoginBrandPanel.tsx` logo row: use `flexDirection="row"` always (do not flip for RTL) and wrap in a `dir="ltr"` container or apply `writingDirection="ltr"` so the mark + wordmark maintain left-to-right order regardless of locale. |
| RTL-05 | **OR divider text — AR copy direction** | `OrDivider` label in AR: `'أو تابع باستخدام'` rendered with `writingDirection="rtl"` and `textAlign="center"` (centered on the divider). | AR capture shows the divider text centered — RTL does not affect centering. Match. But see M-21 for copy correction. | — | Copy fix only (M-21). |
| RTL-06 | **Social row order in AR** | `SocialRow` in `loginParts.tsx`: `flexDirection={direction === 'rtl' ? 'row-reverse' : 'row'}`. In RTL, Google is rendered rightmost, Microsoft leftmost. | AR capture social row: "جوجل" (Google) at the far right, "آبل" (Apple) center, "مايكروسوفت" (Microsoft) at the far left. The `row-reverse` achieves this since Google is first in source order. Match. | — | Accepted. |
| RTL-07 | **Input text alignment in AR** | `TextField/index.tsx` `TextInput style.textAlign: dir === 'rtl' ? 'right' : 'left'`. | AR capture: email field shows `parent@email.com` left-aligned within a RTL input (because it's a Latin technical string). SKILL.md: keep Latin numerals + `dir=ltr` for emails. The email field value should stay LTR even in RTL locale. | Minor | For the email field specifically (`keyboardType="email-address"`), always use `textAlign: 'left'` and `writingDirection: 'ltr'` regardless of locale. Add a `forceLatinInput?: boolean` prop to `TextField` or check `keyboardType` in the component. |
| RTL-08 | **AR subtitle direction number** | `resources.ts` AR `login.subtitle`: `'سجّل دخولك للحفاظ على سلسلتك 🔥'` (after fix M-17). No numerals — no issue. | — | — | Accepted. |

---

## New components needed

None. All fixes are to existing components and token values. See Design Gaps below for token additions.

---

## Design gaps flagged (not silently fixed)

| Gap ID | Description | Affected element |
|--------|-------------|-----------------|
| GAP-A | `lineHeightScale` at step 7 (H1/32px) is 40px; `type-display.html` specifies `line-height:1.15` = 36.8px. A 3px discrepancy. Resolution requires updating `packages/design-system/src/fonts/index.ts` `lineHeightScale[7]` from `40` to `37` — impacts all H1 text. Flag for design-system maintainer. | All H1-scale `Text` |
| GAP-B | The brand panel background gradient `linear-gradient(165deg, #4F3FB0 0%, #3B2C8F 50%, #1E1B4B 100%)` has no named token. The three named gradients (`gradXp`, `gradReward`, `gradLevelUp`) do not include this brand-panel purple. Add `gradBrandPanel` to `packages/design-system/src/tokens/gradients.ts` and a corresponding `--lx-grad-brand-panel` CSS variable to `colors_and_type.css`. | `SplitFormScaffold` brand panel |
| GAP-C | `$border` token is `rgba(255,255,255,0.08)` but `components-input.html` resting border is `rgba(255,255,255,0.10)`. A 0.02 alpha discrepancy. Either update `$border` to 0.10 (impacts all borders) or define a separate `$borderInput` token at 0.10 alpha. | All input resting borders |
| GAP-D | Disabled button surface `#2A2D3E` (from `PagesPublic.jsx`) has no named token. It falls between `$card` (#1E293B) and `$cardSoft` (#334155). The fix (M-08) approximates with `$cardSoft` — a ~10% brighter surface than the design intent. If exact match is needed, add `$cardDisabled: '#2A2D3E'` to `colors.ts`. |Disabled `Button` |
| GAP-E | Inner-tab `borderRadius: 10` in the PersonaToggle (from `PagesPublic.jsx`) has no named token (between `$sm`=8 and `$button`=16). Local literal `10` is used in the fix. If this radius appears elsewhere, add `$xs: 10` or `$buttonInner: 10` to `radius` scale. | `PersonaToggle` inner tabs |

---

## Implementation handoff

| Fix ID | Target file | Exact change |
|--------|-------------|--------------|
| B-01 | `apps/student-app/app/(auth)/login.tsx` | Eyebrow `Text`: remove `$tablet={{ color: '$primary' }}` conditional; set `color="$primaryLight"` unconditionally |
| B-02 | `apps/student-app/app/(auth)/login.tsx` | Eyebrow `Text`: change `fontWeight="700"` → `"800"`, `letterSpacing={1.5}` → `{1.44}` |
| M-01 | `apps/student-app/app/(auth)/login.tsx` | Heading `Text`: change `fontSize={28}` → `{32}` (already 800 weight — confirm no change needed) |
| M-02 | `apps/student-app/app/(auth)/login.tsx` | Heading `Text`: add `letterSpacing={-0.64}` |
| M-03 | `apps/student-app/app/(auth)/_components/PersonaToggle.tsx` | Outer `Stack borderRadius`: `"$pill"` → `"$button"` (16px). Inner tab `borderRadius`: `"$pill"` → `{10}` |
| M-05 | `packages/shared/src/i18n/resources.ts` | `auth.login.personaParent` EN → `"👨‍👩‍👦 I'm a Parent"` / AR → `"👨‍👩‍👧 أنا ولي أمر"`. `personaStudent` EN → `"🎓 I'm a Student"` / AR → `"🎓 أنا طالب"` |
| M-06 | `packages/ui/src/components/TextField/index.tsx` | Replace emoji reveal toggle with a `Text` node rendering the i18n-resolved show/hide label string. Add `showLabel?: string` / `hideLabel?: string` props to `TextFieldProps`, default to `'Show'`/`'Hide'`. Color: `$primaryLight`, `fontSize: 12`, `fontWeight: '600'` |
| M-07 | `packages/ui/src/components/TextField/index.tsx` | Move reveal toggle from inside the input container to the label row. Add a `labelRight` slot in the `YStack > Text` label row: `<Stack flexDirection="row" justifyContent="space-between"><Text label .../>{secureTextEntry && <revealToggle />}</Stack>` |
| M-08 | `packages/ui/src/components/Button/index.tsx` | Disabled variant: remove `opacity: 0.4`. Change `backgroundColor` from `'$card'` to `'$cardSoft'`. Remove any opacity on the disabled frame |
| M-09 | `packages/ui/src/components/Button/index.tsx` | Primary variant: add web-only `boxShadow` with inset: `$platform-web: { boxShadow: '0 4px 12px rgba(99,102,241,0.4), inset 0 1px 0 rgba(255,255,255,0.2)' }`. Keep native `shadowColor`/`shadowOpacity` unchanged |
| M-10 | `apps/student-app/app/(auth)/_components/loginParts.tsx` | `SocialButton borderRadius`: `"$button"` → `{14}`. Add `const socialButtonRadius = 14` local constant |
| M-12 | `apps/student-app/app/(auth)/_components/loginParts.tsx` | `SocialButton pressStyle`: `{ scale: 0.97 }` → `{ scale: 0.95 }` |
| M-13 | `apps/student-app/app/(auth)/_components/LoginBrandPanel.tsx` | Logo-mark container `borderRadius`: `"$button"` → `"$card"` |
| M-14 | `apps/student-app/app/(auth)/_components/LoginBrandPanel.tsx` | Star `fontSize`: `{72}` → `{96}` |
| M-15 | `apps/student-app/app/(auth)/_components/LoginBrandPanel.tsx` | Brand tagline `fontSize`: `{44}` → `{48}`, `fontWeight`: `"800"` → `"900"` |
| M-16 | `packages/shared/src/i18n/resources.ts` | AR `auth.login.brand.socialProof`: `'أكثر من 240,000 طفل يتعلمون اليوم'` → `'أكثر من ٢٤٠٬٠٠٠ طفل يتعلمون اليوم'` |
| M-17 | `packages/shared/src/i18n/resources.ts` | AR `auth.login.subtitle`: `'سجّل دخولك لمواصلة سلسلة إنجازاتك 🔥'` → `'سجّل دخولك للحفاظ على سلسلتك 🔥'` |
| M-18 | `packages/shared/src/i18n/resources.ts` | AR `auth.login.submitButton`: `'تسجيل الدخول →'` → `'تسجيل الدخول ←'` |
| M-19 | `apps/student-app/app/(auth)/_components/loginParts.tsx` | `OrDivider Text color`: `"$fg3"` → `"$fg4"`. `letterSpacing`: `{1}` → `{0.48}` |
| m-01 | `apps/student-app/app/(auth)/login.tsx` | Heading `Text`: add `lineHeight={37}` (interim fix until GAP-A is resolved in design-system) |
| m-04 | `packages/ui/src/components/TextField/index.tsx` | Label `letterSpacing`: `{0.6}` → `{0.72}` |
| m-05 | `packages/ui/src/components/TextField/index.tsx` | Input wrapper `height`: `{52}` → `{48}`. `TextInput style.height`: `52` → `48` |
| m-07 | `packages/ui/src/components/TextField/index.tsx` | `placeholderTextColor`: `"#94A3B8"` → `colors.fg3` (import from `@learnexia/design-system`) |
| m-09 | `apps/student-app/app/(auth)/_components/LoginForm.tsx` | "Forgot password?" `Text fontSize`: `{14}` → `{12}` |
| m-10 | `apps/student-app/app/(auth)/_components/loginParts.tsx` | Checkbox box: `width/height` `{20}` → `{22}`. `borderRadius`: `"$sm"` → `{6}` |
| m-11 | `apps/student-app/app/(auth)/_components/loginParts.tsx` | Checkbox label `fontSize`: `{14}` → `{13}` |
| m-12 | `apps/student-app/app/(auth)/_components/loginParts.tsx` | Checkbox label `color`: `"$fg3"` → `"$fg2"` |
| m-13 | `apps/student-app/app/(auth)/login.tsx` | "Create parent account" link `fontWeight`: `"600"` → `"800"` |
| m-15 | `apps/student-app/src/components/FormScaffold.tsx` | Brand panel `Stack`: add `$platform-web: { background: 'linear-gradient(165deg,#4F3FB0 0%,#3B2C8F 50%,#1E1B4B 100%)' }`. Keep `backgroundColor="$primary"` for native |
| m-16 | `apps/student-app/src/components/FormScaffold.tsx` | Add 12 particle dots inside brand panel (web-only, `position="absolute"`, `backgroundColor="rgba(255,255,255,0.25)"`, varying `width/height 3–5`, scattered positions from `PagesPublic.jsx`) |
| m-17 | `apps/student-app/app/(auth)/_components/LoginBrandPanel.tsx` | Social proof `Text color`: `"$fg2"` → `"$fg3"`. `fontSize`: `{14}` → `{13}` |
| m-18 | `apps/student-app/app/(auth)/login.tsx` | Eyebrow `textTransform`: `"uppercase"` → `direction === 'rtl' ? 'none' : 'uppercase'` |
| m-19 | `packages/shared/src/i18n/resources.ts` | AR `auth.login.newParent`: `'جديد على ليرنيكسيا؟'` → `'جديد على Learnexia؟'` |
| m-21 | `packages/shared/src/i18n/resources.ts` | AR `auth.login.orContinueWith`: `'أو تابع باستخدام'` → `'أو واصل بـ'` |
| m-24 | `packages/ui/src/components/TextField/index.tsx` | Focused state (web): add `$platform-web: { boxShadow: '0 0 0 2px #4F46E5, 0 0 0 6px rgba(99,102,241,0.35)' }` to the input wrapper Stack; remove `shadowRadius`/`shadowColor`/`shadowOpacity` from web platform |
| m-26 | `apps/student-app/app/(auth)/_components/SocialIcons.tsx` | `SIZE`: `18` → `20` |
| m-27 | `apps/student-app/app/(auth)/_components/SocialIcons.tsx` | `GoogleIcon`: simplify to white `borderRadius={9999}` disc (`SIZE×SIZE`) + dark "G" `Text`. Remove four-quadrant colored segments |
| m-28 | `apps/student-app/app/(auth)/_components/SocialIcons.tsx` | `AppleIcon`: replace custom shape with `Text fontSize={SIZE}`"🍎" inside a `SIZE×SIZE` container |
| m-29 | `apps/student-app/app/(auth)/_components/SocialIcons.tsx` | `MicrosoftIcon`: replace four-Stack grid with `Text color="$fg1" fontSize={SIZE}`"⊞" inside a `SIZE×SIZE` container |
| RTL-04 | `apps/student-app/app/(auth)/_components/LoginBrandPanel.tsx` | Logo row: change `flexDirection={rowDir}` to `flexDirection="row"` always; wrap the row `Stack` with a `dir="ltr"` attribute (web) or `writingDirection="ltr"` to preserve LTR mark→wordmark order |
| RTL-07 | `packages/ui/src/components/TextField/index.tsx` | When `keyboardType === 'email-address'`, force `style.textAlign: 'left'` and `style.writingDirection: 'ltr'` regardless of `dir` prop |
| GAP-B | `packages/design-system/src/tokens/gradients.ts` | Add `gradBrandPanel: 'linear-gradient(165deg, #4F3FB0 0%, #3B2C8F 50%, #1E1B4B 100%)'` and add `--lx-grad-brand-panel` to `design-system/colors_and_type.css` |
| GAP-D | `packages/design-system/src/tokens/colors.ts` | (Optional) Add `cardDisabled: '#2A2D3E'` if the `$cardSoft` approximation is not close enough after visual review |

---

## Accepted deferrals (intentional deviations from capture)

1. **Language/theme toggle** (`LocaleThemeControls`) — the top-corner locale+theme switch is an added affordance not present in the capture (predates the feature). Intentional deviation; do not remove.
2. **Social auth no-op** — Google/Apple/Microsoft buttons are UI stubs. Capture shows them as interactive; implementation disables the OAuth flow until the backend story lands. Intentional.
3. **Flash message banner** — the session-expired flash card is a runtime-only element; not visible in the static capture. No deviation.
4. **`LocaleThemeControls` appearance** — the language pill switch and theme button are additions; not in the capture. Intentional.

---

## Counts

| Severity | Count |
|----------|-------|
| Blocker | 2 |
| Major | 19 |
| Minor | 31 |
| Accepted | 10 |
| Design Gaps flagged | 5 |
