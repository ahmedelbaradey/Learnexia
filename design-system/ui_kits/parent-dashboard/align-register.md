# Pixel-Alignment Delta — Register Screen
> Targets: `design-system/screenshots/web/03-register.png` (EN/LTR) and `design-system/screenshots/web-ar/03-register.png` (AR/RTL).
> Current implementation: `apps/student-app/app/(auth)/register.tsx` + `_components/RegisterForm.tsx` + `_components/RegisterFeaturePanel.tsx` + `apps/student-app/src/components/FormScaffold.tsx` + `packages/ui/src/components/Button/index.tsx` + `packages/ui/src/components/TextField/index.tsx` + `packages/ui/src/components/CheckboxField/index.tsx` + `packages/ui/src/components/Select/index.tsx`.
> Proving cards cited per row: `preview/web-auth-split.html`, `preview/web-benefits-panel.html`, `preview/components-input.html`, `preview/ar-input.html`, `preview/mobile-password-meter.html`, `preview/components-buttons.html`, `colors_and_type.css`, `ui_kits/parent-dashboard/PagesPublic.jsx` (`RegisterWebPage`).

---

## BLOCKER

| # | Element | Current | Target [token + proving card] | Severity | Fix |
|---|---------|---------|-------------------------------|----------|-----|
| B-1 | PasswordStrengthMeter primitive | **Absent — no component exists anywhere in the monorepo** (`packages/ui/src` scan returned zero `PasswordStrength*` files). HANDOFF already flags it as a pending primitive. | Screenshot `03-register.png` shows 8 masking dots and a plain "At least 6 characters" helper. The `mobile-password-meter.html` card defines a 4-segment meter (Weak/Fair/Good/Strong) with colors: `#EF4444` / `#F59E0B` / `#FACC15` / `#22C55E`, segments `h:4px radius:9999px gap:4px`, label `font-weight:800 font-size:12px`. The capture does NOT show the meter in the filled state; only the helper hint is visible. But the helper currently does not transition to a meter on typing. | **Blocker** | Create `packages/ui/src/components/PasswordStrengthMeter/index.tsx`. Spec: 4 pill segments, `height:4px`, `gap:4px`, `borderRadius:9999px`, inactive segment `rgba(255,255,255,0.08)`. States: 1 seg=`$danger`, 2 segs=`$accent`, 3 segs=`$xp`, 4 segs=`$secondary`. Label `fontSize:12 fontWeight:800 color=matching token`. Render below password `TextField` in place of or alongside the plain hint, only when `value.length > 0`. Wire to `registerParentSchema` strength scorer (6+ chars = segment 2+; uppercase+lowercase+digit = segment 3+; special char = segment 4). |
| B-2 | Progress bar gradient missing | `register.tsx` renders the 50% fill as a plain `backgroundColor="$primary"` (solid indigo). | Screenshot `03-register.png` shows the filled half as a `linear-gradient(90deg, #A855F7, #4F46E5)` (purple → indigo). `PagesPublic.jsx` line 391: `background:'linear-gradient(90deg,#A855F7,#4F46E5)'`. Token: `--lx-grad-levelup` (`#A855F7 → #6366F1`). | **Blocker** | Replace the fill `Stack` with a `GradientBox` (or inline `backgroundImage` on web via Tamagui's `style` escape). Use `gradients.levelup` from `packages/design-system/src/tokens/gradients.ts`. LTR: left-to-right. RTL: the bar itself must stay `direction:ltr` (progress bars read L→R universally per SKILL.md §Skill 4 rule 6). |
| B-3 | Button disabled state background | `Button` index.tsx `disabled` variant: `backgroundColor: '$card'` (`#1E293B`) with `opacity: 0.4`. PagesPublic `RegisterWebPage` line 462-464 disabled button style: `background:'#2A2D3E'`, `color:'#64748B'`, no opacity. Screenshot shows disabled CTA as a dark slate chip — visually matching `#2A2D3E` not `#1E293B@0.4`. The `#1E293B@0.4` renders too light. | Capture `03-register.png`: disabled CTA is perceptibly a flat mid-slate, no indigo tint, text muted. `components-buttons.html` `.disabled`: `background:#1E293B; color:#64748B`. `colors_and_type.css`: `--lx-card: #1E293B`. The kit card matches the Button component, not `#2A2D3E`. **Delta:** the button component matches the design kit; the PagesPublic kit card DIVERGES. The screenshot, which is the pixel-perfect target, matches the `#1E293B` card token (after opacity application it reads the same). This is NOT a code bug. **No fix needed on Button** — but the FormScaffold must NOT override Button disabled colour. | **Blocker** | Verify in browser that disabled CTA with `$card @0.4` opacity visually matches the screenshot. If the result reads too transparent on the dark bg, set `opacity: 0.6` instead of `0.4` on the `disabled` variant. Decision: raise to reviewer. |

---

## MAJOR

| # | Element | Current | Target [token + proving card] | Severity | Fix |
|---|---------|---------|-------------------------------|----------|-----|
| M-1 | Input height | `TextField/index.tsx`: `height={52}` (Stack + TextInput `style.height:52`). | Screenshot `03-register.png` + `PagesPublic RegisterWebPage` `webInputStyle()`: `height:48`. `components-input.html` `.input`: `height:48px`. `ar-input.html` div height matches `48px`. The spec token `--lx-size-h3: 18px` is irrelevant; the target is **48px** for web inputs. | **Major** | Change `TextField` `height` from 52 → 48 on web. Strategy: add a `$tablet` responsive prop or a platform gate so mobile keeps 52 (thumb target) and web renders 48. Alternatively apply universally at 48 and add `hitSlop` for native. Same 48px fix applies to `Select` trigger row. |
| M-2 | Input border-radius | `TextField` and `Select`: `inputRadius = 14` (local constant). | `PagesPublic webInputStyle()`: `borderRadius:14` (matches). `components-input.html` `.input`: `border-radius:14px` (matches). Screenshot: inputs show a noticeably rounded rect but less than card (20px). 14px is correct per the single-use gap token. **Delta: label casing** — `TextField` renders `textTransform:"uppercase"` with `letterSpacing:0.6` (matches kit). No radius fix needed. | **Major** | No radius fix. Confirm `Select` also uses `inputRadius=14` (it does). |
| M-3 | Checkbox background when unchecked | `CheckboxField`: `backgroundColor={checked ? '$primary' : 'transparent'}`. | `PagesPublic` line 443: unchecked checkbox row `background: '#1E293B'` — the *row* wrapper has a card fill, not the box. `03-register.png`: the checkbox container row has background `#1E293B`-ish, with the checkbox box itself transparent-bordered. The component wrapper currently has no `background` on its outer `XStack`. The capture shows the checkbox row embedded in a light-card (`$card` = `#1E293B`) container. | **Major** | The `CheckboxField` component does not render a card wrapper — the form wraps it. In `RegisterForm.tsx`, the `CheckboxField` is bare. In `PagesPublic`, the entire checkbox label row is wrapped in a styled div with `background: agreed ? 'rgba(34,197,94,0.06)' : '#1E293B'`. Add a `Stack` wrapper around `CheckboxField` in `RegisterForm.tsx` with `backgroundColor="$card"` `borderRadius={14}` `padding="$3 $4"` `borderWidth={1}` `borderColor={checked ? '$primary' : '$border'}`. On `checked=true`, add subtle success tint: `backgroundColor="rgba(34,197,94,0.06)"` `borderColor="rgba(34,197,94,0.3)"`. This matches both the screenshot and PagesPublic spec. |
| M-4 | Checkbox check mark token | `CheckboxField`: checked renders `✓` as a unicode char in `color="$fg1"` (white). | Screenshot AR `03-register.png`: green check on purple/indigo box. `PagesPublic` line 452: check char `'✓'`, `color:'#0F172A'` (dark/inverse on the `#22C55E` green box). BUT the component uses `backgroundColor={checked ? '$primary' : 'transparent'}` — indigo box, not green. Screenshot EN shows unchecked. Screenshot AR shows checked (indigo bg). `$fg1` white on indigo is readable. However the `PagesPublic` kit uses green (`#22C55E`) for the checked box, not indigo. The `03-register.png` capture shows a green checkbox (filled). | **Major** | Change `CheckboxField` checked background from `$primary` → `$secondary` (green `#22C55E`) and check mark colour from `$fg1` (white) to `$fgInverse` (`#0F172A` dark) to match PagesPublic + screenshot. |
| M-5 | Feature panel icon chip size | `RegisterFeaturePanel`: game-controller chip `width={80} height={80}` `borderRadius="$pill"` `backgroundColor="$primaryHover"`. | `PagesPublic RegisterWebPage` right panel: `🎮 font-size:96` as a bare emoji (no chip). Screenshot `03-register.png` right panel: large game-controller emoji at ~96px, displayed bare in a circular translucent container approximately 120×120px. `web-benefits-panel.html`: bare `font-size:60px` emoji with `filter:drop-shadow`. Current code wraps in a pill chip 80×80 with `$primaryHover` fill — chip is visible and correct but smaller than the screenshot. | **Major** | Increase game-controller chip to `width={100} height={100}` `fontSize={48}`. Or strip the chip entirely and render the emoji bare at `fontSize={80}` with a `filter:drop-shadow(0 0 20px rgba(250,204,21,0.5))`. The screenshot shows NO visible chip border, just the large emoji. Recommended: match `web-benefits-panel.html` approach — bare emoji, drop-shadow glow. |
| M-6 | Feature panel headline font size | `RegisterFeaturePanel`: `fontSize={40} lineHeight={46}`. | Screenshot `03-register.png`: headline "Set up once. Watch them learn forever." renders at approximately 40px. `PagesPublic` line 482: `fontSize:40`. `web-benefits-panel.html`: `font-size:26px` (card is compact). The full-page PagesPublic spec at 40px matches the screenshot. **Font-size is correct.** However `fontWeight="800"` is used; spec says `fontWeight:900` in PagesPublic. Token closest: `--lx-weight-black: 800`. Both 800 and 900 are "black" weight — Poppins Black is at 900. | **Major** | Change headline `fontWeight` from `"800"` → `"900"` (`$black` = 900, matching PagesPublic `fontWeight:900`). Also add `letterSpacing={-0.02 * 40}` (`-0.8em equivalent → -0.02em`) per `--lx-tracking-tight`. Current code has no `letterSpacing` on the headline. |
| M-7 | Feature panel bullet icon chip radius | `RegisterFeaturePanel` bullets: `borderRadius="$button"` (16px). | `PagesPublic` bullet icon chips: `borderRadius:12`. `web-benefits-panel.html`: `border-radius:10px`. Screenshot bullets: visible small rounded square chips. `$button` = 16px is slightly too round. | **Major** | Change bullet icon chip `borderRadius` from `"$button"` (16) → 12 (inline value; no matching token, same gap as `inputRadius`). |
| M-8 | Feature panel bullet text colour | `RegisterFeaturePanel` bullets: `color="$fg2"` (`#CBD5E1`). | `PagesPublic` bullets: `color:'rgba(255,255,255,0.92)'`. Screenshot: bullets appear near-white. `$fg2` = `#CBD5E1` is `rgb(203,213,225)` ≈ 88% white — close but slightly cooler. `rgba(255,255,255,0.92)` is warmer. | **Major** | Change bullet `Text color` from `"$fg2"` → `"$fg1"` (`#F8FAFC`) at 92% opacity — apply `opacity={0.92}` on the Text, or use `rgba(248,250,252,0.92)` via inline style. |
| M-9 | Step eyebrow + progress bar: layout direction | `register.tsx` lines 63-89: eyebrow label renders first, then progress bar as a separate row. `PagesPublic` line 387-391: eyebrow and bar are rendered in the same `flex` row — label LEFT, bar fills the remaining horizontal space. | Screenshot `03-register.png`: eyebrow text "STEP 1 OF 2" is inline LEFT of the progress bar (same row), bar runs to the right edge. Current code renders them stacked (`gap="$2"` in a column). | **Major** | In `register.tsx`, change the step+progress section from a column to a row: `<Stack flexDirection="row" alignItems="center" gap="$3">` — eyebrow text at natural width, progress `Stack` `flex={1}`. RTL: `flexDirection={rowDir}` (already have the `rowDir` variable). The bar fill remains `direction:ltr` internally. |
| M-10 | Logo chip size | `register.tsx` logo chip: `width={40} height={40}` `borderRadius="$button"`. | Screenshot `03-register.png`: logo mark chip visually ~36×36px or 40×40px. `PagesPublic` line 382: `<img width:36 height:36>`. `web-auth-split.html` line 6: `width:28 height:28`. The existing 40×40 matches the PagesPublic `RegisterWebPage` call more closely. No change needed for the icon mark. However the wordmark `fontSize={22} fontWeight="800"` vs PagesPublic `fontSize:20 fontWeight:900`. | **Major** | Change wordmark `fontWeight` from `"800"` → `"900"`. `fontSize` 22 vs 20 is acceptable (22 is slightly larger and reads better). Keep 22 unless reviewer decides to match exactly. Also: `common.appName` resolves to `'Learnexia'` in EN and `'ليرنيكسيا'` in AR. The brand name should remain Latin script `Learnexia` with `dir="ltr"` in both locales (SKILL.md §Skill 4: keep brand name Latin). Add `dir="ltr"` force on the wordmark Text in RTL context. |
| M-11 | Progress bar height | `register.tsx`: progress container `height={6}`, fill `height={6}`. | `PagesPublic` progress bar: `height:4` container/fill. Screenshot: bar appears as a thin 4px line. | **Major** | Change progress container and fill height from 6 → 4 (`--lx-space-1: 4px`). |
| M-12 | Subtitle colour | `register.tsx` subtitle: `color="$fg3"` (`#94A3B8`). | Screenshot + `PagesPublic` line 394: subtitle `color:'#94A3B8'` — same as `$fg3`. **Matches.** | Confirmed OK | No fix. |
| M-13 | ParentOnlyBanner border colour | `register.tsx` `ParentOnlyBanner`: `borderColor="$primary"` (`#4F46E5` solid). | `PagesPublic` line 400: `border: '1px solid rgba(168,85,247,0.3)'` (purple 30%). `$purple` tint matches screenshot banner border. `$border` is rgba 8% white — too faint. | **Major** | Change banner `borderColor` from `"$primary"` → `"rgba(168,85,247,0.3)"` (inline; no matching token). |
| M-14 | ParentOnlyBanner background | `register.tsx`: `backgroundColor="$primarySoft"` (`rgba(79,70,229,0.18)` — indigo tint). | `PagesPublic` line 399: `background:'rgba(168,85,247,0.1)'` (purple 10%). Screenshot banner has a subtle purple tint, not indigo. Token `$purpleSoft = rgba(168,85,247,0.18)` is close but 18% vs 10%. | **Major** | Change `backgroundColor` from `"$primarySoft"` → `"$purpleSoft"` which is `rgba(168,85,247,0.18)`. This is slightly stronger than PagesPublic's 10% but the screenshot is indistinct between 10–18% at this colour — use `$purpleSoft` to stay token-only. |
| M-15 | ParentOnlyBanner title colour | `register.tsx`: `color="$primaryLight"` (`#A5B4FC` — indigo-300). | `PagesPublic` line 404: `color:'#A855F7'` (purple). Screenshot: banner title is purple, not light-indigo. Token: `$purple = #A855F7`. | **Major** | Change `color="$primaryLight"` → `"$purple"`. |
| M-16 | SplitFormScaffold form column max-width | `FormScaffold.tsx` SplitFormScaffold right column inner content: `$tablet={{ maxWidth: 440, paddingHorizontal: 0 }}`. | `PagesPublic RegisterWebPage` form column: `maxWidth:560 margin:'0 auto'` with `padding:'56px'`. Screenshot form column: content appears ~500-520px wide with generous 56px side padding. 440 inner width + 48px (=$6) outer horizontal padding = ~536px total — close but the inner content width is 96px less than PagesPublic. | **Major** | Change inner content max-width from 440 → 500 in `SplitFormScaffold`. The outer `$6` (24px×2) padding still applies; total visible width ~548px which matches the PagesPublic `560 - 2×56 = 448` inner width. Note: changing FormScaffold affects ALL split screens (login too) — check login capture before committing. |
| M-17 | AR: Step eyebrow copy mismatch | AR `resources.ts` `ar.auth.register.step`: `'الخطوة 1 من 2'`. | Screenshot `03-register.png` AR: eyebrow reads `الخطوة ١ من ٢` (Eastern-Arabic numerals). SKILL.md §Skill 4 rule 5: use Eastern-Arabic numerals for inline text. Current AR string uses Latin `1` and `2`. | **Major** | In `ar.auth.register.step`, change `'الخطوة 1 من 2'` → `'الخطوة ١ من ٢'`. Similarly check `ar.auth.register.progressA11y`: `'تقدّم التسجيل: الخطوة 1 من 2'` → `'تقدّم التسجيل: الخطوة ١ من ٢'`. |
| M-18 | AR: feature title copy mismatch | AR `resources.ts` `ar.auth.register.feature.title`: `'أعدّه مرة واحدة. وشاهدهم يتعلمون للأبد.'` | Screenshot `03-register.png` AR panel: `جهّز الحساب مرة. شاهدهم يتعلمون للأبد.` (shorter phrasing). `PagesPublic` index-ar.html register panel text (read from captured right panel): matches the screenshot phrasing. | **Major** | Align AR feature title. Update `ar.auth.register.feature.title` → `'جهّز الحساب مرة. شاهدهم يتعلمون للأبد.'`. Verify with native-Arabic speaker before shipping. |
| M-19 | AR: parentOnlyTitle / parentOnlyBody copy | AR `resources.ts`: `parentOnlyTitle: 'لولي الأمر فقط'`, `parentOnlyBody: 'لا يمكن للأطفال التسجيل بأنفسهم. ستنشئ حساباتهم في الخطوة التالية.'` | Screenshot AR: banner title `ولي أمر / وصي قانوني فقط`, body starts with `الأطفال لا يستطيعون التسجيل بأنفسهم. ستنشئ حساباتهم في الخطوة التالية.` — the title includes the slash-separated variant. | **Major** | Update `ar.auth.register.parentOnlyTitle` → `'ولي أمر / وصي قانوني فقط'`. Body `ar.auth.register.parentOnlyBody` starts with `الأطفال لا يستطيعون التسجيل...` — update to match. Verify with native-Arabic speaker. |
| M-20 | Button CTA copy arrow | EN `resources.ts` `submitButton`: `'Continue → Add Children'`. | Screenshot EN: button reads `Continue → Add Children`. Matches. AR `submitButton`: `'متابعة ← إضافة الأطفال'`. Screenshot AR: button reads `متابعة ← إضافة الأطفال`. Matches. **No fix for copy.** However the EN arrow `→` should be a Unicode `→` char (U+2192), and in RTL the arrow flips to `←`. The i18n string already contains the correct arrow per locale. **Verify**: the `Button` component renders `children` as a `Text` node; the arrow must remain as part of the translated string, not appended by JS logic. Confirmed OK — no change. | OK | No fix needed. |
| M-21 | AR: email field value — must remain LTR | `TextField` renders `textAlign: dir === 'rtl' ? 'right' : 'left'`. | SKILL.md §Skill 4 rule 5: email addresses must stay Latin+`dir=ltr`. The screenshot AR shows the email field content `parent@email.com` displayed left-to-right. The label "البريد الإلكتروني" is RTL-aligned. | **Major** | Add a special prop `forceValueLtr?: boolean` to `TextField`. When `true`, set `writingDirection: 'ltr'` and `textAlign: 'left'` on the `TextInput` regardless of `direction`. Use this on the email field in `RegisterForm`. Also apply to the child's email field in the add-child form. |
| M-22 | AR: country select text alignment | `Select` renders selected label with `textAlign:"left"` regardless of `dir`. | Screenshot AR: country select label "السعودية" is right-aligned (RTL). `Select/index.tsx` line 129: `textAlign:"left"` is hardcoded (not using `dir`). | **Major** | Change `Select` text label `textAlign` from hardcoded `"left"` to `dir === 'rtl' ? 'right' : 'left'`. Same fix for the error message `textAlign`. |

---

## MINOR

| # | Element | Current | Target [token + proving card] | Severity | Fix |
|---|---------|---------|-------------------------------|----------|-----|
| m-1 | TextField label `textAlign` | `TextField`: label `textAlign="left"` hardcoded. | AR screenshot: label "الاسم الكامل" is right-aligned. `ar-input.html` label `font-family:'Cairo'` — no explicit `text-align` but as RTL block it aligns right. | **Minor** | Change `TextField` label `textAlign` from hardcoded `"left"` to `dir === 'rtl' ? 'right' : 'left'`. |
| m-2 | Full-name + Country row gap | `RegisterForm.tsx`: `Stack gap="$4"` (16px). | `PagesPublic` two-column grid: `gap:14`. Screenshot: columns have a visible gap close to 14px. `$3` = 12px, `$4` = 16px — 14 is between tokens. | **Minor** | Use `gap="$3"` (12px) as the closest token below 14px, or accept `$4`. No new token needed. |
| m-3 | Field-to-field vertical gap | `RegisterForm.tsx` outer `Stack gap="$4"` (16px). | `PagesPublic` fields use `WebField` which has `marginBottom:14` each. 14px vs 16px. | **Minor** | Acceptable. Keep `$4` (16px) which gives slightly more breathing room. No fix required. |
| m-4 | Input placeholder colour | `TextField`: `placeholderTextColor="#94A3B8"` (hardcoded hex). | Token `--lx-fg3: #94A3B8` = `$fg3`. Same hex value — no visual difference. | **Minor** | Replace hardcoded hex with `colors.fg3` import from `packages/design-system/src/tokens/colors.ts`. No visual change. |
| m-5 | Password eye-toggle icon | `TextField`: `{revealed ? '🙈' : '👁'}` emoji. | Screenshots show no eye icon visible in the password field (dots only). PagesPublic has no reveal toggle on the register password field. SKILL.md: emoji must be semantic only. 🙈 is decorative here. | **Minor** | Replace 🙈 with a Lucide `EyeOff` SVG icon at 18px, `$fg3` colour. Replace 👁 with Lucide `Eye`. This aligns with `design-system/README.md` iconography rule (Lucide primary set). |
| m-6 | ParentOnlyBanner emoji | `register.tsx` `ParentOnlyBanner`: `👨‍👩‍👧` (family ZWJ sequence). | `PagesPublic` line 402: `👨‍👩‍👦` (man+woman+boy). Screenshot: shows what appears to be a family emoji. Both are semantically equivalent but differ by last child emoji. | **Minor** | Use `👨‍👩‍👧` consistently (matches the `ar` screenshot which may show the girl variant). Confirm with design team which family emoji is official. |
| m-7 | Feature panel bullet vertical gap | `RegisterFeaturePanel` bullets: `gap="$5"` (20px). | `PagesPublic` bullets: `gap:14`. `web-benefits-panel.html`: `gap:10px`. Screenshot bullets: gap looks ~16-20px. | **Minor** | Reduce from `$5` (20) → `$4` (16) to reduce vertical whitespace slightly. |
| m-8 | Feature panel max-width on bullets | `RegisterFeaturePanel` bullets container: `maxWidth={520}`. | `PagesPublic` bullets container: `maxWidth:460`. Screenshot panel width: 50% of 1280 = 640px, with 56px padding = ~528px content. 460 max looks correct to prevent overly long lines. | **Minor** | Change bullets `maxWidth` from 520 → 460. |
| m-9 | Checkbox box border in unchecked state | `CheckboxField`: `borderColor={hasError ? '$danger' : checked ? '$primary' : '$borderStrong'}`. Unchecked = `$borderStrong` (`rgba(255,255,255,0.16)`). | `PagesPublic` unchecked box: `border:'2px solid rgba(255,255,255,0.2)'`. `rgba(0,255,255,0.2)` vs `rgba(255,255,255,0.16)` — very close. Screenshot: faint white border. | **Minor** | No functional fix. The 0.16 vs 0.2 difference is imperceptible on screen. Acceptable. |
| m-10 | Focus ring implementation | `TextField` / `Select`: focus state sets `borderColor="$borderFocus"` + `shadowColor` + `shadowOpacity`. | `components-input.html` `.focus`: `box-shadow: 0 0 0 4px rgba(99,102,241,0.25)` + `border-color:#4F46E5`. `borders-focus.html`: `0 0 0 2px #4F46E5` inner + `0 0 0 6px rgba(99,102,241,0.45)` outer. Current `shadowRadius:4` and `shadowOffset:{0,0}` approximates the glow but does not produce a true ring on React Native Web. | **Minor** | On web (platform gate), replace `shadowColor/shadowOpacity/shadowRadius` with a CSS `boxShadow: '0 0 0 4px rgba(99,102,241,0.25)'` via Tamagui's `$platform-web` media prop. This matches `components-input.html` exactly. |
| m-11 | Button `letterSpacing` | `Button/index.tsx`: `letterSpacing={0.16}` (raw pt value). | `components-buttons.html` `.btn`: `letter-spacing:0.01em`. 0.16pt ≈ 0.01em at 16px. Functionally the same. | **Minor** | No fix needed. |
| m-12 | RTL: Feature panel game chip self-alignment | `RegisterFeaturePanel`: `alignSelf={direction === 'rtl' ? 'flex-end' : 'flex-start'}`. | Screenshot AR: game controller sits at top-left of the right panel (which is now the LEFT visual panel in RTL). Since the panel is on the LEFT in RTL split, `flex-end` will push the chip to the visual right — incorrect. The chip should always align to the START of its column's reading direction. | **Minor** | Change to `alignSelf="flex-start"` unconditionally. In RTL the entire column is already mirrored by the parent `row-reverse`; the chip's own alignment should remain `flex-start` (which is visually right-to-left start). If RTL layout is `row-reverse` the panel comes first visually on left — `flex-start` keeps the chip at the left edge of the left panel, matching the screenshot. |
| m-13 | `haveAccount` row margin-top | `RegisterForm.tsx`: `marginTop="$2"` (8px) on the "Already have an account?" row. | `PagesPublic` line 469: `marginTop:20`. Screenshot: visible gap between CTA and the sign-in link. `$5` = 20px matches PagesPublic. | **Minor** | Change `marginTop="$2"` → `marginTop="$5"` on the bottom "haveAccount" row. |
| m-14 | TextField label `writingDirection` not set on error | `TextField` error `Text` (line 178): `textAlign="left"` hardcoded, no `writingDirection`. | AR screenshot: error text should be RTL-aligned. | **Minor** | Change error `Text` `textAlign` from hardcoded `"left"` to `dir === 'rtl' ? 'right' : 'left'`. Companion to M-21. |
| m-15 | `passwordHelper` hint font size | `register.tsx` password helper: `fontSize={12}`. | `PagesPublic` `WebField hint`: `fontSize:11`. Screenshot: helper text is small, close to 11-12px. 12px matches `--lx-size-small`. | **Minor** | Keep 12px (`$small`). No fix. |
| m-16 | AR: `common.appName` used in logo wordmark | `register.tsx` logo renders `{t('common.appName')}` → AR: `'ليرنيكسيا'`. | SKILL.md §Skill 4: brand name `Learnexia` stays Latin + `dir=ltr`. Screenshot AR: wordmark is the Latin text `Learnexia` in both EN and AR screenshots (see chrome URL bar area — brand name never transliterated in the UI). | **Minor** | Do NOT use `common.appName` for the visual wordmark. Hardcode `"Learnexia"` in the logo `Text` with `dir="ltr"` (or use a conditional `direction === 'rtl' ? 'Learnexia' : t('common.appName')`). The translated app name `'ليرنيكسيا'` is for a11y / voice-over only, not the visual mark. |

---

## Design Gaps (no silent fixes)

| Gap | Description |
|-----|-------------|
| GAP-DS-1 | **PasswordStrengthMeter** — not in design kit (`preview/`) and not in `packages/ui`. Must be built as a new primitive. Spec fully defined by `mobile-password-meter.html`. |
| GAP-DS-2 | **Gradient progress bar** — no `GradientBox`-wrapped progress bar primitive exists. Current `GradientBox` component is generic; a `ProgressBar` wrapper using `--lx-grad-levelup` needs defining, or the fill `Stack` must use a web `backgroundImage` escape. |
| GAP-DS-3 | **`forceValueLtr` prop on TextField** — not in spec, not in current component. Needed for email inputs in RTL locale per SKILL.md rules. |
| GAP-DS-4 | **Checkbox wrapper card** — `CheckboxField` is a bare component; the card-frame wrapper (bg + border + checked-tint) is specified by `PagesPublic` but is not part of the component. It is caller-side styling in `RegisterForm`. This is intentional (flexible), but must be documented: callers must add the wrapper. |
| GAP-DS-5 | **Eastern-Arabic numeral policy in i18n strings** — currently only enforced inconsistently. The AR `resources.ts` needs a systematic audit for any inline numerals that should be Eastern-Arabic (step label, count, XP values). |

---

## Copy Cheat Sheet — Register Screen (EN + AR)

| Key | EN | AR (target) |
|-----|----|-------------|
| `auth.register.step` | `Step 1 of 2` | `الخطوة ١ من ٢` (Eastern-Arabic) |
| `auth.register.title` | `Create your parent account` | `أنشئ حساب ولي الأمر` |
| `auth.register.subtitle` | `You'll add your children's accounts next.` | `ستضيف حسابات أطفالك في الخطوة التالية.` |
| `auth.register.parentOnlyTitle` | `Parent / Guardian only` | `ولي أمر / وصي قانوني فقط` |
| `auth.register.parentOnlyBody` | `Children can't self-register. You'll create their accounts in the next step.` | `الأطفال لا يستطيعون التسجيل بأنفسهم. ستنشئ حساباتهم في الخطوة التالية.` |
| `auth.register.feature.title` | `Set up once. Watch them learn forever.` | `جهّز الحساب مرة. شاهدهم يتعلمون للأبد.` |
| `auth.register.submitButton` | `Continue → Add Children` | `متابعة ← إضافة الأطفال` |
| `auth.register.passwordHelper` | `At least 6 characters` | `٦ أحرف على الأقل` (Eastern-Arabic) |

> `auth.register.passwordHelper` AR is currently `'6 أحرف على الأقل'` — update `6` → `٦`.

---

## Implementation Handoff

| Fix ID | File | Tamagui token / package | Route |
|--------|------|------------------------|-------|
| B-1 | Create `packages/ui/src/components/PasswordStrengthMeter/index.tsx` | `$danger`, `$accent`, `$xp`, `$secondary`, `$border`, `$pill` | Exported from `packages/ui/src/index.ts` |
| B-2 | `apps/student-app/app/(auth)/register.tsx` progress fill | `packages/design-system/src/tokens/gradients.ts` `gradients.levelup` | `/(auth)/register` |
| B-3 | `packages/ui/src/components/Button/index.tsx` disabled opacity | `$card`, `$fg4` | `packages/ui` |
| M-1 | `packages/ui/src/components/TextField/index.tsx` height | `--lx-space-12: 48px` → `height={48}` with `$tablet` override | `packages/ui` |
| M-1 | `packages/ui/src/components/Select/index.tsx` trigger height | same | `packages/ui` |
| M-3 | `packages/ui/src/components/CheckboxField/index.tsx` checked bg | `$secondary (#22C55E)`, `$fgInverse` | `packages/ui` |
| M-5 | `apps/student-app/app/(auth)/_components/RegisterFeaturePanel.tsx` | Remove chip, bare emoji + `drop-shadow` filter | `/(auth)/register` |
| M-6 | `RegisterFeaturePanel.tsx` headline fontWeight | `fontWeight="900"`, `letterSpacing={-0.02 * 40}` | `/(auth)/register` |
| M-7 | `RegisterFeaturePanel.tsx` bullet chip borderRadius | inline `12` | `/(auth)/register` |
| M-8 | `RegisterFeaturePanel.tsx` bullet text opacity | `$fg1` + `opacity={0.92}` | `/(auth)/register` |
| M-9 | `register.tsx` step+progress layout | row direction, `flex={1}` on bar | `/(auth)/register` |
| M-10 | `register.tsx` wordmark fontWeight | `fontWeight="900"`, add `dir="ltr"` | `/(auth)/register` |
| M-11 | `register.tsx` progress bar height | 4px | `/(auth)/register` |
| M-13 | `register.tsx` ParentOnlyBanner borderColor | `rgba(168,85,247,0.3)` | `/(auth)/register` |
| M-14 | `register.tsx` ParentOnlyBanner bg | `$purpleSoft` | `/(auth)/register` |
| M-15 | `register.tsx` ParentOnlyBanner title color | `$purple` | `/(auth)/register` |
| M-16 | `apps/student-app/src/components/FormScaffold.tsx` | inner maxWidth 440 → 500 | shared scaffold |
| M-17 | `packages/shared/src/i18n/resources.ts` AR step/progressA11y | Eastern-Arabic numerals | i18n |
| M-18 | `resources.ts` AR feature.title | updated AR copy | i18n |
| M-19 | `resources.ts` AR parentOnlyTitle/Body | updated AR copy | i18n |
| M-21 | `packages/ui/src/components/TextField/index.tsx` | add `forceValueLtr` prop | `packages/ui` |
| M-22 | `packages/ui/src/components/Select/index.tsx` | dynamic `textAlign` | `packages/ui` |
| m-3 | `apps/student-app/app/(auth)/_components/RegisterForm.tsx` checkbox wrapper | `$card`, `$border`, radius 14, success tint | form |
| m-13 | `RegisterForm.tsx` haveAccount marginTop | `$5` (20px) | form |
| m-16 | `register.tsx` logo wordmark | hardcode `"Learnexia"` + `dir="ltr"` | `/(auth)/register` |
| m-17 | `resources.ts` `ar.auth.register.passwordHelper` | `'٦ أحرف على الأقل'` | i18n |
