# P9-10 (student-app) — Kid-facing energy UI (⚡ طاقة المساعد)

> Story: [../../../../user-stories/Phase-9-Payments-Billing/P9-10-kid-energy-ui.md](../../../../user-stories/Phase-9-Payments-Billing/P9-10-kid-energy-ui.md)
> Phase 9 · SP: 5 · FR-CREDIT-5 · Est. hours · App: **apps/student-app** (Expo universal, child area)
> **Designer stage REQUIRED before implementation.** READ-ONLY child surface — no purchase/payment UI.

> **Status: 🔲 Not started**

## Tasks

| ID | Task | Target | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| P9-10-FE-1 | **Design spec (REQUIRED):** `design-system/ui_kits/student-mobile/P9-10-energy-meter.md` — cover: `EnergyMeter` component (icon ⚡ not ♥, distinct colour token NOT hearts red, distinct placement from P4-04 hearts slot); `EnergyCostPill` (⚡1/⚡3/⚡5 on action buttons, before confirm); spend animation (energy decrement on successful AI delivery); `LowEnergyState` and `OutOfEnergyState` ("refills on {date}" + "ask a parent" CTA — informational only, no payment action); EN+AR copy, RTL mirrors, tokens only. Reference: `design-system/screenshots/mobile/18-hearts.png` (the thing to be visually distinct from), `08-home.png`, `11-lesson.png`, `12-quiz.png`. **The meter MUST NOT visually resemble hearts.** | `design-system/ui_kits/student-mobile/P9-10-energy-meter.md` | P4-04 hearts design (reference only); P9-01 energy balance endpoint exists (OQ-1) | 5 | 🔲 |
| P9-10-FE-2 | (Conditional) **mini BE read** if the child-facing energy-balance endpoint is missing from P9-01/P9-03: implement `GET /api/Credits/Balance/Me` returning `{ successed: true, data: { spendable: int, refillDate: date } }` in the Billing/Credit module. If the endpoint already exists, this task is a no-op — confirm before starting. Then add typed api-client hook `useEnergyBalance()` (child JWT) + NSwag regen or hand-rolled type | `packages/api-client/src/credits/useEnergyBalance.ts`; BE endpoint if missing (coordinate with P9-01/P9-03 implementer) | P9-01/P9-03 endpoint status (OQ-1) | 3 | 🔲 |
| P9-10-FE-3 | Cost-source hook `useActionCosts()` — returns per-action energy costs (hint ⚡1, explain-mistake ⚡3, deep-explanation ⚡5, similar-example ⚡5). Confirm whether costs are served via the P9-03 cost-table endpoint or a static config read; use whichever exists. **Never hard-code cost values** | `packages/api-client/src/credits/useActionCosts.ts` | P9-03 or P9-11 cost endpoint | 1 | 🔲 |
| P9-10-FE-4 | New primitives in `@learnexia/ui` packages: `EnergyMeter` (prop: `balance: number`, uses ⚡ icon, distinct colour token); `EnergyCostPill` (prop: `cost: number` — renders ⚡N beside an action button); `OutOfEnergyState` (props: `refillDate: Date`, `onAskParent?: () => void` — informational only; no payment call). All tokens only, i18n EN+AR via `useLocale`, RTL via `useLocale().direction`. **Do NOT reuse `Hearts` primitive visual** | `packages/ui/src/energy/EnergyMeter.tsx`, `EnergyCostPill.tsx`, `OutOfEnergyState.tsx` | P9-10-FE-1 (design spec); P9-10-FE-2 (balance hook) | 6 | 🔲 |
| P9-10-FE-5 | Wire primitives into the **lesson/quiz header**: place `EnergyMeter` alongside the hearts slot but visually distinct (different icon, colour, placement — per design spec). Show `EnergyCostPill` on each AI Helper action button (hint, explain-mistake, deep-explanation, similar-example). On action confirm → play spend animation only if P9-03 response indicates a charge occurred (trust BE, not FE prediction). Show `LowEnergyState` / `OutOfEnergyState` on zero/low balance. **Never show "Credits" to the child.** | `app/(student)/lesson/[id].tsx`; `app/(student)/quiz/[id].tsx`; AI Helper action components (P3-12) | P9-10-FE-4; P3-12 AI Helper UI exists | 5 | 🔲 |
| P9-10-FE-6 | i18n strings: `en.energy.*` and `ar.energy.*` — "طاقة" / "energy", low-energy warning, out-of-energy copy ("refills on {date}", "ask a parent"), cost labels (⚡1 hint / ⚡3 explain / ⚡5 deep / ⚡5 practice). **The word "Credits" must NEVER appear** in either language in child-area strings. RTL-safe | `packages/shared/src/i18n/en/energy.ts`, `ar/energy.ts` (new namespace) | P9-10-FE-4 | 1 | 🔲 |
| P9-10-FE-7 | E2E tests (Playwright, web PWA, child JWT): energy meter renders with current balance and is **visually distinct from hearts** (assert different CSS class/component/icon — not the hearts element); each Helper action shows its cost (⚡1/⚡3/⚡5) before confirm; successful Helper action decrements energy + spend animation; refusal/error does NOT decrement; out-of-energy shows "refills monthly" + "ask a parent" with **no purchase/checkout/plan control anywhere on child routes** (assert absence); RTL (ar) + LTR (en); **the word "Credits" never appears** in child UI (assert both languages) | `apps/student-app/e2e/billing/energy-meter.spec.ts` | P9-10-FE-5; P9-03 backend; P3-12 AI Helper UI | 5 | 🔲 |

## Acceptance-criteria coverage

- Energy meter shows balance, visually distinct from hearts → **FE-4** + **FE-5** + **FE-7** (assert distinct)
- Each Helper action shows cost before confirm → **FE-4** (`EnergyCostPill`) + **FE-5**
- Spend animation on successful charge only → **FE-5** + **FE-7**
- Low/out-of-energy "ask a parent" state, no purchase UI → **FE-4** (`OutOfEnergyState`) + **FE-7** (assert no purchase control)
- RTL/Arabic + EN; never "Credits" → **FE-6** + **FE-7** (assert word absence)
- Single kid-facing energy total (granted + purchased as one number) → **FE-2** + **FE-4**

## Contract from Backend (P9-01/P9-03/P9-10-FE-2)

- `GET /api/Credits/Balance/Me` (child JWT) → `{ successed: true, data: { spendable: 120, refillDate: "2026-07-01" } }` — confirm this endpoint exists from P9-01/P9-03; if not, create it in FE-2
- Per-action costs: `{ hint: 1, explainMistake: 3, deepExplanation: 5, similarExample: 5 }` — from P9-03 or P9-11

## Notes

- **OQ-1 (child energy-balance read endpoint):** highest-risk dependency. Confirm with P9-01/P9-03 implementer whether `GET /api/Credits/Balance/Me` exists and returns the right shape before FE-2. If it is missing, FE-2 creates it (BE mini-read — coordinate with the backend team).
- **OQ-2 (single number vs pool split):** show granted + purchased as one `spendable` total. The refill copy ("part of your energy refills monthly") applies only to the granted portion — confirm copy simplification with lead.
- **OQ-3 ("Ask a parent" behaviour):** informational-only for v1 (no notification relay). The CTA renders `OutOfEnergyState`; `onAskParent` is a no-op or shows a friendly dialog in v1. Confirm with lead.
- **OQ-4 (refill date source):** the child's `GrantedBalance` cycle/expiry from P9-02 — confirm it is included in the balance endpoint response.
- **OQ-6 (meter placement):** lesson/quiz header alongside but distinct from hearts. Confirm exact placement with the designer output (FE-1).
- **CLAUDE.md rule 8:** no animation library or state machine pattern introduced without asking. A plain component + CSS/Reanimated transition is the right shape for the spend animation.
- **Never render "Credits," purchase CTA, plan info, or any payment surface on a child route.**
- Blocked by **P9-03** (spend-energy + charge-on-delivery) and **P9-01** (credit account). Parallel to the parent billing stories (P9-05/06/07/08/09) — no dependency on them for the child-UI read.
