# P2-12 (web) — Parent account settings tabs (Notifications / Linked children / Security / Plan & billing)

> Story: [../../../../user-stories/Phase-2-Learning-Core/P2-12-account-settings-tabs.md](../../../../user-stories/Phase-2-Learning-Core/P2-12-account-settings-tabs.md)
> Phase 2 · SP: 21 (epic) · App: **apps/student-app** (Expo web) · Extends **P1-11** Settings shell
> **Design (pixel-perfect):** `design-system/screenshots/web/07-settings.png` (per the designer rule). Reuses the P1-11 tab bar + tokens.

## Tasks
| ID | Task | Target (file / route) | Design ref | Deps | Est (h) |
|---|---|---|---|---|---|
| P2-12-FE-1 | **Notifications** tab: per-type toggles (weekly report, streak/at-risk, product) × channel (email/push); load + save with success/error | `(parent)/settings.tsx` Notifications panel | `web/07-settings.png` | P1-11-FE-10, P2-12-BE-1 | 5 |
| P2-12-FE-2 | **Linked children** tab: list children + status; link (by email) + unlink (confirm) actions; family-scoped | `(parent)/settings.tsx` Linked panel, reuse `ChildCard`/`LinkChildForm` | `web/07-settings.png` | P1-11-FE-10, P2-12-BE-2 | 5 |
| P2-12-FE-3 | **Security** tab: change password (current/new/confirm + strength); active-sessions / sign-out-others if supported | `(parent)/settings.tsx` Security panel | `web/07-settings.png` | P1-11-FE-10, P2-12-BE-3 | 5 |
| P2-12-FE-4 | **Plan & billing** tab: current plan + status + manage/upgrade CTA (read-only until payments story) | `(parent)/settings.tsx` Plan panel | `web/07-settings.png` | P1-11-FE-10, P2-12-BE-4 | 4 |
| P2-12-FE-5 | Pixel-perfect + RTL/en + dark/light QA across the four tabs | QA | `web/07-settings.png` | FE-1…FE-4 | 2 |

## Acceptance-criteria coverage
- Notifications prefs (read/save) → **FE-1** (+ BE-1)
- Linked-children list + link/unlink (family-scoped) → **FE-2** (+ BE-2)
- Change password + session invalidation → **FE-3** (+ BE-3)
- Plan & billing (read) → **FE-4** (+ BE-4)
- Pixel-perfect en/ar, dark/light → **FE-5**

## Contract from Backend (P2-12-BE)
- Notification prefs read/update DTOs; linked-children list + link/unlink (family-scope authz); change-password (validates current, invalidates other sessions); current-plan read. All via `api-client` typed client + `BaseResponse` envelope.

## Notes
- **Blocked by P1-11-FE-10** (Settings shell + tab bar + Profile/Language). Reuses P1-04 link UI + existing Identity flows.
- Payment-provider checkout is out of scope — Plan tab is read-only/stub until a dedicated payments story.
