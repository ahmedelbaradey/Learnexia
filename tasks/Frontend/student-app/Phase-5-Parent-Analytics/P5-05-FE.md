# P5-05 (web) — Parent dashboard charts + wire real analytics data

> Story: [../../../../user-stories/Phase-5-Parent-Analytics/P5-05-parent-dashboard.md](../../../../user-stories/Phase-5-Parent-Analytics/P5-05-parent-dashboard.md)
> Phase 5 · App: **apps/student-app** (Expo web) · Design: `design-system/screenshots/web/05-dashboard.png`, `06-reports.png`
> **Carved from P1-11:** the Dashboard (`(parent)/overview.tsx`) and Reports layouts were built **layout-first** in Phase 1 with stubbed stats + a chart **placeholder**. This task adds the chart primitive(s) and replaces the stubs with real Phase-5 analytics.

## Why deferred to Phase 5
The charts visualize per-child analytics (daily XP, 20-day trend, time-of-day, mastery) that only exist once Phase-5 analytics (P5-01/P5-02/P5-03) land. The P1-11 screens already render everything **except** the charts, against `parentDashboardStubs.ts` (`TODO(P5)`).

## Tasks
| ID | Task | Target (file / route) | Design ref | Deps | Est (h) |
|---|---|---|---|---|---|
| P5-05-FE-1 | **Chart primitive** `BarChart` in `packages/ui` (labelled bars, gradient active bar, value labels, axis); RTL-aware; reuse design-system gradient tokens | `packages/ui/src/components/BarChart` | `web/05-dashboard.png` | P1-08 | 6 |
| P5-05-FE-2 | **Daily-activity bar chart** — replace the `DailyActivityCard` placeholder (`// TODO(P5-05-FE)`) with `BarChart` (XP per day, Mon–Sun) + working **Export CSV** | `(parent)/_components/DailyActivityCard.tsx` | `web/05-dashboard.png` | FE-1, P5-03-BE | 3 |
| P5-05-FE-3 | **Reports charts** — 20-day trend chart + time-of-day breakdown (extend `BarChart` or add a variant) | `(parent)/reports.tsx` | `web/06-reports.png` | FE-1 | 5 |
| P5-05-FE-4 | **Wire real analytics** — replace `parentDashboardStubs.ts` (KPIs + deltas, subject mastery, focus areas, daily/20-day/time-of-day) with the Phase-5 analytics endpoints via the typed `api-client` | `(parent)/overview.tsx`, `reports.tsx`, stubs file | — | P5-01-BE, P5-02-BE, P5-05-BE | 6 |
| P5-05-FE-5 | **"Send Report"** + **period select** become functional (currently no-op stubs) | overview/reports headers | — | P5-04-BE | 3 |
| P5-05-FE-6 | Pixel-perfect + RTL/en + dark/light QA for the now-charted dashboard & reports | QA | `web/05,06.png` | FE-1…FE-5 | 2 |

## Acceptance-criteria coverage
- Chart primitive → **FE-1**; daily-activity chart + Export → **FE-2**; reports charts → **FE-3**
- Real KPIs/mastery/focus/trends (replace stubs) → **FE-4**
- Send Report + period select functional → **FE-5**
- Pixel-perfect QA → **FE-6**

## Contract from Backend (Phase 5)
- Per-child weekly/monthly aggregates: KPIs (+vs-last-week deltas), subject mastery %, weak areas, daily-XP series, 20-day trend, time-of-day buckets; report send. All `BaseResponse<T>` via the typed `api-client`.

## Notes
- **Built layout-first in P1-11** — `(parent)/overview.tsx` + cards exist; this task only adds charts + real data. Reuses `KPIStatCard`/`MasteryBar`/`GradientBox` already in `packages/ui`.
- Subjects = **Math/Science/Arabic/English** (capture's Reading/Art is mock).
- **Ask before any new design pattern** (CLAUDE.md rule 8).
