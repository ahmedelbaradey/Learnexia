# P5-06 (frontend) — Parent grade-transition control

> Story: [../../../../user-stories/Phase-5-Parent-Analytics/P5-06-grade-transition.md](../../../../user-stories/Phase-5-Parent-Analytics/P5-06-grade-transition.md)
> Phase 5 — Parent + Analytics · Module: **parent surface (student-app parent area / Next.js parent dashboard)** · owned by the FE lead. Depends on the backend `P5-06-BE` endpoint.

## Tasks
| ID | Task | Artifact | Deps | Est (h) |
|---|---|---|---|---|
| P5-06-FE-1 | **Grade-transition control on the parent dashboard** (P5-05): a per-child "Change grade" action with a target-grade picker (1–6) and a **confirmation dialog** that states history is preserved (XP/badges/streaks/mastery carry over; skill tree re-scopes). Calls the parent grade-transition endpoint (`P5-06-BE`). Optimistic/refetch the child's dashboard after success. RTL (ar) + en. | parent dashboard child card + confirm modal + api-client | P5-06-BE-1 | — |
| P5-06-FE-2 | **States + errors.** Disable/no-op when target == current grade; surface 403 (not-your-child) and 422 (invalid grade) as friendly localized messages; loading + success toasts. | api-client error handling + UI states | FE-1 | — |
| P5-06-FE-3 | **frontend-e2e-tester:** parent changes a linked child's grade → confirm dialog → dashboard reflects the new grade; non-owned child blocked; RTL (ar) + en. | frontend-e2e-tester | FE-1, FE-2 | — |

## Notes
- The destructive-sounding action is reversible (parent can transition again) but a **confirmation step is required** (clear "history preserved" copy) so it isn't triggered accidentally.
- Mirrors existing parent-dashboard component + api-client shapes (rule #8 — ask before new patterns). Backend (`P5-06-BE`) ships the endpoint + re-scope.
