# P10-09 (student-app) — Failed payments & refunds (dunning UI) — Parent area

> Story: [../../../../user-stories/Phase-10-Payments-Billing/P10-09-failed-payments-refunds.md](../../../../user-stories/Phase-10-Payments-Billing/P10-09-failed-payments-refunds.md)
> Phase 10 · SP: 5 · FR-PAY-5 · Est. hours · App: **apps/student-app** (Expo universal, parent area)
> **Light FE — mostly BE + notifications. Parent area only. No child surface.**

> **Status: 🔲 Not started**

## Tasks

| ID | Task | Target | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| P10-09-FE-1 | Surface `PastDue`/`Dunning`/`Downgrading` subscription status in the Plan & billing tab (P10-05-FE-4): show a **dunning banner** ("your payment failed — update payment method / retry") when `currentPlan.status === 'PastDue'` or `'Dunning'`. Reuse `useCurrentPlan` hook (P10-05-FE-2) — no new endpoint needed; status is already in the plan response. EN+AR, RTL. `DunningBanner` component | `components/billing/DunningBanner.tsx`; extend `app/(parent)/settings/plan.tsx` | P10-05-FE-4 (plan tab); P10-09-BE-2 (status transitions exist) | 2 | 🔲 |
| P10-09-FE-2 | i18n strings for dunning/refund states: `en.billing.dunning.*` and `ar.billing.dunning.*` — failed payment notice, retry CTA, "plan will downgrade on {date}", refund confirmation copy. RTL-safe | `packages/shared/src/i18n/en/billing.ts`, `ar/billing.ts` (extend) | P10-09-FE-1 | 1 | 🔲 |
| P10-09-FE-3 | E2E tests (light, Playwright, web PWA): parent sees `PastDue`/`Downgrading` status banner in the plan tab after a simulated failed-payment webhook; refund appears as a negative entry in billing history (cross-checks P10-08-FE-6); no child surface shows payment-failure state | `apps/student-app/e2e/billing/dunning.spec.ts` | P10-09-FE-1; P10-08-FE-6; backend with dunning webhooks | 2 | 🔲 |

## Acceptance-criteria coverage (FE portion)

- Parent sees `PastDue`/`Downgrading` status + dunning banner → **FE-1** (`DunningBanner`)
- Refund as negative entry in billing history → **FE-3** (cross-checks P10-08 E2E)
- No child surface for dunning/refund → **FE-3** (assert absence in child routes)
- Heavy verification (idempotency, clawback, webhook security) → **api-tester** + **security-auditor** (backend)

## Notes

- The dunning **notification** (email/push) is handled by the Notifications module via BE integration event — no FE work required for the notification itself.
- The parent can see the dunning status and a retry CTA via the existing plan-tab surface; FE-1 adds the banner component.
- If the lead wants a "retry payment" button (re-initiating checkout), that reuses `useStartSubscriptionCheckout` from P10-06-FE-2 — wire it from the `DunningBanner` CTA.
- **No child surface** — children only see the energy meter (P10-10); a clawback simply reduces their displayed energy and may surface the out-of-energy "ask a parent" state naturally.
