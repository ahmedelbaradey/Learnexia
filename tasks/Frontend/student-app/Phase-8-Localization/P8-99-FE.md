# P8-99 (FE) — App-shell language foundation (fonts · UI-language persistence · RTL/i18n pass)

> Stories: axis-A completion of [../../../../user-stories/Phase-8-Localization/P8-01-set-child-learning-language.md](../../../../user-stories/Phase-8-Localization/P8-01-set-child-learning-language.md) (the `PreferredLanguage` default/edit half) + FE-relevant acceptance of [../../../../user-stories/Phase-6-Stabilization/P6-03-localization-rtl-pass.md](../../../../user-stories/Phase-6-Stabilization/P6-03-localization-rtl-pass.md).
> Phase 8 — Localization · Epic: Localization · App: **apps/student-app** + **packages** · Foundation/QA wave.
> Architecture: [../../../../docs/dev/FRONTEND_ARCHITECTURE.md](../../../../docs/dev/FRONTEND_ARCHITECTURE.md) §6 · [../../../../docs/architecture/localization-architecture.md](../../../../docs/architecture/localization-architecture.md) §1 (axis A).
> Brief: [../../../../docs/briefs/P8-localization-FE.md](../../../../docs/briefs/P8-localization-FE.md).
> **Why a new ID:** the work below is real, cross-cutting axis-A/RTL FE work with no dedicated story task file. It is split out so the regen blocker (FE-0) can be sequenced first and the font/RTL work can run in parallel with P8-01/P8-04. Folds in the FE-relevant slice of P6-03.

## Tasks
| ID | Task | Target (file / route) | Deps | Est (h) |
|---|---|---|---|---|
| P8-99-FE-0 | **PREREQ / BLOCKER:** regenerate the api-client (`refresh:swagger` → `gen:api`) against a backend running merged P8 code so `AddChildCommand.learningLanguage`, `MeResponse.learningLanguage`, and `Change-Learning-Language` are exposed. Commit the generated client (do not hand-edit `src/generated/**`). | `packages/api-client` (generated client + `swagger.json`) | merged P8 BE | 2 |
| P8-99-FE-1 | **Brand-font runtime loading:** add `expo-font` dep; register Cairo/Tajawal/Poppins via `useFonts`/`loadAsync` at app start + the font plugin in `app.config.ts`; ensure Tamagui `$heading`/`$body` faces resolve on **web and native**. | `apps/student-app` (app entry, `app.config.ts`, font assets/loaders) | — *(lead: source from `feat/design-system-pixel-align` or build fresh — open Q)* | 5 |
| P8-99-FE-2 | Add a `useUpdateUserLanguage` hook wrapping the **existing** `updateUserLanguage` (`EditUserPreferredLanguageCommand`) — no regen needed. | `packages/api-client/src/hooks/useUpdateUserLanguage.ts` (+ index) | — | 2 |
| P8-99-FE-3 | **Promote + persist the UI-language switch:** move/duplicate the `LocaleThemeControls` language control into the authenticated account/settings surface; on change, persist to the backend via FE-2 **and** sync the locale store/i18n; choice survives sign-out/sign-in. | `apps/student-app/app/(parent)/settings.tsx` (+ reuse `LocaleThemeControls`) | P8-99-FE-2, Design Spec | 4 |
| P8-99-FE-4 | **RTL / native-restart UX:** web flips direction instantly; native shows the existing restart-prompt before `react-native-restart`; no half-flipped state on toggle. | `apps/student-app` locale-switch path + `RestartPrompt.tsx` | P8-99-FE-3 | 3 |
| P8-99-FE-5 | **i18n key-completeness sweep:** find/remove hardcoded user-facing strings on the built Phase-1/Phase-2 screens; move them to `packages/shared` resources (ar + en). | `apps/student-app` screens + `packages/shared` i18n | — | 4 |
| P8-99-FE-6 | **RTL review pass (P6-03 FE-relevant):** review all built screens in Arabic (RTL) + English — logical/direction props, no clipping/overlap/wrong-side alignment, mixed-direction (numbers/dates/Latin-in-Arabic) correct; log issues with severity, fix or schedule. | QA across built screens | P8-99-FE-1, FE-5 | 5 |

## Acceptance-criteria coverage
- Fonts (Cairo/Tajawal/Poppins) actually render on web + native → **FE-1**
- UI `PreferredLanguage` editable independently of learning language **and persisted** → **FE-2, FE-3**
- RTL flip + native-restart UX correct → **FE-4**
- No hardcoded strings on built screens → **FE-5**
- Every screen reads correctly in Arabic RTL + English (P6-03) → **FE-6**
- api-client exposes the P8 contracts (unblocks P8-01-FE / P8-04-FE) → **FE-0**

## Contract from Backend
- **FE-0:** regen surfaces add-child `learningLanguage`, `/Me` `learningLanguage`, `Change-Learning-Language`.
- **FE-2:** `updateUserLanguage(EditUserPreferredLanguageCommand { userPreferredLanguage })` **already present** in the generated client — no regen needed for axis-A persistence.

## Notes
- **FE-0 is the wave-wide blocker** — P8-01-FE and P8-04-FE cannot land until it does. FE-1…FE-6 are **not** blocked by FE-0 and can run in parallel.
- **Open Q (lead):** source the font registration from the unmerged `feat/design-system-pixel-align` branch (cherry-pick) or build fresh? Do not check that branch out.
- **Open Q (lead):** confirm `updateUserLanguage` is the intended endpoint for an authenticated user updating their own `PreferredLanguage` (vs a profile-update variant).
- Mirror existing hook/component shapes (`LocaleThemeControls`, existing hooks); ask the lead before any new design pattern (CLAUDE.md rule 8). No API calls in components; no server data in Zustand.
