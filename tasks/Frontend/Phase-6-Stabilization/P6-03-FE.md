# P6-03 (frontend / QA) — Localization & RTL review pass

> Story: [../../../user-stories/Phase-6-Stabilization/P6-03-localization-rtl-pass.md](../../../user-stories/Phase-6-Stabilization/P6-03-localization-rtl-pass.md)
> Phase 6 — Stabilization · Epic: QA & Stabilization · SP: 3 · NFR-5 · **Frontend / QA only — owned by the FE lead. No backend.**
> Arabic-first is a core differentiator (BRD §1); this is a systematic launch-gate review across every screen in **Arabic (RTL)** and **English**.

## Tasks
| ID | Task | Artifact | Deps | Est (h) |
|---|---|---|---|---|
| P6-03-FE-1 | **Screen-by-screen RTL/LTR review** across the student app (onboarding/auth, learn/quiz, gamification, AI tutor, dashboard, notifications inbox, energy/billing) and the admin console. Verify mirroring (layout, icons, chevrons, progress direction), fonts (Cairo/Tajawal/Poppins), and that text is not clipped/overlapping/wrongly-aligned. | review checklist + per-surface findings doc | — | — |
| P6-03-FE-2 | **Mixed-direction + formatting checks:** numbers, dates, currency (energy/billing), and mixed ar+en strings render correctly (bidi); RTL-correct number/percentage placement (mastery %, XP, leaderboard). | findings doc | FE-1 | — |
| P6-03-FE-3 | **frontend-e2e-tester:** automate the highest-value RTL assertions (key screens render `dir="rtl"` under `ar`, no overflow on the critical journey) so the pass is repeatable, not one-off. | Playwright RTL specs (extend the admin/student e2e suites) | FE-1 | — |
| P6-03-FE-4 | **Triage + fix/schedule:** log every issue with severity; fix the blockers, schedule the rest. Record exit state (screens reviewed, issues open/closed). | findings + fixes | FE-1..FE-3 | — |

## Acceptance-criteria coverage
- All screens reviewed in ar (RTL) + en; layout/mirroring/fonts correct → **FE-1**
- No clipping/overlap/wrong-alignment → **FE-1**
- Numbers/dates/mixed-direction render correctly → **FE-2**
- Issues logged + fixed/scheduled by severity → **FE-4** (+ repeatable assertions in **FE-3**)

## Notes
- **No backend.** Backend localization (resx EN+AR, locale resolution, the P9-10 notification localization, P6-04 report-string localization) is already in place; this is the FE/visual + bidi review on top.
- FE-lead owned (the backend lead does not build FE). Tracked here so the launch gate (P6-04 exit criteria) can reference a real task.
