# P7-08 (admin-dashboard) — Manage child profiles & grade overrides

> Story: [../../../../user-stories/Phase-7-Admin-Console/P7-08-manage-child-profiles-and-grade-overrides.md](../../../../user-stories/Phase-7-Admin-Console/P7-08-manage-child-profiles-and-grade-overrides.md)
> Phase 7 · SP: 5 · SRS §3, FR-ID-2/3, FR-LR-1 · Est. hours · App: **apps/admin-dashboard** (Next.js 15)
> Reuses `packages/api-client`, `packages/shared`, `packages/ui` (Tamagui via `@tamagui/next-plugin`). Builds on the P1-10 admin shell (`AdminShell`, `useAdminGuard`, authStore).

## Tasks
| ID | Task | Target | Deps | Est (h) |
|---|---|---|---|---|
| P7-08-FE-1 | **Edit child profile** form: language + country selects with validation, only on child accounts | `app/(admin)/users/[id]/edit/page.tsx` | P7-06-FE-2, P7-08-BE-1 | 4 |
| P7-08-FE-2 | **Grade override** dialog: grade select (1–6), required reason + confirm; warning that curriculum re-scopes but history is preserved | `GradeOverrideDialog.tsx` | P7-08-FE-1, P7-08-BE-2 | 4 |
| P7-08-FE-3 | api-client mutation hooks (`useUpdateChildProfile`, `useOverrideChildGrade`) with TanStack Query invalidation of profile/activity | `packages/api-client` | P7-08-FE-1/2, P7-08-BE-3 | 2 |
| P7-08-FE-4 | Inline validation + envelope error surfacing (invalid grade, unsupported language/country); RTL/Arabic + English copy | `app/(admin)/users/[id]/edit/page.tsx`, `GradeOverrideDialog.tsx` | P7-08-FE-1/2, P7-08-BE-4 | 2 |

## Acceptance-criteria coverage
- Edit language/country → **FE-1** (+ P7-08-BE-1)
- Grade override + re-scope notice → **FE-2** (+ P7-08-BE-2/3)
- History-preserved messaging → **FE-2**
- Reject invalid grade / unsupported language/country → **FE-4**
- Admin-only access → `useAdminGuard` (+ P7-08-BE-4)

## Contract from Backend (P7-08-BE)
- `PATCH /api/Admin/Users/{childId}/profile` `{ language?, country? }` → `BaseResponse<AdminUserProfileDto>`.
- `POST /api/Admin/Users/{childId}/grade` `{ grade, reason, confirm }` → `BaseResponse<AdminUserProfileDto>`.

## Notes
- Grade override mirrors the parent-driven transition (P5-06): the UI states clearly that curriculum re-scopes while XP/badges/streaks/mastery are preserved. Validation errors return via the `BaseResponse` envelope and show inline. Reuses shared packages; RTL/Arabic + English per project i18n.
