# P7-02 (admin-dashboard) — Manage lessons & lesson content

> Story: [../../../../user-stories/Phase-7-Admin-Console/P7-02-manage-lessons-and-content.md](../../../../user-stories/Phase-7-Admin-Console/P7-02-manage-lessons-and-content.md)
> Phase 7 · SP: 8 · SRS §3, FR-LR-1, FR-LR-2 · Est. hours · App: **apps/admin-dashboard** (Next.js 15)
> Reuses `packages/api-client`, `packages/shared`, `packages/ui` (Tamagui via `@tamagui/next-plugin`). Builds on the P1-10 admin shell (`AdminShell`, `useAdminGuard`, authStore).

## Tasks
| ID | Task | Target | Deps | Est (h) |
|---|---|---|---|---|
| P7-02-FE-1 | Lessons list under a unit + lesson create/edit form (title, difficulty, est. time, lock) | `app/(admin)/curriculum/lessons/page.tsx`, `LessonForm.tsx` | P7-01-FE-*, P7-02-BE-1 | 5 |
| P7-02-FE-2 | **Content-block editor**: add blocks by type (text/image/video/callout) with per-type input | `components/ContentBlockEditor.tsx` | P7-02-FE-1, P7-02-BE-3 | 6 |
| P7-02-FE-3 | Drag-to-reorder content blocks within a lesson + reorder lessons within a unit | `ReorderableList` reuse | P7-02-FE-2, P7-02-BE-4 | 5 |
| P7-02-FE-4 | Remove block / delete lesson with confirm + cache invalidation | delete dialogs, hooks | P7-02-FE-1, P7-02-BE-5 | 3 |
| P7-02-FE-5 | Block-type renderers preview inline (so author sees student-ish rendering) | `components/BlockPreview.tsx` | P7-02-FE-2 | 3 |

## Acceptance-criteria coverage
- Lesson CRUD with metadata under unit → **FE-1**
- Add content blocks with type/payload → **FE-2**
- Reorder/remove blocks persists → **FE-3, FE-4**
- Reorder lessons within unit → **FE-3**
- Delete lesson (blocks handled) → **FE-4**
- Admin-only access → **FE-1** (guard) + middleware

## Contract from Backend (P7-02-BE)
- `GET /api/Learning/Units/{unitId}/Lessons` and `.../Lessons/{id}/ContentBlocks` → `BaseResponse<List<...>>`.
- `POST/PUT/DELETE` Lessons & ContentBlocks → `BaseResponse<Guid>`/`<bool>`; `PUT .../Reorder` → `BaseResponse<bool>`.

## Notes
- Content-block `payload` is type-shaped JSON; renderers live in `packages/ui` where reusable by the student app later. TanStack Query v5 + Zustand v5; RTL/Arabic + English.
