# P7-01 (admin-dashboard) — Manage subjects & units

> Story: [../../../../user-stories/Phase-7-Admin-Console/P7-01-manage-subjects-and-units.md](../../../../user-stories/Phase-7-Admin-Console/P7-01-manage-subjects-and-units.md)
> Phase 7 · SP: 5 · SRS §3, FR-ADM-1 · Est. hours · App: **apps/admin-dashboard** (Next.js 15)
> Reuses `packages/api-client`, `packages/shared`, `packages/ui` (Tamagui via `@tamagui/next-plugin`). Builds on the P1-10 admin shell (`AdminShell`, `useAdminGuard`, authStore).

## Tasks
| ID | Task | Target | Deps | Est (h) |
|---|---|---|---|---|
| P7-01-FE-1 | Curriculum → **Subjects** list page: table of subjects (grade, order, active) via TanStack Query | `app/(admin)/curriculum/subjects/page.tsx` | P1-10-FE-3, P7-01-BE-1 | 4 |
| P7-01-FE-2 | Subject + Unit create/edit forms (modal/drawer) with validation, wired to `@learnexia/api-client` | `components/SubjectForm.tsx`, `UnitForm.tsx` | P7-01-FE-1, P7-01-BE-2 | 5 |
| P7-01-FE-3 | Unit list under a subject + drag-to-reorder for subjects and units, persisting via Reorder endpoint | `components/ReorderableList.tsx` | P7-01-FE-2, P7-01-BE-3 | 5 |
| P7-01-FE-4 | Activate/deactivate toggle with optimistic update + cache invalidation | `useToggleActive` hook | P7-01-FE-1, P7-01-BE-4 | 3 |
| P7-01-FE-5 | Delete with "unit not empty" error surfaced; admin route guard reuse | delete dialog, `useAdminGuard` | P7-01-FE-1, P7-01-BE-5 | 2 |

## Acceptance-criteria coverage
- Subjects list with grade/order/active → **FE-1**
- CRUD units under subject → **FE-2**
- Drag-reorder persists → **FE-3**
- Activate/deactivate, inactive preserved → **FE-4**
- Reject delete of non-empty unit (error shown) → **FE-5**
- Admin-only access → **FE-5** (guard) + middleware

## Contract from Backend (P7-01-BE)
- `GET /api/Learning/Subjects` → `BaseResponse<List<SubjectDto>>`; `.../Subjects/{id}/Units` → list.
- `POST/PUT/DELETE` Subjects & Units → `BaseResponse<Guid>`/`<bool>`; `PUT .../Reorder` + `.../Active` → `BaseResponse<bool>`.

## Notes
- Builds on the P1-10 admin shell; add a Curriculum nav section. TanStack Query v5 + Zustand v5; RTL/Arabic + English. Reuse Tamagui `packages/ui` components.
