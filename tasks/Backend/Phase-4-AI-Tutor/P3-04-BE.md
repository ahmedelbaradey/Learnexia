# P3-04 (Backend) — Explain a concept on demand (AI Helper intent #1)

> Story: [../../../user-stories/Phase-4-AI-Tutor/P3-04-explain-concept.md](../../../user-stories/Phase-4-AI-Tutor/P3-04-explain-concept.md)
> Phase 4 — AI Tutor · Epic: Prompt Builder & Tutor · SP: 3 · FR-AI-1, FR-AI-4, FR-AI-5, FR-AI-6 · Est. unit: hours
> **Backend only — no separate FE task file.** The UI surface is owned by P3-12 (AI Tutor UI).
>
> **AI Helper MVP — read first:** See `docs/briefs/ai-helper-mvp.md`. P3-04 implements **intent #1 — "اشرح السؤال"**. Grounding is fetched via `ILearningContextProvider` (seeded corpus at MVP). No context → refuse-and-redirect (not a generic explanation). Instrumentation events emitted. Build order is NOT gated on P3-07 or BL-*.

> **Status: 🔲 Not started** — hard-blocked by P3-01 (AI Gateway) + P3-02 (Safety Layer) + P3-03 (Prompt Builder + `ILearningContextProvider` seam). P3-07 is NOT required at MVP — `SeededCorpusContextProvider` is the grounding source. LEAD GATE required on OQ-1/OQ-2/OQ-3 before dispatch.

## Lead decisions required before dispatch

| # | Question | Recommendation |
|---|---|---|
| OQ-1 | **Streaming mechanism (CLAUDE.md rule 8 — ask-first):** SSE (`text/event-stream`) vs `IAsyncEnumerable<string>` chunked vs WebSocket? | **SSE** — simplest for Expo/web; one-way; easy retry; errors return typed SSE `error` event |
| OQ-2 | **Streaming vs mandatory safety:** buffer-fully → safety-filter → then "stream" the pre-checked text (OQ-2a, safest); chunk-level filtering (OQ-2b); stream + post-hoc redaction (OQ-2c)? | **(a) buffer → filter → emit** — safest, provably satisfies AC-2; mitigate latency with cheap model routing |
| OQ-3 | Orchestration location/seam: which module owns the explain handler, and how does it reach Curriculum retrieval + Learning lesson context? | Dedicated **AI-tutor slice** in the `Curriculum` module (or new `AiTutor` slice); consumes Learning via `Shared.Contracts` lesson-info contract; consumes Curriculum retrieval in-process |
| OQ-4 | Tutor-interaction logging: `ILoggerManager` only vs a lightweight table for P7-11 / adaptivity? | `ILoggerManager` at MVP; fire-and-forget if a table is added (never blocks the stream) |
| OQ-5 | Empty-retrieval behavior for explain: decline or proceed with reduced grounding? | **Proceed with reduced grounding** — explain may use prompt-level grade/age/language + lesson title without curriculum chunks; must NOT fabricate specific curriculum facts |

## Backend tasks

| ID | Task | Artifact | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| P3-04-BE-1 | Define `ExplainConceptCommand { LessonId?, ConceptId?, SkillId?, Question? }` — grade/age/language resolved from `ICurrentUserService` (child JWT), NOT the body. Validate with `ValidationBehavior` (at least one of LessonId/ConceptId/SkillId required; `Question` max length). | `AiTutor.Application/Features/Explain/Commands/ExplainConceptCommand.cs`, `ExplainConceptCommandValidator.cs` | OQ-1, OQ-3 resolved | 2 | 🔲 |
| P3-04-BE-2 | Define `ILessonContextContract` in `Shared.Contracts/Learning/` — cross-module seam so the explain handler can read lesson title + subject + grade without a cross-module FK. Signature: `Task<LessonContextDto?> GetLessonContextAsync(int lessonId, CancellationToken ct)`. Implement adapter in `Learning.Infrastructure`. | `Shared.Contracts/Learning/ILessonContextContract.cs`, `Shared.Contracts/Learning/LessonContextDto.cs`, `Learning.Infrastructure/Contracts/LessonContextContractAdapter.cs` | OQ-3 resolved, P2-05 (lesson entity exists) | 3 | 🔲 |
| P3-04-BE-3 | Implement `ExplainConceptCommandHandler` — orchestration (revised for AI Helper MVP): (1) resolve grade/age/language from `ICurrentUserService`; (2) **call `ILearningContextProvider.GetContextAsync(studentId, skillId, questionId?, wrongAnswer:null, ct)`** — this replaces the direct `IChunkRetrievalContract` call; (3) **if `LearningContext.Chunks` is empty → return the refuse-and-redirect response** (from `RedirectResponseBuilder.Build(skillName, language)`) + emit `HelpDeclined { Reason=NoContext }` fire-and-forget; do NOT call the LLM; (4) on non-empty context: call P3-03 Prompt Builder with `HelperIntent.Explain` + `LearningContext`; (5) call P3-01 Gateway stream; (6) **buffer full response** (OQ-2a); (7) pass buffer to P3-02 Safety Layer; if safety fails → return typed gentle error (AC-6) without streaming any content; (8) stream the pre-checked text to the client (per OQ-1 wire format); (9) emit `HelpDelivered { Intent=Explain, ContextSource }` fire-and-forget. No progression side effects. Inject `ICurrentUserService`, `ILearningContextProvider`, P3-03 prompt builder, P3-01 gateway client, P3-02 safety client, `ILessonContextContract`, `IIntegrationEventPublisher`, `ILoggerManager`. | `AiTutor.Application/Features/Explain/Commands/ExplainConceptCommandHandler.cs` | P3-04-BE-1, P3-04-BE-2, P3-01, P3-02, P3-03-BE-9 (`ILearningContextProvider` seam) | 6 | 🔲 |
| P3-04-BE-4 | Implement the streaming HTTP endpoint: `POST /api/AiTutor/Explain`, `[Authorize(Roles="Student")]`. **SSE endpoint** (per OQ-1): sets `Content-Type: text/event-stream`; add `X-Accel-Buffering: no` response header for proxy compatibility. Uses the **pinned SSE wire contract** (do NOT deviate): `event: message` + `data: {"content":"<token>"}` for each content chunk; `event: redirect` + `data: {"type":"lesson","targetId":"<skillId>"}` for the no-context refuse-and-redirect response; `event: error` + `data: {"code":"<ErrorCode>","message":"<safe message>"}` for gateway/safety failures; `event: done` + `data: [DONE]` as the stream terminator. Does NOT use `NewResult(...)` (SSE bypasses the buffered `BaseResponse<T>` envelope). **Safety rule: provider output is buffered → safety-filtered → only then streamed as `event: message` chunks. Never raw-passthrough unfiltered LLM tokens to a child.** Document this pattern and the wire contract in a code comment. Pin this contract in HANDOFF.md before the P3-12 FE batch is dispatched. | `AiTutor.Api/Controllers/ExplainController.cs` | P3-04-BE-3, OQ-1 confirmed | 4 | 🔲 |
| P3-04-BE-5 | Add rate limiting for the explain endpoint: bound requests per student per time window (cost/abuse mitigation per security-auditor requirements). Reuse the existing rate-limit infrastructure (P6-06) — configure a new policy for the tutor endpoints. | Rate-limit policy config in Host or module middleware | P3-04-BE-4, P6-06 (rate-limit infra) | 2 | 🔲 |
| P3-04-BE-6 | DI wiring: `AddAiTutorApplication` (AutoMapper + validators; no `AddMediatR`); `AddAiTutorInfrastructure` (gateway/safety/prompt/retrieval client registrations; `ILessonContextContract` adapter). Wire `InitializeAsync` if module has DB. Register rate-limit policy. | `AiTutor.Application/DependencyInjection.cs`, `AiTutor.Infrastructure/DependencyInjection.cs`, Host `Program.cs` | P3-04-BE-3, P3-04-BE-5 | 2 | 🔲 |

| P3-04-BE-7 | **`ConceptExplanationCache` table migration** — create the `ai.concept_explanation_cache` table (schema: `Id`, `ConceptId` int, `GradeId` int, `Language` varchar(5), `Difficulty` smallint, `Content` text, `ModelId` varchar(100), `GeneratedAtUtc` timestamptz, `BatchJobId` varchar(200), `QaPassedAt` timestamptz nullable, `StaleAt` timestamptz nullable; unique index on `(ConceptId, GradeId, Language, Difficulty)`). Module placement: `ai` schema per `ai-cost-routing.md OQ-A`. Manual apply per CONVENTIONS §13. No cross-module FK — `ConceptId`/`GradeId` are plain ints. | `Ai.Infrastructure/Migrations/AddConceptExplanationCache.cs` | P3-01-BE-2 (Ai module exists) | 2 | 🔲 |
| P3-04-BE-8 | **`IConceptExplanationCache` repository + entity** — define `ConceptExplanationCacheEntry` entity and `IConceptExplanationCacheRepository` with `GetAsync(conceptId, gradeId, language, difficulty, ct)` and `UpsertAsync(entry, ct)` in `Ai.Domain` / `Ai.Infrastructure`. No UoW; `UpsertAsync` commits per call (rule 3). | `Ai.Domain/Entities/ConceptExplanationCacheEntry.cs`, `Ai.Domain/Repositories/IConceptExplanationCacheRepository.cs`, `Ai.Infrastructure/Repositories/ConceptExplanationCacheRepository.cs` | P3-04-BE-7 | 2 | 🔲 |
| P3-04-BE-9 | **Cache-first lookup in `ExplainConceptCommandHandler`** — before calling the gateway: (0) context check (BE-3 step 2-3) happens first; if redirect → return immediately; (1) call `IConceptExplanationCacheRepository.GetAsync(conceptId, gradeId, language, difficulty)`; (2) on hit, stream cached content immediately (it is already QA-passed and safe); emit `HelpDelivered { ContextSource=SeededCorpus }` fire-and-forget; (3) on miss, proceed with prompt build → gateway → safety pipeline, then fire-and-forget `IConceptExplanationCacheRepository.UpsertAsync` to populate for subsequent students. Check quota via `IAiUsageBudget.CheckBudgetAsync` (P3-01-BE-14) before any LLM call; on `QuotaExhausted`, serve the cached entry if available, else return a "AI helper busy" gentle response. Route `Explain` task kind to `claude-sonnet-4-6` (NOT Opus) via `AiModelRouter`. Apply prompt-caching `cache_control` on system-prompt + curriculum-context portions (P3-01-BE-12 wiring). Also emit `HelpRequested { Intent=Explain }` at handler entry (before the context check). | `AiTutor.Application/Features/Explain/Commands/ExplainConceptCommandHandler.cs` (update) | P3-04-BE-3, P3-04-BE-8, P3-01-BE-12, P3-01-BE-14, P3-03-BE-9 | 3 | 🔲 |
| P3-04-BE-10 | **Offline batch pre-generation job for concept explanations** — implement `PreGenerateConceptExplanationsJob` (Hangfire job or `IHostedService` — confirm host with lead per `ai-cost-routing.md OQ-B`): (1) enumerate all `(ConceptId, GradeId, Language, Difficulty)` combinations not yet present in `ai.concept_explanation_cache` (or where `StaleAt IS NOT NULL`); (2) build generation prompts via P3-03 Prompt Builder; (3) submit to `IAiBatchGateway.SubmitBatchAsync` (Sonnet model `claude-sonnet-4-6`); (4) poll for completion via `IAiBatchGateway.PollBatchAsync`; (5) on completion, upsert results into `ConceptExplanationCacheRepository`. Separate **Opus QA pass**: submit completed entries to `IAiBatchGateway` with model `claude-opus-4-8` for QA review; set `QaPassedAt` on pass, log failures for human review. Trigger: on content publish and on-demand via admin endpoint. Always verify current Anthropic model IDs before launch. | `Ai.Application/Jobs/PreGenerateConceptExplanationsJob.cs`, `Ai.Infrastructure/Jobs/PreGenerateConceptExplanationsJobScheduler.cs` | P3-04-BE-8, P3-01-BE-13 | 5 | 🔲 |

| P3-04-BE-11 | **`RedirectResponseBuilder` utility** — implement a small, localizable utility that produces the refuse-and-redirect response copy for the Explain intent. Accepts `skillName` (string) and `language` (ar/en). Returns the localized redirect string per `ai-helper-mvp.md §4`. The `skillName` is resolved from `ILessonContextContract` or `ILearningContextProvider`. This utility is shared across P3-04 and P3-05. | `AiTutor.Application/Services/RedirectResponseBuilder.cs` | P3-03-BE-9 | 1 | 🔲 |
| P3-04-BE-12 | **Instrumentation integration events** — declare `HelpRequestedIntegrationEvent { StudentId, Intent, SkillId, QuestionId? }`, `HelpDeliveredIntegrationEvent { StudentId, Intent, SkillId, QuestionId?, ModelUsed, ContextSource }`, `HelpDeclinedIntegrationEvent { StudentId, Intent, SkillId, Reason }` in `Shared.Contracts/AiTutor/`. Wire fire-and-forget publish in `ExplainConceptCommandHandler` at the three emit points (BE-3/BE-9): `HelpRequested` at entry, `HelpDelivered` on successful delivery, `HelpDeclined` on no-context redirect. Handlers for P5-03 / P6-02 consume these events (future — declare now, handlers later). | `Shared.Contracts/AiTutor/HelpRequestedIntegrationEvent.cs`, `HelpDeliveredIntegrationEvent.cs`, `HelpDeclinedIntegrationEvent.cs` | P3-04-BE-3 | 2 | 🔲 |

## Acceptance-criteria coverage

- AC-1 (grounded + appropriate, active-skill scoped): P3-03 Prompt Builder with `HelperIntent.Explain` injects grade/age/language/LearningContext/child-safe tone; scoped to active skill/question → **BE-3, BE-9**
- AC-2 (context-grounded + safe): `ILearningContextProvider` grounding + Safety Layer (P3-02) — no bypass path even under buffered-stream strategy (OQ-2a); cache path is pre-QA-passed → **BE-3, BE-4, BE-9**
- AC-3 (refuse-and-redirect): empty `LearningContext.Chunks` → redirect response + `HelpDeclined` event; no LLM call; tested explicitly → **BE-3, BE-11**
- AC-4 (streaming + latency): SSE endpoint streams pre-checked text (or cached text); cheap model routing (Sonnet, not Opus) for < 4 s budget → **BE-4, BE-9**
- AC-5 (no progression decisions): handler writes no mastery/XP/unlock → **BE-3**
- AC-6 (graceful errors): SSE `event: error` with typed payload; no raw stack trace; quota exhaustion degrades to cache not error → **BE-4, BE-9**
- AC-7 (instrumentation): `HelpRequested`, `HelpDelivered`/`HelpDeclined` events emitted fire-and-forget → **BE-12**
- AC-8 (conventions): `[Authorize(Roles="Student")]`; grade/age/language from JWT; `ILoggerManager`; module isolation; streaming/envelope pattern documented → **BE-1, BE-3, BE-4, BE-6**
- **Cache-primary** — cache-hit path serves from `ConceptExplanationCache`; cache-miss triggers gateway (Sonnet) + async cache-write → **BE-8, BE-9**
- **Offline pre-generation** — batch job populates `ConceptExplanationCache` via Batch API (Sonnet) + Opus QA pass → **BE-10**
- **Quota enforcement** — `IAiUsageBudget` checked before LLM; `QuotaExhausted` degrades to cache → **BE-9**

## Notes

- **Streaming pattern is new** for this codebase (everything else is buffered `BaseResponse<T>`). CLAUDE.md rule 8 applies: confirm OQ-1 (SSE) with the lead before implementing BE-4.
- **Safety-buffering (OQ-2a):** the full response is buffered before any token is streamed to the child. This is slightly higher latency than chunk-level filtering but is the only provably safe option at MVP. Cache-hit path skips this (already safe).
- **Cross-cluster deps (REVISED):** P3-01 (gateway + BE-12 prompt-caching + BE-13 batch gateway + BE-14 quota), P3-02 (safety), P3-03 (prompt builder + `ILearningContextProvider` seam from P3-03-BE-9). **P3-07 is NOT a cross-cluster dep for P3-04** — the `SeededCorpusContextProvider` is the MVP grounding source. P3-07 adds `RagContextProvider` later via config.
- **`ILessonContextContract` (BE-2)** is a shared-file edit in `Shared.Contracts` — serialize against any concurrent story touching that package.
- **Parallelizable with P3-05:** P3-04 and P3-05 share the same pipeline and can be built together once the infra cluster lands. Reuse DI registrations. P3-04-BE-7 migration may be combined with P3-05-BE-9 migration into a single migration file.
- **Security-auditor is a hard gate** for this story (AI text shown to children). Audit covers: no-bypass under buffered-stream, prompt-injection via context/question, child-data minimization, no secret leakage, rate limits, quota enforcement correctness, **scope guard is enforced (refuse-and-redirect when no context, no out-of-scope explain reaches the LLM)**.
- **Wire format — pinned contract (pin in HANDOFF.md before P3-12 FE dispatch):** `event: message` + `data: {"content":"<token>"}` (content chunks); `event: redirect` + `data: {"type":"lesson","targetId":"<skillId>"}` (no-context redirect); `event: error` + `data: {"code":"<ErrorCode>","message":"<safe message>"}` (failures); `event: done` + `data: [DONE]` (stream terminator). Provider output is **buffered → safety-filtered → only then streamed** as `event: message`. Never raw-passthrough unfiltered tokens to a child. The committer must document this exact contract in HANDOFF.md as a prerequisite for the P3-12 FE dispatch gate (OQ-2 in P3-12).
- **Cost routing:** runtime `Explain` calls use `claude-sonnet-4-6` (NOT Opus). The pre-generation job uses `claude-sonnet-4-6` (generation) and `claude-opus-4-8` (QA). Always verify current Anthropic model IDs before launch.
- **Cross-reference:** `docs/briefs/ai-cost-routing.md` governs all model-routing, cache, quota, and Batch API decisions. `docs/briefs/ai-helper-mvp.md` governs the intent model, `ILearningContextProvider` seam, refuse-and-redirect rule, and closed-loop instrumentation.
- **Build order is parallel with Gamification-FE / Parent work** — NOT gated on BL-* or P3-07.
- **New task IDs added:** P3-04-BE-11 (`RedirectResponseBuilder`), P3-04-BE-12 (instrumentation integration events). Both are Status: 🔲.
