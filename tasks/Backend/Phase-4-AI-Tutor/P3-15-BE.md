# P3-15 (backend) — AI Batch API & offline pre-generation (cost optimization) — BACKLOG

> Story: [../../../user-stories/Phase-4-AI-Tutor/P3-15-ai-batch-and-offline-pregeneration.md](../../../user-stories/Phase-4-AI-Tutor/P3-15-ai-batch-and-offline-pregeneration.md)
> Brief: [../../../docs/briefs/ai-cost-routing.md](../../../docs/briefs/ai-cost-routing.md) (authoritative cost-routing doc — Batch API section)
> Phase 4 · Module: **Ai** (`IAiBatchGateway` in `Shared.Contracts/Ai`, `ClaudeBatchProvider` in `Ai.Infrastructure`) + offline jobs consuming it.

## Status: BACKLOG — deferred. DO NOT build until scheduled.
Recorded per rule #9 to track the deferred parts of P3-01 (AI Gateway) rather than leaving them as dangling `🔲` sub-tasks inside `P3-01-BE.md`. **Not MVP-blocking** — the runtime AI tutor (streaming SSE P3-04/05/06 + on-demand generation + `AiResponseCache`) is complete and merged. This is a **cost optimization** (Anthropic Batch API ≈ 50% cheaper for bulk/offline work) that matters at scale. **Decompose into full tasks when the lead schedules it** (run analyzer → planner first).

## Candidate scope (to refine at scheduling)
| ID (provisional) | Task | Origin |
|---|---|---|
| P3-15-BE-1 | `IAiBatchGateway` seam in `Shared.Contracts/Ai/` — `Task<BatchSubmissionResult> SubmitBatchAsync(IReadOnlyList<AiRequest>, ct)` + `Task<BatchPollResult> PollBatchAsync(string batchJobId, ct)` + the two result DTOs. | was **P3-01-BE-13** |
| P3-15-BE-2 | `ClaudeBatchProvider` in `Ai.Infrastructure/Providers/` over the Anthropic Batch API; register in `AddAiModule`; config-selected, mirror the existing `IAiGateway` provider shape (rule #8 — one interface, one impl, no Strategy/Factory). | was **P3-01-BE-13** |
| P3-15-BE-3 | Deterministic **fake** batch provider for tests (submit → poll → results) so CI exercises submit/poll/result-mapping with **no live keys** (mirror the existing `DeterministicFakeAiGateway`). | new |
| P3-15-BE-4 | Offline **cache pre-warm** job — batch-generate common explain / why-wrong / practice responses per grade+subject+language into `AiResponseCache` (auto-approve threshold reused); fail-soft, **idempotent** (no duplicate rows on re-run). | consumer of BE-1/2 |
| P3-15-BE-5 | Offline **bulk question generation** job (P3-06 path) via batch → persist as `Draft` into the **P7-09 moderation** queue (never auto-publish), grounded only on retrieved curriculum context. | consumer of BE-1/2 |
| P3-15-BE-6 | Cost/quota guardrails + scheduling config per `ai-cost-routing.md`; off by default (no keys / no schedule). | brief |
| P3-15-BE-7 | api-tester + **security-auditor** (AI prompts + generated child-facing content + cost) — fail-soft, idempotency, grounding/no-hallucination, Draft-not-Published, no secrets logged. | gate |

## Explicitly OUT of scope (do not build under this story)
- **Per-plan daily request cap (`IAiUsageBudget`, was P3-01-BE-14)** — **SUPERSEDED by the Phase 10 energy economy** (charge-on-delivery, built + QC'd). P10 owns AI cost control; the interim guardrail is moot.
- **`IGlobalSettingsProvider` (was P3-01-BE-15)** — already satisfied: P10-12 ships the DB-backed + Redis-cached + audited `GlobalSettings` (the bootstrap seam was the interim).

## Notes
- **Activation is devops** even once built: dormant until provider keys + a job schedule exist (`docs/dev/AI-ACTIVATION-RUNBOOK.md`).
- Deps (all built + merged): P3-01 gateway, P3-04/05/06, `AiResponseCache`, P7-09 moderation.
- Module isolation: batch seam lives in `Shared.Contracts/Ai`; offline jobs in `Ai.Infrastructure` (question generation calls the Learning generation handler via existing seams). No cross-module FK.
