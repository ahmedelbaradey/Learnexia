# P3-10 (Backend) — Schedule spaced-repetition practice

> Story: [../../../user-stories/Phase-4-AI-Tutor/P3-10-spaced-repetition.md](../../../user-stories/Phase-4-AI-Tutor/P3-10-spaced-repetition.md)
> Phase 4 — AI Tutor · Epic: Adaptivity & Student Modeling · SP: 3 · FR-AD-4, FR-AD-2/3, FR-AI-6 · Est. unit: hours
> **Backend/infra only — no Frontend tasks.** Depends on P3-09 (mastery + reserved SR columns); parallel with P3-08 and P3-13 once P3-09 merges.

> **Status: ✅ Built + merged** — scheduler/engine/sweep/due-endpoint/mastery-integration in commit `25e9b98`; the deferred cross-module `ReviewDueIntegrationEvent` seam added 2026-06-20 (`feat/P3-10-review-due-event`), which **unblocks P9-09**.

## Backend tasks

| ID | Task | Artifact | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| P3-10-BE-1 | Verify that P3-09's migration already added `ReviewIntervalDays int default 0`, `NextReviewDueAt timestamptz NULL`, `RepetitionNumber int default 0` to `StudentSkillMastery`; if columns are missing, produce migration `AddSpacedRepetitionColumns` extending `StudentSkillMastery` (schema `learning`, all timestamps `timestamptz` UTC); confirm build + `dotnet ef migrations list` clean before proceeding | migration `AddSpacedRepetitionColumns` (only if P3-09 did not reserve the columns) | P3-09 merged | 1 | 🔲 |
| P3-10-BE-2 | `SpacedRepetitionOptions` — config-bound options: `int[] IntervalLadderDays` (default `[1, 3, 7, 14, 30]`), `int NeedsReviewIntervalDays` (default 1), `string JobCronExpression` (default `"0 0 * * *"` — daily midnight UTC); bound from `SpacedRepetition:Engine` config section; **SM-2 ease factor flagged as deferred upgrade — do NOT implement without lead approval** | `Application/Options/SpacedRepetitionOptions.cs`, `appsettings.json` section | None | 1 | 🔲 |
| P3-10-BE-3 | `SpacedRepetitionEngine` — pure static domain service: `ComputeNextReview(int repetitionNumber, bool improved, SpacedRepetitionOptions options) → (int nextIntervalDays, int nextRepetitionNumber)` (advance ladder on success, reset to index 0 on failure); `IsDue(MasteryStatus status, DateTime? nextReviewDueAt, DateTime utcNow) → bool` (due if `NeedsReview` OR (`Mastered` AND `utcNow >= nextReviewDueAt`)); all time arithmetic in UTC; unit-test: ladder progression (5+ cases), reset on failure, due-rule for both branches, idempotency (same inputs → same output) | `Domain/Services/SpacedRepetitionEngine.cs`, `tests/Modules.Learning.UnitTests/SpacedRepetitionEngineTests.cs` (NEW) | P3-10-BE-2 | 4 | 🔲 |
| P3-10-BE-4 | Repository additions for SR scheduler: `GetDueMasteryRowsAsync(DateTime utcNow)` (returns all `StudentSkillMastery` rows where `IsDue` per the engine rules — used by the sweep job); `UpdateSpacedRepetitionFieldsAsync(int studentSkillMasteryId, int intervalDays, int repetitionNumber, DateTime nextDueAt)` (targeted update for interval progression); ensure all queries use UTC `DateTimeKind` consistently (reference HANDOFF P2-08 UTC-quirk note) | `Application/Abstractions/ILearningRepository.cs` (EDIT), `Infrastructure/Repository/LearningRepository.cs` (EDIT) | P3-10-BE-1, P3-10-BE-3 | 3 | 🔲 |
| P3-10-BE-5 | `SpacedRepetitionSweepJob` in `Learning.Infrastructure/Jobs/` (mirror `StreakSweepJob`): iterates all `StudentSkillMastery` rows, calls `SpacedRepetitionEngine.IsDue`, recomputes `NextReviewDueAt` from current `RepetitionNumber` + `LastPracticedAt` for mastered-but-stale skills; pure recompute so running twice yields the same `NextReviewDueAt` (idempotent); register via `RecurringJob.AddOrUpdate` with a fixed job ID (`"SR-Sweep"`) in Learning module DI extension | `Infrastructure/Jobs/SpacedRepetitionSweepJob.cs` (NEW), Learning DI extension (EDIT) | P3-10-BE-4 | 4 | 🔲 |
| P3-10-BE-6 | Interval-progression completion hook: extend `CompleteAttemptCommandHandler` (after P3-09's mastery upsert, inside the same explicit transaction) — for each skill touched, if a `StudentSkillMastery` row exists and the skill was due, call `SpacedRepetitionEngine.ComputeNextReview(improved = newMasteryStatus != NeedsReview)` and write `ReviewIntervalDays` + `RepetitionNumber` + `NextReviewDueAt` back; if not due, skip (don't reset interval for non-review practice) | `Application/Features/Attempts/Commands/CompleteAttempt/CompleteAttemptCommandHandler.cs` (EDIT) | P3-10-BE-4, P3-09-BE-5 | 3 | 🔲 |
| P3-10-BE-7 | `GetDueReviewsQuery` + `GET /api/Learning/Reviews/Due` (`[Authorize(Roles="Student")]`, IDOR-guarded by `ICurrentUserService.UserId`); returns list of `DueReviewDto` (skillId, skillName, nextReviewDueAt, status); `BaseResponse<T>` + `Successed`; localization keys (`DueReviewsRetrievedSuccessfully`) in `SharedResourcesKey.cs` + both `.resx` files | `Application/Features/Reviews/Queries/*` (NEW), `Api/Controllers/ReviewsController.cs` (NEW), localization files (EDIT) | P3-10-BE-4 | 3 | 🔲 |

## Acceptance-criteria coverage

- **AC1 — Due selection:** `SpacedRepetitionEngine.IsDue` (status + `NextReviewDueAt`) + sweep job + repo query → **BE-3, BE-4, BE-5**
- **AC2 — Surfaced for reinforcement:** `GET /api/Learning/Reviews/Due` queryable due list → **BE-7**
- **AC3 — Background + idempotent:** `SpacedRepetitionSweepJob` daily recurring Hangfire job; pure recompute = idempotent → **BE-5**
- **AC4 — Expanding interval:** `SpacedRepetitionEngine.ComputeNextReview` ladder advancement + reset on completion hook → **BE-3, BE-6**

## Notes

- **Depends on P3-09 merged.** The SR columns may already be in the P3-09 migration (reserved by BE-1 of P3-09); BE-1 here is a verification step.
- **Parallel with P3-08 and P3-13** once P3-09 is merged (independent of the difficulty chain).
- **UTC discipline (critical):** all `DateTime` values for SR are `timestamptz`/UTC. Reference the HANDOFF P2-08 note on `DateTime.Kind=Local` bug. The engine operates on UTC `DateTime` arguments only.
- **Pattern caution (rule 8):** plain static engine + config options. SM-2 ease-factor model is a flagged deferred upgrade — do NOT introduce `EaseFactor` or a pluggable `IIntervalAlgorithm` without asking the lead.
- **Cross-module missions surfacing (AC2):** P3-10 exposes the due-list HTTP endpoint; Gamification missions (P4-06) will later consume it via an integration contract. Do NOT build a Gamification FK or cross-module call now. HANDOFF note required.
- **Hangfire in tests:** reuse the Gamification-job test setup pattern; the sweep job should be triggerable in the integration test harness via `BackgroundJob.Enqueue`.
- **Shared-file edits (serialize):** `ILearningRepository.cs`, `LearningRepository.cs`, `CompleteAttemptCommandHandler.cs`, `SharedResourcesKey.cs`, `*.resx`, Learning DI extension.
