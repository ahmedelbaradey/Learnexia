# P4-12 (Backend) — Timed-event participation

> Story: [../../../user-stories/Phase-3-Gamification/P4-12-timed-event-participation.md](../../../user-stories/Phase-3-Gamification/P4-12-timed-event-participation.md)
> Phase 3 — Gamification (Week 5) · Epic: Gamification Module · SP: 5 · FR-GM-9 (SRS §4.6); extends P4-11 · Est. unit: hours
> Stack: .NET 10 `gamification` module, CQRS/MediatR, Npgsql. Builds on P4-11 (`TimedEvent` + `IActiveTimedEventsQuery`). Pairs with P9-12.
> **Status: 🔲 Not started.**

## Backend tasks
| ID | Task | Artifact | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| P4-12-BE-1 | `TimedEventParticipation` aggregate + EF config + migration (`(TimedEventId, StudentId)` unique; Progress, Target, State, JoinedUtc, CompletedUtc?) | entity + `gamification` migration | P4-11-BE-3 | 4 | 🔲 |
| P4-12-BE-2 | **Lazy create** participation on a child's first qualifying action inside an active window (NOT pre-materialized at event start) | hook into the XP-award / qualifying-action path | P4-12-BE-1, P4-11-BE-3 | 4 | 🔲 |
| P4-12-BE-3 | Accumulate **progress** from qualifying in-window actions; transition in-progress → **completed** at target; ignore actions after `EndUtc` | progress/complete domain logic | P4-12-BE-2 | 4 | 🔲 |
| P4-12-BE-4 | Grant the completion reward **through the existing XP/badge engine** — no parallel reward path | wire into existing reward handlers | P4-12-BE-3, P4-02-BE-3, P4-05-BE-3 | 3 | 🔲 |
| P4-12-BE-5 | Cross-module read seams: `IEligibleStudentsForTimedEventQuery` (scope-matched cohort, on-demand) + participant/progress query | `Shared.Contracts/Gamification` interface + Gamification impl (Scoped) | P4-12-BE-1 | 4 | 🔲 |
| P4-12-BE-6 | Per-student lifecycle integration events (progress-milestone / ending-soon-at-risk / completed), post-commit, fail-soft, opaque ids | `Shared.Contracts/Gamification` events + republishers | P4-12-BE-3 | 4 | 🔲 |
| P4-12-BE-7 | Ending-soon/at-risk scan (close-but-incomplete near window close) + config dials (target, halfway %, ending-soon lead time, eligibility scope) | background scan + `TimedEventParticipationOptions` | P4-12-BE-3, P4-12-BE-6 | 4 | 🔲 |
| P4-12-BE-8 | Endpoint: my participation/progress for active events (JWT-scoped) | controller action | P4-12-BE-3 | 2 | 🔲 |

## Acceptance-criteria coverage
- Lazy per-child participation, no upfront fan-out → **BE-1, BE-2**
- Progress tracking + completion state, window-scoped, clean end → **BE-3**
- Reward via existing XP/badge engine, no parallel path → **BE-4**
- Eligibility (recruitment cohort) + participant read seams, no PII → **BE-5**
- Per-student progress/ending/completion integration events (fail-soft, post-commit) → **BE-6, BE-7**
- Config-driven dials, tunable without deploy → **BE-7**

## Contract for downstream (P9-12 Notifications)
- `IEligibleStudentsForTimedEventQuery.GetEligibleAsync(timedEventId, atUtc)` → eligible `studentId`s (scope-matched, on-demand — no materialized rows).
- `TimedEventParticipationProgressIntegrationEvent` / `...EndingSoonIntegrationEvent` / `...CompletedIntegrationEvent` — opaque ids + numeric scalars only.

## Contract for Frontend (P4-12-FE)
- My active-event participation + progress + completion (JWT-scoped read) for the timed-event card/countdown (extends the P4-11-FE surface).

## Notes
- **Recipient model (lead decision 2026-06-20):** participation entity + lifecycle + progress + completion + eligibility queries — **no blind active-student blast, no upfront per-child fan-out**. Mirror the `IActiveTimedEventsQuery` / `IStudentXpQuery` cross-module read-seam shape. Reward strictly through the existing engine (no parallel path — same rule as P4-11-BE-5). **analyzer + planner first** — pin "qualifying action" (in-window XP vs mission completion) and the per-event target source.
