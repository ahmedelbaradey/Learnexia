# P7-07 (Backend) — Suspend, reactivate & delete accounts

> Story: [../../../user-stories/Phase-7-Admin-Console/P7-07-suspend-reactivate-delete-accounts.md](../../../user-stories/Phase-7-Admin-Console/P7-07-suspend-reactivate-delete-accounts.md)
> Phase 7 — Admin Console · Epic: Admin — User & Account Management · SP: 5 · SRS §3, FR-ADM-6 · Est. unit: hours
> Stack: .NET 10 `Identity` module, CQRS/MediatR, FluentValidation, Npgsql.

## Backend tasks
| ID | Task | Artifact | Deps | Est (h) |
|---|---|---|---|---|
| P7-07-BE-1 | Account-status model: add `AccountStatus` (Active/Suspended/Deleted) + last-reason/actor fields to the Identity user; migration | `ApplicationUser.cs`, migration | P1-01-BE-2 | 3 |
| P7-07-BE-2 | `SuspendAccountCommand` — set Suspended, invalidate sessions/tokens, require `reason`; validator enforces reason + rejects already-deleted | `SuspendAccountCommandHandler.cs`, validator | P7-07-BE-1 | 3 |
| P7-07-BE-3 | `ReactivateAccountCommand` — restore Active, keep prior reason/history | `ReactivateAccountCommandHandler.cs`, validator | P7-07-BE-1 | 2 |
| P7-07-BE-4 | `DeleteAccountCommand` — two-step confirm + reason; remove/anonymize; for parents, cascade option to linked children (P1-04); explicit transaction for the multi-write | `DeleteAccountCommandHandler.cs`, validator | P7-07-BE-1, P1-04-BE-* | 4 |
| P7-07-BE-5 | Publish `AccountSuspended` / `AccountReactivated` / `AccountDeleted` integration events so learning/gamification clean up (no cross-module write) | `Shared.Contracts` events | P7-07-BE-2/3/4 | 3 |
| P7-07-BE-6 | `[Authorize(Policy = AuthorizationPolicies.AdminOnly)]` on lifecycle endpoints (extend Identity's existing `UserManagementController`, already AdminOnly) + emit audit entries (actor/timestamp/target/reason) per P7-12 | `UserManagementController.cs` (existing) | P1-05-BE-1, P7-12-BE-* | 2 |

## Acceptance-criteria coverage
- Suspend (reason + confirm, blocks sign-in) → **BE-1, BE-2**
- Reactivate (restore, history kept) → **BE-3**
- Cascade to linked children → **BE-4**
- Delete (two-step confirm + reason, anonymize, notify modules) → **BE-4, BE-5**
- Actor/timestamp/reason recorded + audited; reject already-deleted → **BE-2, BE-6**
- Admin-only access (non-admin 403) → **BE-6**

## Contract for Frontend (P7-07-FE)
- `POST /api/Admin/Users/{id}/suspend` `{ reason }` → `BaseResponse<AdminUserProfileDto>`.
- `POST /api/Admin/Users/{id}/reactivate` `{ reason? }` → `BaseResponse<AdminUserProfileDto>`.
- `DELETE /api/Admin/Users/{id}` `{ reason, confirm, cascadeChildren? }` → `BaseResponse<bool>`.
- All gated by the admin policy; commands are validated (ValidationBehavior on `ICommand<>`); `Successed` flag per envelope.

## Notes
- Reuses the **Identity** module — Identity already has `UserManagementController` gated AdminOnly; this story extends it (NOT greenfield). Mirror the **Identity / Learning** module shape (not Catalog — Catalog was removed 2026-06-03, PR #84).
- Cross-module cleanup is via `Shared.Contracts` integration events only — never direct FK/writes. Delete touches multiple Identity rows (parent + children) → wrap in an **explicit transaction** (no Unit of Work). Child data is sensitive: reason + confirm + audit are mandatory.
- **Policy:** `AuthorizationPolicies.AdminOnly` for now; promoting to granular `{Module}.{Action}` policies (via `Claims.GenerateModules()`) is a follow-up if the lead wants finer-grained authz.
