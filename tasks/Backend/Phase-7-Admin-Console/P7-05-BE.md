# P7-05 (Backend) — Publish, version & preview curriculum content

> Story: [../../../user-stories/Phase-7-Admin-Console/P7-05-publish-version-preview-curriculum.md](../../../user-stories/Phase-7-Admin-Console/P7-05-publish-version-preview-curriculum.md)
> Phase 7 — Admin Console · Epic: Admin — Curriculum Management · SP: 8 · SRS §3, FR-ADM-4 · Est. unit: hours
> Stack: .NET 10 `learning` + `assessment` modules, CQRS/MediatR, FluentValidation, Npgsql.

## Backend tasks
| ID | Task | Artifact | Deps | Est (h) |
|---|---|---|---|---|
| P7-05-BE-1 | Add lifecycle state (`Draft`/`Published`) to versionable curriculum entities (subject/unit/lesson/quiz) + EF config + Npgsql migration | `LifecycleState` prop, configs, migration | P7-01-BE-*, P7-02-BE-*, P7-04-BE-* | 5 |
| P7-05-BE-2 | `ContentVersion` entity (entity ref, version no., publishedAt, publishedBy, snapshot payload) + EF config + migration | `Domain/Entities/ContentVersion.cs`, migration | P7-05-BE-1 | 5 |
| P7-05-BE-3 | Publish command: snapshot current draft → new `ContentVersion`, flip live version, atomic in an explicit transaction | `PublishCommand(Handler)` | P7-05-BE-2 | 5 |
| P7-05-BE-4 | Student-facing reads filter to **latest published version**; admin reads return both live + pending draft | query filters | P7-05-BE-1, P7-05-BE-3 | 4 |
| P7-05-BE-5 | Preview query: render a draft as a student would without publishing; admin-only policy | `GetPreviewQuery(Handler)` | P7-05-BE-4 | 3 |
| P7-05-BE-6 | Rollback/revert command: restore the previous published `ContentVersion` (explicit transaction) | `RollbackCommand(Handler)` | P7-05-BE-3 | 3 |

## Acceptance-criteria coverage
- Edits accumulate as draft, not served to students → **BE-1, BE-4**
- Publish records a versioned snapshot (timestamp/author) → **BE-2, BE-3**
- Live vs pending-draft distinguishable → **BE-4**
- Preview draft as student without publishing → **BE-5**
- Revert/rollback restores previous published version → **BE-6**
- Admin-only access → **BE-5** (policy across publish/preview/rollback)

## Contract for Frontend (P7-05-FE)
- `GET /api/Learning/{type}/{id}?view=admin` → `BaseResponse<...>` with `liveVersion` + `draft` distinguished; `view=student` → latest published only.
- `POST /api/Learning/{type}/{id}/Publish` → `BaseResponse<int>` (new version no.).
- `GET /api/Learning/{type}/{id}/Preview` → `BaseResponse<...>` (student-rendered draft).
- `POST /api/Learning/{type}/{id}/Rollback` → `BaseResponse<int>`. Mirror equivalents in `assessment` for quizzes. All admin-only.

## Notes
- Lifecycle + version rows span `learning` and `assessment`; each module owns its own versions (no cross-module FK). Publish/rollback are atomic multi-writes via an explicit transaction (no Unit of Work).
- Mirror Catalog; `Successed`, `NewResult(...)`, `ILoggerManager`.
