# P7-05 (Backend) — Publish, version & preview curriculum content

> Story: [../../../user-stories/Phase-7-Admin-Console/P7-05-publish-version-preview-curriculum.md](../../../user-stories/Phase-7-Admin-Console/P7-05-publish-version-preview-curriculum.md)
> Phase 7 — Admin Console · Epic: Admin — Curriculum Management · SP: 8 · SRS §3, FR-ADM-4 · Est. unit: hours
> Stack: .NET 10 `Learning` module (covers both curriculum hierarchy and quizzes — there is no `assessment` module), CQRS/MediatR, FluentValidation, Npgsql.

> **Reality check (harden + extend, not greenfield):** Existing Learning controllers expose Create/Update/Delete but they are currently **anonymous (live auth hole — see `docs/briefs/phase-7-admin-gap-analysis.md`)**. This story adds the draft/published lifecycle on top of those entities — secure them via `[Authorize(AdminOnly)]` (covered by P7-01..P7-04) before exposing the publish/rollback endpoints.

## Backend tasks
| ID | Task | Artifact | Deps | Est (h) |
|---|---|---|---|---|
| P7-05-BE-1 | Add lifecycle state (`Draft`/`Published`) to versionable curriculum entities (subject/unit/lesson/quiz, all in `Learning`) + EF config + Npgsql migration | `LifecycleState` prop, configs, migration | P7-01-BE-*, P7-02-BE-*, P7-04-BE-* | 5 |
| P7-05-BE-2 | `ContentVersion` entity (entity ref, version no., publishedAt, publishedBy, snapshot payload) + EF config + migration | `Modules.Learning/Domain/Entities/ContentVersion.cs`, migration | P7-05-BE-1 | 5 |
| P7-05-BE-3 | Publish command: snapshot current draft → new `ContentVersion`, flip live version, atomic in an explicit transaction; `[Authorize(Policy = AuthorizationPolicies.AdminOnly)]` | `PublishCommand(Handler)` | P7-05-BE-2 | 5 |
| P7-05-BE-4 | Student-facing reads filter to **latest published version**; admin reads return both live + pending draft | query filters | P7-05-BE-1, P7-05-BE-3 | 4 |
| P7-05-BE-5 | Preview query: render a draft as a student would without publishing; `[Authorize(Policy = AuthorizationPolicies.AdminOnly)]`. Snapshots/preview media references stay served via `Shared.Kernel.Storage.IStorageService` | `GetPreviewQuery(Handler)` | P7-05-BE-4 | 3 |
| P7-05-BE-6 | Rollback/revert command: restore the previous published `ContentVersion` (explicit transaction); `[Authorize(Policy = AuthorizationPolicies.AdminOnly)]` | `RollbackCommand(Handler)` | P7-05-BE-3 | 3 |
| P7-05-BE-7 | Per-language tree operations: publish/version/preview/rollback operate on a single `(SubjectCode, Language)` tree (language inherited from the Subject root via `SubjectLanguageResolver`) — publishing the Ar Math tree never touches the En Math tree. Snapshots record the tree's resolved language for traceability | publish/preview/rollback handlers + `ContentVersion` language tag | P7-05-BE-3, P7-01-BE-7 | 3 |
| P7-05-BE-8 | Per-language publication-coverage query: report publish state (draft/published/version no.) per `(SubjectCode, Language)` tree for a grade, so admins see which language trees are still unpublished; read-only, AdminOnly | `GetPublicationCoverageQuery(Handler)` | P7-05-BE-4, P7-01-BE-8 | 3 |

## Acceptance-criteria coverage
- Edits accumulate as draft, not served to students → **BE-1, BE-4**
- Publish records a versioned snapshot (timestamp/author) → **BE-2, BE-3**
- Live vs pending-draft distinguishable → **BE-4**
- Preview draft as student without publishing → **BE-5**
- Revert/rollback restores previous published version → **BE-6**
- Publish/version/preview/rollback scoped per `(SubjectCode, Language)` tree → **BE-7**
- Per-language publication-coverage (which language trees are published) → **BE-8**
- Admin-only access → **BE-5** (policy across publish/preview/rollback)

## Contract for Frontend (P7-05-FE)
- `GET /api/Learning/{type}/{id}?view=admin` → `BaseResponse<...>` with `liveVersion` + `draft` distinguished; `view=student` → latest published only.
- `POST /api/Learning/{type}/{id}/Publish` → `BaseResponse<int>` (new version no.).
- `GET /api/Learning/{type}/{id}/Preview` → `BaseResponse<...>` (student-rendered draft).
- `POST /api/Learning/{type}/{id}/Rollback` → `BaseResponse<int>`. `{type}` covers subject/unit/lesson/quiz — all under the `Learning` module. All admin-only.
- `GET /api/Learning/Publish/Coverage?gradeId=` → `BaseResponse<PublicationCoverageDto>` (publish state per `(subjectCode, language)` tree). Admin-only.

## Notes
- **Localization (Phase 8, merged):** each grade has **6 subject roots** (`SubjectCode` × `Language`); publish/version/preview/rollback are per `(SubjectCode, Language)` tree — the two Math (Ar/En) trees publish independently. Language is inherited from the Subject root (`SubjectLanguageResolver`), not a column on child entities. Coverage reads complement P7-01-BE-8's existence coverage with publish-state per language tree.
- Lifecycle + version rows live entirely in the `Learning` module (covers subject/unit/lesson/quiz — there is no separate `assessment` module). Publish/rollback are atomic multi-writes via an explicit transaction (no Unit of Work).
- Mirror **Learning**; `Successed`, `NewResult(...)`, `ILoggerManager`.
- **Storage:** preview/media references stay behind `Shared.Kernel.Storage.IStorageService`; no module-local MinIO.
- **Module isolation:** no cross-module FK; any cross-module data flows via `Shared.Contracts` only.
- **Policy:** `AuthorizationPolicies.AdminOnly` for now; promoting to granular `{Module}.{Action}` policies (via `Claims.GenerateModules()`) is a follow-up if the lead wants finer-grained authz.
