# P1-06 (Backend) — PostgreSQL + pgvector + Redis (Npgsql migration)

> Story: [../../../user-stories/Phase-1-Foundation/P1-06-provision-postgres-pgvector-redis.md](../../../user-stories/Phase-1-Foundation/P1-06-provision-postgres-pgvector-redis.md)
> Phase 1 — Foundation · Epic: Data/DB · SP: 5 · NFR-2, SRS §7 · Est. unit: hours
> **Backend/infra only — no Frontend tasks. Blocker for all backend work.**

## Backend tasks
| ID | Task | Artifact | Deps | Est (h) |
|---|---|---|---|---|
| P1-06-BE-1 | Switch EF Core provider SQL Server → **Npgsql** in each module's `AddXInfrastructure` DI | module `DependencyInjection.cs` | — | 5 |
| P1-06-BE-2 | Provision **PostgreSQL 16 + pgvector** and **Redis** containers; enable `vector` extension | `docker-compose.yaml` (with P1-07) | — | 4 |
| P1-06-BE-3 | Regenerate Identity migration for Npgsql; configure **per-module schemas** (identity, learning, assessment, gamification, analytics, curriculum) | EF migrations | P1-06-BE-1 | 5 |
| P1-06-BE-4 | Wire Redis as `IDistributedCache` for sessions + token cache | host config | P1-06-BE-2 | 2 |
| P1-06-BE-5 | Remove SQL Server provider/packages; verify Identity tables operate on PostgreSQL | cleanup + smoke test | P1-06-BE-3 | 3 |
| P1-06-BE-6 | Prove a usable **pgvector** column type (vector) via a sample migration | migration + test | P1-06-BE-3 | 2 |

## Acceptance-criteria coverage
- API connects via Npgsql, migrations run → **BE-1, BE-3, BE-5**
- pgvector enabled + vector column usable → **BE-2, BE-6**
- Redis for cache + session → **BE-4**
- Per-module schemas/migrations configured → **BE-3**
- Legacy SQL Server removed; Identity works on Postgres → **BE-5**

## Notes
- One-time migration, not a decision (SRS §7); everything else in Phase 1/2 is **blocked by** this story.
- **Status update (P1-06):** The SQL Server → Npgsql provider switch is **already complete** in the codebase — every module DbContext uses `UseNpgsql`, per-module schemas + PostgreSQL migrations exist, and docker-compose runs `pgvector/pgvector:pg17` + `redis`. There is no `Microsoft.EntityFrameworkCore.SqlServer` package anywhere (only a stale commented block in Identity DI, now removed). BE-1, BE-2, BE-3 (for the identity/catalog/notifications modules), and BE-5 are done. The remaining real work was BE-4 (wire Redis as `IDistributedCache` in the Host) and BE-6 (prove a usable `pgvector` column).
