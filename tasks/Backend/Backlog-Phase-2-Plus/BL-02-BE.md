# BL-02 (Backend + Python) — Parse curriculum files into structured content (Multimodal Parsing — Stage 1)

> Story: [../../../user-stories/Backlog-Phase-2-Plus/BL-02-multimodal-parsing.md](../../../user-stories/Backlog-Phase-2-Plus/BL-02-multimodal-parsing.md)
> Backlog Phase 2+ — Curriculum Intelligence · Epic: Curriculum Intelligence · SP: 5 · FR-CI-2 (parsing/OCR half) · Est. unit: hours
> **Two stacks: .NET orchestration + Python parsing service. Python tasks have IDs `BL-02-PY-n`.**
>
> **Cross-cutting system decisions (A–E) are in `docs/briefs/curriculum-system-of-record.md`.** BL-02 is impacted by Decision B (provenance tree initialization — new BE-6) and Decision C (all content stays in Draft version context; BL-02 does not publish). No embedding involvement (Decision D — that is BL-05/P3-07).

> **Status: ✅ BUILT + MERGED** (`feat/BL-02-multimodal-parsing`, 2026-06-23 — first Python in the repo) — .NET orchestration slice (trigger + store artifact + status/diagnostics) + Python parsing service (Azure Document Intelligence OCR + RAG-Anything multimodal extraction). *(Prior "Not started" marker was stale planning-time.)*

> **LEAD DECISIONS:**
> - ~~**Q1 — .NET-vs-Python boundary:**~~ **DECIDED: DB-outbox + Python poller** (see `docs/briefs/curriculum-system-of-record.md` §4b). .NET writes `PipelineJobs` rows (BL-01-BE-9); Python polls and claims them (BL-02-PY-9); a .NET `BackgroundService` (BL-02-BE-7) advances `CurriculumDocument.ParseStatus` on completion. No MediatR event handler — MediatR events cannot reach a separate Python process.
> - **Q2 — Scope in this repo:** Python service (`python/curriculum_intelligence/`) lives in this repo unless lead decides otherwise. PY-1..9 are scoped there.
> - **Q3 — Artifact storage:** large parsed JSON in MinIO (object) with a relational pointer (recommended) vs. JSONB column in Postgres?
> - **Q4 — Azure DI provisioning:** is an Azure Document Intelligence resource + key available (Arabic OCR)? Where do secrets live?
> - **Q5 — Module placement:** inherits BL-04's decision.
>
> **LEAD-APPROVED STACK (encode as decided; do not re-litigate):**
> - **Orchestration framework:** RAG-Anything (HKUDS; built on LightRAG). Verify MIT license before adopting.
> - **Primary OCR/parser:** Azure Document Intelligence (`prebuilt-layout` model). Must preserve Arabic diacritics/tashkeel end-to-end through extraction and artifact serialization. Azure DI is plugged into RAG-Anything as a **custom parser** — the framework provides the orchestration pipeline; Azure DI provides the Arabic-accurate OCR/layout recognition.
> - **Fallback / benchmark comparison:** RAG-Anything's default parsers (MinerU, PaddleOCR). NOT the default path; used only for benchmark comparison and as the fallback if Azure DI is unavailable or fails.
> - **Diagram / image understanding:** Claude (Opus/Sonnet-Vision) as the VLM for Arabic-capable diagram and image captioning. This is **offline, async work** — expensive models are cost-justified (see `docs/briefs/ai-cost-routing.md` offline lane, when that brief exists).
> - **Benchmark-first gate:** before dispatching PY-2 (Azure DI integration), build a 20–30 page Arabic benchmark set (dense text, diacritized lower-grade pages, geometry/science diagrams, equation-heavy Math, a scanned/photographed worksheet) and measure Azure DI vs MinerU vs PaddleOCR + Claude diagram-captioning on real Arabic accuracy. **This benchmark gates BL-02 dispatch.** Results stored in `python/curriculum_intelligence/benchmarks/`.
> - All tool recommendations are "recommended-and-benchmark-to-confirm," not absolute guarantees. Verify licenses (RAG-Anything, LightRAG: MIT — confirm before adopting).

## Backend tasks (.NET — orchestration slice)

| ID | Task | Artifact | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| BL-02-BE-1 | Extend `CurriculumDocument` with parse-result fields: `ParsedArtifactObjectKey` (string, nullable — MinIO key for the structured JSON), `ParseStatus` (enum or string: `Pending`/`Processing`/`Done`/`Failed`), `ParseDiagnostics` (string, nullable), `ParsedAt` (DateTimeOffset, nullable). Npgsql migration `AddCurriculumDocumentParseFields`. Index on `ParseStatus`. | `Domain/Entities/CurriculumDocument.cs` (extend), `Persistence/Configurations/CurriculumDocumentConfig.cs` (extend), migration | BL-01 complete | 3 | 🔲 |
| BL-02-BE-2 | `IParsingServiceClient` interface (Infrastructure abstraction, mockable): `Task<ParseArtifactResult> ParseAsync(string objectKey, string contentType, CancellationToken ct)` where `ParseArtifactResult` carries `{ bool Success, string? ArtifactJson, string? Diagnostics }`. Register a typed HTTP client in the module's DI (config-driven base URL + API key — secrets from environment/secret manager, never source). | `Application/Abstractions/IParsingServiceClient.cs`, `Infrastructure/Clients/ParsingServiceClient.cs`, DI registration | BL-01 complete | 4 | 🔲 |
| BL-02-BE-3 | **REVISED (result-reader, not event handler):** `ParseCurriculumDocumentResultHandler` — a component invoked by the .NET job-advance `BackgroundService` (BE-7) when a `PipelineJobs` row transitions to `Done` or `Failed` for `JobType='parse'`. Reads `ResultJson` from the completed job row (containing `{ artifact_key, chapters: [...], parse_status, diagnostics }`); stores the artifact pointer (`ParsedArtifactObjectKey`) on `CurriculumDocument`; sets `ParseStatus = Done` + `ParsedAt` on success, or `ParseStatus = Failed` + `ParseDiagnostics` on failure. Does NOT call `IParsingServiceClient` directly — the Python service is the caller, not the callee here. Mirror `BaseResponseHandler`, `ILoggerManager`, `ServerError<T>`. Note: `IParsingServiceClient` (BE-2) is retained as a mockable abstraction for api-tester integration tests that need to simulate a completed job without a real Python service. | `Application/Features/CurriculumDocuments/Handlers/ParseCurriculumDocumentResultHandler.cs` | BL-02-BE-1, BL-02-BE-7 | 4 | 🔲 |
| BL-02-BE-4 | `ReparseCurriculumDocumentCommand` + handler: admin-triggered per-document re-run (AC4 idempotency). Resets `ParseStatus = Pending`, overwrites the prior artifact on success. Admin-only command. Idempotent: calling again while already `Processing` returns a 409/conflict envelope. | `Application/Features/CurriculumDocuments/Commands/ReparseDocument/**` | BL-02-BE-2, BL-02-BE-3 | 3 | 🔲 |
| BL-02-BE-5 | Extend `CurriculumDocumentsController` with parse endpoints: `POST /{id}/reparse` (admin-only, triggers re-parse). Parse status and diagnostics already visible via BL-01's GET detail endpoint (no new GET needed unless a dedicated parse-status endpoint is required). Wire `[Authorize(Roles="Admin")]`. | `Api/Controllers/CurriculumDocumentsController.cs` (extend) | BL-02-BE-3, BL-02-BE-4 | 2 | 🔲 |
| BL-02-BE-6 | **Provenance tree initialization (Decision B):** invoked by `ParseCurriculumDocumentResultHandler` (BE-3) when a `parse` job completes successfully. Deserializes the `chapters` array from the job's `ResultJson` (chapter numbers, titles, page ranges — written by the Python service in PY-4). Creates one `ContentSource` record linked to the `CurriculumDocument` (type = `Book` for PDF/DOCX, `Image` for image files, `Worksheet` if layout indicates worksheet). Creates one `Chapter` record per detected chapter. Idempotent: re-parse deletes prior `Chapter` rows for this `ContentSource` and re-inserts (upsert on `(ContentSourceId, Number)`). Uses entities from BL-04 (`ContentSource`, `Chapter`) — BL-04 must be complete first. Mirror `BaseResponseHandler`, `ILoggerManager`. | `Application/Features/CurriculumDocuments/Handlers/ParseCurriculumDocumentResultHandler.cs` (extend), `ContentSource`/`Chapter` persistence within chosen module's DbContext | BL-02-BE-3, BL-04 complete | 3 | 🔲 |
| BL-02-BE-7 | **NEW — .NET job-advance `BackgroundService` for parse jobs (DB-outbox poller):** Implement a `ParseJobAdvanceService : BackgroundService` that polls `curriculum.PipelineJobs WHERE Status IN ('Done','Failed') AND JobType='parse'` on a configurable interval (default 5 s). For each completed job row: calls `ParseCurriculumDocumentResultHandler` (BE-3) which reads `ResultJson` and advances `CurriculumDocument.ParseStatus`. On success: sets `PipelineJob.Status = 'Done'` (already done by Python) and marks it processed (or deletes the row; confirm with lead). On failure: increments `RetryCount`; if `RetryCount >= MaxRetries` (config, default 3), sets `Status = 'PermanentlyFailed'` and logs an alert. Thread-safe: use `SELECT ... FOR UPDATE SKIP LOCKED` to avoid double-processing if multiple .NET instances run. Registered in the module's DI as a hosted service. Add config class `PipelineJobPollerConfiguration` (interval, max retries). | `Infrastructure/BackgroundServices/ParseJobAdvanceService.cs`, `Infrastructure/Configuration/PipelineJobPollerConfiguration.cs`, DI registration in module | BL-01-BE-9 (PipelineJobs entity exists), BL-02-BE-3 | 4 | 🔲 |

## Python pipeline tasks — parsing service

> **Python service identity (LOCKED 2026-06-13 — FINAL LOCKED AI ARCHITECTURE):** a FastAPI service (matching SRS §2 `aigw` / `curriculum-intelligence`) that implements the `IParsingServiceClient` contract, living **same-repo at `python/curriculum_intelligence/`** (folder layout: `app/ parsers/ embeddings/ kg/ workers/`). The earlier "separate repo vs sub-folder — lead to confirm" question is **CLOSED → same-repo.** **Shared Python foundation:** `BL-02-PY-1` (service skeleton — FastAPI app, `requirements`, the `PipelineJobs` poll/claim framework, DB access layer, Dockerfile, CI) is the **shared foundation that `BL-03-PY` and `BL-05-PY` build on** — it runs first; those stories extend it, they do not re-bootstrap the service. All `BL-02-PY-n` tasks are scoped to `python/curriculum_intelligence/`. **Note:** this Python service is the offline ingestion factory; it is distinct from the runtime BGE-M3 TEI embedding endpoint that P3-07 (`P3-07-BE-0`) stands up for the AI Tutor — same model family (BGE-M3), different lane (offline batch vs synchronous runtime).
>
> **Frameworks evaluated:** RAG-Anything (HKUDS, built on LightRAG) as the orchestration backbone. Verify MIT license (RAG-Anything, LightRAG) before adopting. Azure DI is plugged in as a custom parser overriding RAG-Anything's default OCR (MinerU/PaddleOCR). MinerU and PaddleOCR remain available as fallback/benchmark parsers.

| ID | Task | Artifact | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| BL-02-PY-9 | **NEW — Python polling loop + atomic job claim (DB-outbox poller, must run first):** Implement the `PipelinePoller` class that replaces the `POST /parse` HTTP endpoint as the ingestion trigger. Poll `curriculum.PipelineJobs WHERE Status='Pending' AND JobType='parse'` on a configurable interval (default 5 s; `LISTEN/NOTIFY` optimisation optional). Atomically claim a job row: `UPDATE curriculum."PipelineJobs" SET "Status"='Processing', "ClaimedAt"=now() WHERE "Id"=<id> AND "Status"='Pending' RETURNING *` — this ensures only one worker processes a given job. On claim success: run the parse pipeline (calls PY-2/PY-3/PY-4); write results back: `UPDATE ... SET "Status"='Done'/'Failed', "ResultJson"='<artifact>', "CompletedAt"=now(), "ErrorMessage"=<err>`. On claim failure (another worker claimed it first): skip and poll next. Connect to Postgres directly via `psycopg` (or `asyncpg`) using the same connection string as the .NET backend (config-driven, from env var `DATABASE_URL`). No FastAPI route is needed for the polling path; FastAPI app is retained for health/status endpoints. Note: the Python service does NOT need an HTTP caller from .NET — it drives itself by polling. | `python/curriculum_intelligence/pipeline_poller.py`, `python/curriculum_intelligence/config.py` | Lead confirms scope/location + `PipelineJobs` table exists (BL-01-BE-9) | 3 | 🔲 |
| BL-02-PY-1 | **REVISED (polling entry-point replaces HTTP trigger):** Bootstrap the Python parsing service: FastAPI app skeleton with health/status endpoints only (the `POST /parse` HTTP endpoint is replaced by the `PipelinePoller` from PY-9 — .NET no longer calls Python over HTTP for triggering). Dockerfile, `pyproject.toml`. Install RAG-Anything, `azure-ai-documentintelligence`, boto3 (MinIO), `anthropic` SDK, `psycopg[binary]` or `asyncpg` (for DB polling). Confirm and document RAG-Anything + LightRAG license (MIT — verify before adopting). Entry point starts both the FastAPI app (health) and the `PipelinePoller` (via asyncio or a thread). | `python/curriculum_intelligence/main.py`, `Dockerfile`, `pyproject.toml` | Lead confirms scope/location; BL-02-PY-9 (poller design) | 4 | 🔲 |
| BL-02-PY-1b | **Arabic OCR benchmark set:** assemble a 20–30 page benchmark corpus: (a) dense Arabic prose page, (b) diacritized (tashkeel-heavy) lower-grade page, (c) geometry/science diagram page with Arabic labels, (d) equation-heavy Math page, (e) scanned/photographed worksheet. Store sample files in `python/curriculum_intelligence/benchmarks/samples/`. This benchmark must be run (PY-7) before PY-2 is dispatched. | `python/curriculum_intelligence/benchmarks/samples/` | Lead provides sample files | 2 | 🔲 |
| BL-02-PY-2 | **Azure Document Intelligence integration (PRIMARY parser):** given an `object_key`, download the file from MinIO (S3 SDK), call Azure DI `AnalyzeDocumentAsync` (model `prebuilt-layout` — handles text + tables + figures + layout structure). Set Arabic (`ar`) locale explicitly. **Preserve diacritics/tashkeel:** confirm Azure DI returns diacritized text; log and assert any tashkeel stripping in the smoke test. Plug Azure DI into RAG-Anything as a custom parser (override the default MinerU/PaddleOCR parsers). Return raw Azure DI analysis result. | `services/parsing/azure_di_client.py`, `services/parsing/rag_anything_custom_parser.py` | BL-02-PY-1, BL-02-PY-7 (benchmark gate passed) | 5 | 🔲 |
| BL-02-PY-3 | **RAG-Anything multimodal extraction layer (orchestration):** use RAG-Anything (with Azure DI as the custom parser backend) to extract `{ text, images: [...], tables: [...], equations: [...], layout: [...] }` with page/region source references for traceability. For images and diagrams, call **Claude (Sonnet-Vision or Opus as cost allows per the offline lane)** to generate Arabic-capable captions (Arabic text recognized, mixed ar/en supported). Handle DOCX via python-docx as a separate pre-processing step before feeding to Azure DI. Keep MinerU/PaddleOCR wired as the RAG-Anything fallback path so the framework can fall back if Azure DI is unavailable. | `services/parsing/multimodal_extractor.py`, `services/parsing/diagram_captioner.py` | BL-02-PY-2 | 6 | 🔲 |
| BL-02-PY-4 | Normalize and serialize the structured artifact to JSON. **Schema must include chapter structure (Decision B — provenance tree):** `{ document_id, chapters: [{ number, title, page_start, page_end }], chunks: [{ content_type, text, image_ref, diagram_caption, source_page, source_region, chapter_number, parser_used }] }`. The `chapters` array is what the .NET orchestrator (BE-6) uses to populate `Chapter` records in the provenance tree. Include a `parser_used` field (`azure_di` / `mineru` / `paddleocr`) per chunk for traceability. Write the JSON artifact back to MinIO (large textbooks must use MinIO; the `chapters` array is small enough to return inline alongside the MinIO key). | `services/parsing/artifact_serializer.py` | BL-02-PY-3 | 3 | 🔲 |
| BL-02-PY-5 | Error handling, diagnostics, and re-run support: surface Azure DI / extraction failures with structured diagnostics (error code + message) in the API response so the .NET orchestrator can store `ParseDiagnostics`. Idempotent: re-calling `POST /parse` for the same document overwrites the prior artifact. If Azure DI fails, log and retry with MinerU fallback; record the fallback in `parser_used`. | `services/parsing/error_handling.py` | BL-02-PY-4 | 3 | 🔲 |
| BL-02-PY-6 | **Arabic OCR smoke test (extended):** parse a sample scanned Arabic PDF (textbook page, from the benchmark corpus PY-1b); assert: (1) `text` field is non-empty and contains Arabic characters; (2) `source_page` references are present; (3) **diacritics/tashkeel are preserved** — assert at least one diacritic character (U+064B–U+065F range) appears in the extracted text when the sample page contains tashkeel; (4) `parser_used` field equals `azure_di` for primary-path assertions. | `tests/test_arabic_ocr.py` | BL-02-PY-2, BL-02-PY-3 | 3 | 🔲 |
| BL-02-PY-7 | **Arabic OCR benchmark runner (benchmark-first gate):** run Azure DI, MinerU, and PaddleOCR against all five benchmark samples (PY-1b). Measure per-page: character accuracy rate (CAR), tashkeel preservation rate, diagram/table detection flag. Record results in `python/curriculum_intelligence/benchmarks/results/benchmark_report.md`. The benchmark must confirm Azure DI is the superior Arabic parser (or flag if not, escalating to lead) before PY-2 is dispatched. Also run Claude diagram-captioning on the geometry/diagram sample and record caption quality. | `python/curriculum_intelligence/benchmarks/benchmark_runner.py`, `benchmarks/results/benchmark_report.md` | BL-02-PY-1b | 5 | 🔲 |
| BL-02-PY-8 | **Diacritics-preservation integration test:** end-to-end test that sends a diacritized Arabic page through the full parse pipeline (Azure DI → RAG-Anything extraction → artifact serialization) and asserts: (1) tashkeel characters survive each stage; (2) the final artifact JSON's `text` field contains the expected diacritized words from the sample; (3) no encoding normalization step silently strips U+064B–U+065F. | `tests/test_diacritics_preservation.py` | BL-02-PY-6, BL-02-PY-4 | 3 | 🔲 |

## Acceptance-criteria coverage

- AC 1 — Scanned Arabic PDFs OCR'd via Azure DI (primary) → **PY-2, PY-6, PY-7 (benchmark gate), PY-8 (diacritics)**
- AC 2 — Extract text/images/tables/equations/layout (RAG-Anything orchestration + Claude diagram captioning) → **PY-3, PY-4**
- AC 3 — Structured artifact with source references + `parser_used` traceability + chapter structure (Decision B) → **PY-4** (chapter array written into `PipelineJobs.ResultJson`); **.NET reads result + stores artifact pointer + provenance records** → **BE-3 (revised), BE-6, BE-7 (poller)**
- AC 4 — Failures surface with diagnostics; per-document re-run; Azure DI fallback to MinerU recorded → **BE-4, BE-5** (.NET — re-parse writes new Pending job row); **PY-5** (Python error handling)
- AC Seam — Cross-process transport is DB-outbox + Python poller (no MediatR, no broker) → **BL-01-BE-9** (PipelineJobs entity), **BE-7** (.NET poller), **PY-9** (Python poller)
- AC B — Provenance tree initialized (`ContentSource` + `Chapter` rows) after successful parse → **BE-6** (reads from job ResultJson)

## Notes

- **Lead-approved stack (do not re-litigate):** RAG-Anything as orchestration framework (verify MIT license); Azure Document Intelligence as the PRIMARY custom parser (Arabic print+handwriting+layout+RTL; diacritics/tashkeel must be preserved end-to-end); MinerU/PaddleOCR are the FALLBACK/benchmark parsers plugged into RAG-Anything's default path; Claude (Sonnet-Vision/Opus) as the VLM for diagram/image captioning in the offline lane.
- **Benchmark-first gate:** PY-1b + PY-7 must complete before PY-2 is dispatched. The benchmark report must confirm Azure DI superiority on Arabic (or escalate to lead if results are mixed).
- **Cross-process transport — DB-outbox + Python poller (DECIDED):** .NET writes `PipelineJobs` rows (BL-01-BE-9 entity); Python polls + claims + writes results (PY-9); .NET `BackgroundService` reads results + advances document status (BE-7). MediatR integration events are NOT the transport — they are in-process only. No external broker is added. The `PipelineJobs` table is the contract; swapping the transport to a broker later is a contained change. See `curriculum-system-of-record.md` §4b.
- api-tester validates the **.NET orchestration** by seeding a `Done` `PipelineJobs` row directly in the test DB (simulating a completed Python parse job) and asserting that the .NET poller advances `CurriculumDocument.ParseStatus` correctly. No real Azure DI or Python service needed in CI.
- **Security-auditor gate is REQUIRED** (external service credentials + AI/admin surface + Claude VLM key) before reviewer.
- No designer stage; no student-facing UI.
- If the lead confirms the Python service is tracked in a separate repo, PY-1..8 tasks become a cross-team hand-off note; the .NET BE-1..5 tasks remain in scope for this repo.
- **Azure DI key and Claude API key** must be in a secret manager / environment variable — never in source (architecture §14).
- Large parsed JSON artifacts go to MinIO (S3) with a relational pointer — not in a Postgres column (textbooks can be many MB; avoid row bloat).
- All Azure DI / BGE-M3 / RAG-Anything specifics are "recommended-and-benchmark-to-confirm," not absolute guarantees. Verify licenses before adopting.
