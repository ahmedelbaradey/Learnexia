# BL-04 (Backend) — Curriculum, knowledge-graph & vector schema (Technical Enabler)

> Story: [../../../user-stories/Backlog-Phase-2-Plus/BL-04-curriculum-vector-schema.md](../../../user-stories/Backlog-Phase-2-Plus/BL-04-curriculum-vector-schema.md)
> Backlog Phase 2+ — Curriculum Intelligence · Epic: Curriculum Intelligence · SP: 3 · FR-CI-4 (underpins FR-CI-1/2/3) · Est. unit: hours
> **Backend/infra only — no Frontend tasks. No Python pipeline tasks (schema enabler is pure .NET/EF).**
>
> **Cross-cutting system decisions (A–E) are in `docs/briefs/curriculum-system-of-record.md` — read that document before implementing any task.**

> **Status: 🔲 Not started** — Multiple entities: `CurriculumChunk` (NO inline vector — Decision D), `chunk_embeddings` (separate table — Decision D), `ContentSource`/`Chapter`/`ProvenanceMapping` (provenance tree — Decision B), `CurriculumVersion` (versioning — Decision C), `KGSuggestion` (KG suggestion queue — Decision E), `SkillKey` column on `KnowledgeNode` (stable semantic identity — Decision C). `KnowledgeNode`/`KnowledgeEdge` already exist in P2-11 and are reused in place (the `curriculum` module is their logical owner, not their physical home — Decision A).

> **LEAD DECISION REQUIRED BEFORE ANY TASK STARTS:**
> - **Q1 — New module or fold into `learning`?** Option A = new `curriculum` module; Option B = fold all new entities into `learning`. Affects every task below. Do NOT scaffold a new module without explicit lead sign-off (CLAUDE.md). **Regardless of this choice, `KnowledgeNode`/`KnowledgeEdge` stay in `learning` — this is decided (Decision A).**
> - **Q2 — Embedding model: RESOLVED → BGE-M3, N=1024.** Embeddings live in a separate `chunk_embeddings` table (Decision D — `EmbeddingVectorRef` inline column is REMOVED from `CurriculumChunk`). `chunk_embeddings` uses `vector(1024)`. Paid alternative: Cohere `embed-multilingual-v3` (also 1024-dim — compatible). Verify BGE-M3 license before adopting.
> - **Q3 — `SkillKey` backfill:** the column is added nullable. Confirm whether the data migration backfilling P2-11 seed rows is in BL-04 scope (task BE-13) or a separate story.
> - **Q4 — `CurriculumVersion` + P7-05 alignment:** confirm BL-04's `CurriculumVersion` entity is the same entity P7-05's publish/version surface will use, with `{Draft, Active, Archived}` status enum (`Active` = `Published` in P7-05 language). Do not duplicate.

## Backend tasks (.NET)

| ID | Task | Artifact | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| BL-04-BE-1 | **Module scaffold (option A only):** Create `Learnexia.Modules.Curriculum` projects (Domain / Application / Infrastructure / Api), add to `Learnexia.Modular.sln`, add `AddCurriculumModule` / `InitializeAsync` migrate hook, register assembly in Host — mirror `Learning` module structure exactly. **Skip if option B chosen.** | `Modules/Curriculum/**`, `.sln` update, `Program.cs` | Lead decision Q1 | 6 | 🔲 |
| BL-04-BE-2 | Add `Pgvector` + `Pgvector.EntityFrameworkCore` NuGet packages to `Directory.Packages.props`; configure Npgsql with `o.UseVector()` on the chosen DbContext; enable `CREATE EXTENSION IF NOT EXISTS vector;` in the migration (or a pre-migration script). **Shared file — serialize with any concurrent `Directory.Packages.props` edit.** | `Directory.Packages.props`, chosen DbContext | BL-04-BE-1 (if option A); or `LearningDbContext` | 2 | 🔲 |
| BL-04-BE-3 | Add `CurriculumChunk` domain entity: fields `Id` (int PK), `ConceptId` (int — FK if option B, plain indexed int if option A), `Content` (string), `Metadata` (string/JSONB), `Difficulty` (string per SRS §6), `ProvenanceRef` (string — references the source Page/Block within a `ContentSource`; format: `{ContentSourceId}:{ChapterId}:{PageOrBlockRef}`). **Do NOT add an `EmbeddingVectorRef` vector column here — embeddings are in the separate `chunk_embeddings` table (Decision D; see BL-04-BE-7).** Derive from `AggregateRoot`, mirror `Concept.cs`/`Skill.cs`. | `Domain/Entities/CurriculumChunk.cs` | BL-04-BE-1 (if option A) | 3 | 🔲 |
| BL-04-BE-4 | EF config `CurriculumChunkConfig.cs`: `ToTable("CurriculumChunks", schema)`, `ConceptId` indexed (FK with nav if option B; plain `HasIndex` if option A — no EF nav to cross-module entity), `ProvenanceRef` mapped as a required string. Index on `ConceptId`. Add `DbSet<CurriculumChunk>` to chosen DbContext. Add a code comment: "Embeddings are in the chunk_embeddings table (Decision D). See curriculum-system-of-record.md §4." | `Persistence/Configurations/CurriculumChunkConfig.cs`, chosen DbContext | BL-04-BE-2, BL-04-BE-3 | 2 | 🔲 |
| BL-04-BE-5 | Npgsql EF migration `AddCurriculumChunkTable`: enable `vector` extension first, then create `CurriculumChunks` table in chosen schema (`curriculum` or `learning`). Verify migration applies cleanly on a fresh PostgreSQL instance (no cross-module FK violations). Note: the vector extension is enabled here for `chunk_embeddings` (BE-7) — do it once. | `Migrations/AddCurriculumChunkTable.cs` | BL-04-BE-4 | 2 | 🔲 |
| BL-04-BE-6 | **Revised persistence smoke test:** write a `CurriculumChunk` (with `ProvenanceRef`) and read it back; assert round-trip equality. Write a `ChunkEmbedding` row (see BE-7) for that chunk with `Model="bge-m3"`, `Version="1.0"`, `Dimension=1024`, and a real `Vector(1024)` value; read it back and assert round-trip equality. Confirm `vector` extension is present in the DB. Mirror the existing integration-test harness in `Learnexia.IntegrationTests`. | `IntegrationTests/BL-04_CurriculumSchema_Tests.cs` | BL-04-BE-5, BL-04-BE-7 | 3 | 🔲 |
| BL-04-BE-7 | **`chunk_embeddings` entity + config + migration (Decision D — separate embedding table):** Add `ChunkEmbedding` domain entity: `Id` (bigint PK), `ChunkId` (int FK → `CurriculumChunk`, ON DELETE CASCADE), `Model` (string varchar 64 — e.g. `"bge-m3"`), `Version` (string varchar 32), `Dimension` (int), `Vector` (`Vector(1024)` pgvector type — dimension 1024 for BGE-M3/Cohere-embed-multilingual-v3; a different-dimension model requires a separate table), `CreatedAt` (DateTimeOffset). EF config `ChunkEmbeddingConfig.cs`: `ToTable("chunk_embeddings", schema)`, FK to `CurriculumChunk` with cascade, composite index on `(ChunkId, Model, Version)`. Add code comment: "Default Dimension=1024 for BGE-M3 (multilingual, open-weight) and Cohere embed-multilingual-v3. A model with a different output dimension requires a new table — see curriculum-system-of-record.md §4. HNSW/IVFFlat ANN index deferred to BL-05/P3-07." Add `DbSet<ChunkEmbedding>` to chosen DbContext. Npgsql migration `AddChunkEmbeddingsTable`. | `Domain/Entities/ChunkEmbedding.cs`, `Persistence/Configurations/ChunkEmbeddingConfig.cs`, chosen DbContext, `Migrations/AddChunkEmbeddingsTable.cs` | BL-04-BE-4 | 4 | 🔲 |
| BL-04-BE-8 | **Provenance tree entities + configs + migration (Decision B):** Add `ContentSource` entity: `Id`, `Type` (enum {Book, Image, Worksheet, VideoTranscript}), `Title` (string), `Publisher` (string, nullable), `Edition` (string, nullable), `Language` (`ContentLanguage` enum — reuse existing), `GradeId` (int, plain indexed — cross-module ref if option A). Add `Chapter` entity: `Id`, `ContentSourceId` (FK → `ContentSource`), `Number` (int), `Title` (string), `PageStart` (int nullable), `PageEnd` (int nullable). Add `ProvenanceMapping` entity: `Id`, `ChunkId` (FK → `CurriculumChunk`), `ContentSourceId` (FK → `ContentSource`), `ChapterId` (int FK → `Chapter`, nullable), `PageOrBlockRef` (string), `PedagogicalNodeId` (int — plain int, cross-module if option A), `PedagogicalNodeType` (string — e.g. `"Lesson"`, `"Concept"`). EF configs for all three. Migration `AddProvenanceTree`. Indexes: FK indexes on `ChapterId`, `ContentSourceId`; composite index on `(ChunkId, ContentSourceId)` on `ProvenanceMapping`. | `Domain/Entities/ContentSource.cs`, `Chapter.cs`, `ProvenanceMapping.cs`, `Domain/Enums/ContentSourceType.cs`, EF configs, migration | BL-04-BE-4 | 5 | 🔲 |
| BL-04-BE-9 | **`CurriculumVersion` entity + config + migration (Decision C — immutable-publish versioning):** Add `CurriculumVersion` entity: `Id`, `SubjectCode` (string), `Language` (`ContentLanguage` enum), `Status` (enum {Draft, Active, Archived}), `CreatedAt`, `PublishedAt` (nullable DateTimeOffset), `PublishedByUserId` (int, nullable), `ArchivedAt` (nullable DateTimeOffset), `Notes` (string, nullable). EF config `CurriculumVersionConfig.cs`: `ToTable("CurriculumVersions", schema)`, filtered unique index on `(SubjectCode, Language)` WHERE `Status = 'Active'` (enforces exactly one Active version per tree). Migration `AddCurriculumVersion`. Add code comment linking to `docs/briefs/curriculum-system-of-record.md` §3 and referencing P7-05 (the admin publish/version surface — that story's publish action switches Status; this entity is the same one P7-05 will use). | `Domain/Entities/CurriculumVersion.cs`, `Domain/Enums/CurriculumVersionStatus.cs`, `Persistence/Configurations/CurriculumVersionConfig.cs`, migration | BL-04-BE-1 (if option A) | 3 | 🔲 |
| BL-04-BE-10 | **`KGSuggestion` entity + config + migration (Decision E — KG suggestion queue):** Add `KGSuggestion` entity: `Id`, `SourceNodeId` (int — FK or plain int depending on module placement — see notes), `TargetNodeId` (int — same), `RelationshipType` (`EdgeRelationshipType` enum — reuse from `learning`), `Strength` (decimal(5,4)), `SourceCurriculumDocumentId` (int FK → `CurriculumDocument`, nullable), `InferenceModel` (string — e.g. `"lightrag-v1"`), `Status` (enum {Pending, Approved, Rejected}), `ReviewedAt` (nullable DateTimeOffset), `ReviewedByUserId` (int, nullable), `CreatedAt`. EF config `KGSuggestionConfig.cs`: indexes on `Status` (queue scans), on `(SourceNodeId, TargetNodeId, RelationshipType)` (idempotency). Migration `AddKGSuggestionTable`. Note: if option A, `SourceNodeId`/`TargetNodeId` are plain int columns (no EF nav to cross-module `KnowledgeNode`); if option B, real FKs. Add comment: "KGSuggestion rows are auto-inferred proposals — NEVER auto-published to KnowledgeEdge. Admin approval required. See curriculum-system-of-record.md §5." | `Domain/Entities/KGSuggestion.cs`, `Domain/Enums/KGSuggestionStatus.cs`, `Persistence/Configurations/KGSuggestionConfig.cs`, migration | BL-04-BE-1 (if option A) | 4 | 🔲 |
| BL-04-BE-11 | **`SkillKey` column on `KnowledgeNode` (learning module migration — Decision C):** Add a Npgsql migration in the `learning` module that adds `SkillKey varchar(200) NULL` to `KnowledgeNodes`. Add a unique partial index `WHERE "SkillKey" IS NOT NULL` (name: `UX_KnowledgeNodes_SkillKey`). Extend `KnowledgeNodeConfig.cs` to map the new column. Format: `{SubjectCode}.grade{N}.{unitSlug}.{skillSlug}` (document in the config comment). Column starts nullable to allow backfill of existing P2-11 seed rows (see BE-12/BE-13). | `learning` migration `AddSkillKeyToKnowledgeNode`, `KnowledgeNodeConfig.cs` (extend) | BL-04-BE-1 complete (schema must exist for this migration to be safe); P2-11 complete | 3 | 🔲 |
| BL-04-BE-12 | Extend `KnowledgeNode` entity class (`learning` module) to add the `SkillKey` (string nullable) property. Extend the `KnowledgeNode` domain so it can be set at creation (via constructor) and updated (no arbitrary mutation — setting a null key to a value is allowed; once set, treat as immutable unless an explicit correction command is used). | `Domain/Entities/KnowledgeNode.cs` (extend) | BL-04-BE-11 | 2 | 🔲 |
| BL-04-BE-13 | **SkillKey backfill data migration (conditional — confirm Q3 scope):** write a Npgsql SQL data migration that populates `SkillKey` on the existing P2-11 hand-seeded `KnowledgeNode` rows, using the format `{SubjectCode}.grade{GradeLevel}.{unitSlug}.{SkillSlug}` derived from the node's `Name` + `SubjectId` + `GradeId` (slugify: lowercase, replace spaces with hyphens, strip non-alphanumeric). After the data migration, add a NOT NULL constraint to `SkillKey` (requires all rows to be populated first). Include a rollback: remove the NOT NULL constraint on down. | `learning` migration `BackfillSkillKeys` | BL-04-BE-11, BL-04-BE-12 | 3 | 🔲 |

## Acceptance-criteria coverage

- AC 1 — `CurriculumChunk` entity with SRS §6 fields + `ProvenanceRef` (no inline vector) → **BE-3, BE-4**
- AC 2 — `chunk_embeddings` table with `(ChunkId, Model, Version, Dimension, Vector vector(1024))` + pgvector round-trip → **BE-7, BE-6**
- AC 3 — Provenance tree (`ContentSource`, `Chapter`, `ProvenanceMapping`) defined → **BE-8**
- AC 4 — `CurriculumVersion` entity with {Draft, Active, Archived} status + filtered unique index → **BE-9**
- AC 5 — `KGSuggestion` entity (suggestion queue, separate from `KnowledgeEdge`) → **BE-10**
- AC 6 — `SkillKey` column on `KnowledgeNode` (learning module, nullable with unique partial index) → **BE-11, BE-12, BE-13**
- AC 7 — `KnowledgeNode`/`KnowledgeEdge` reuse (P2-11, no rebuild, curriculum is logical owner only) → **Planner/reviewer note; no task**
- AC 8 — Schema matches SRS §6 (FKs, metadata, difficulty); all migrations apply cleanly on PostgreSQL → **BE-5, BE-6 revised**

## Notes

- **KnowledgeNode/KnowledgeEdge:** already built in `learning` (P2-11, audited 2026-06-07). BL-04 adds only the `SkillKey` column (BE-11/BE-12/BE-13). The `curriculum` module is their LOGICAL OWNER (authors/validates the graph) but NOT their physical home. Cross-module access from `curriculum` goes through `Shared.Contracts` integration events / the Learning command seam — never a direct DbContext reference or EF navigation. See `docs/briefs/curriculum-system-of-record.md` §2.
- **BE-1 is conditional** on the lead's new-module decision (CLAUDE.md standing rule: never scaffold a new module without explicit sign-off). If option B (fold into `learning`), skip BE-1 entirely and reduce total estimate by 6 h.
- **NO inline vector column on `CurriculumChunk` (Decision D).** Embeddings live in the separate `chunk_embeddings` table (BE-7). This enables dual-model coexistence during embedding-model migrations. The `EmbeddingVectorRef` field from the original brief is replaced. Any implementation that adds an inline vector to `CurriculumChunk` is incorrect.
- **Embedding dimension: FIXED at 1024 for `chunk_embeddings.Vector` (BGE-M3 multilingual / Cohere embed-multilingual-v3).** A model with a different output dimension requires a separate table — the `chunk_embeddings` table cannot accommodate a variable dimension in a single column. See `docs/briefs/curriculum-system-of-record.md` §4.
- **ANN index** (HNSW/IVFFlat on `chunk_embeddings.Vector`) deliberately deferred to BL-05/P3-07 when query patterns and chunk volume are known.
- **Provenance tree vs pedagogical tree:** `ContentSource`/`Chapter`/`ProvenanceMapping` are the provenance layer (Decision B). A chapter is NOT a unit. The pedagogical tree (`Grade → Subject → Unit → Lesson → Concept → Skill`) lives in `learning`. See `docs/briefs/curriculum-system-of-record.md` §1.
- **`CurriculumVersion` + P7-05:** the `CurriculumVersion` entity is the same entity P7-05's admin surface will manage. BL-04 models it; P7-05 adds the UI and the publish action that switches Status. Confirm with lead that the Status enum values are aligned (`Active` = `Published` in P7-05 language).
- **`KGSuggestion` is NOT `KnowledgeEdge`.** Suggested edges NEVER auto-flow into `KnowledgeEdge`. Admin approval (BL-03) is the only path from Suggestion to published edge. See `docs/briefs/curriculum-system-of-record.md` §5.
- **`Directory.Packages.props`** is a shared file — BE-2 must not run in parallel with any other story that edits it.
- **`Program.cs` / `.sln`** are shared files — if option A, BE-1 edits must be serialized (PARALLELISM.md).
- **SkillKey format:** `{SubjectCode}.grade{N}.{unitSlug}.{skillSlug}` (e.g. `math.grade4.fractions.add-unlike-denominators`). Slugify = lowercase, spaces→hyphens, strip non-alphanumeric except hyphens. Document in code comments.
- No api-tester stage for this enabler (no HTTP endpoints). Persistence smoke test (BE-6 revised) substitutes.
- No designer stage (backend/infra only, no UI surface).
