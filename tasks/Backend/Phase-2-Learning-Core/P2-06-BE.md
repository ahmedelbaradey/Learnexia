# P2-06 (Backend) — Take a quiz (4 question types)

> Story: [../../../user-stories/Phase-2-Learning-Core/P2-06-take-a-quiz.md](../../../user-stories/Phase-2-Learning-Core/P2-06-take-a-quiz.md)
> Phase 2 — Learning Core · Epic: Assessment Module · SP: 5 · FR-QZ-1 · Est. unit: hours

> **Status: ✅ Done (with design deviation)** — quiz entities (`QuizQuestion`/`Attempt`/`StudentAnswer`), 4 `QuestionType`s + per-type validation/comparison, and `StartAttemptCommand` are implemented. **BE-1 deviation:** no separate `assessment` module was scaffolded — quiz lives inside the **Learning** module (schema `learning`) per the "ask-before-new-modules" decision. **Caveat:** Matching answer-shape comparison is a TODO and no Matching questions are seeded yet. _(Audited 2026-06-07.)_

## Backend tasks
| ID | Task | Artifact | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| P2-06-BE-1 | Scaffold **`assessment`** module + register `AddAssessmentModule` | `Learnexia.Modules.Assessment.*` | P1-06-BE-* | 5 | ✅* |
| P2-06-BE-2 | Entities: `QuizQuestion` (type, text, options, correct answer, difficulty, generatedBy), `Attempt`, `StudentAnswer` + migration (schema `assessment`) | `Domain/Entities/*`, `AssessmentDbContext` | P2-06-BE-1 | 5 | ✅ |
| P2-06-BE-3 | Support 4 question types: MCQ, True/False, Matching, Fill-in-the-blank (model + validation per type) | type discriminator + validators | P2-06-BE-2 | 5 | 🟡 |
| P2-06-BE-4 | Start-quiz endpoint creates an Attempt (`POST /api/Assessment/Quizzes/{id}/Attempt`) | `StartAttemptCommand(Handler)` | P2-06-BE-2 | 3 | ✅ |

> **\*** BE-1/BE-2 implemented under the **Learning** module (schema `learning`), not a separate `assessment` module/schema — deliberate per the module-isolation decision. BE-3 is 🟡: MCQ/TrueFalse/FillInBlank fully modeled + compared; Matching's answer-shape comparison is a TODO and unseeded.

## Acceptance-criteria coverage
- 4 question types supported → **BE-3**
- Start quiz creates Attempt → **BE-4**
- Entities persist the session → **BE-2**
- Per-type rendering → (other stack — P2-06-FE)

## Contract for Frontend (P2-06-FE)
- Question payload includes `type` + type-specific `options`; start-attempt returns `attemptId`.

## Notes
- Covers B3.1. Instant feedback = P2-07; adaptive selection = P3-11; AI generation = P3-06.
