# P2-11 (Backend) — Author the skill dependency graph (relational, hand-authored)

> Story: [../../../user-stories/Phase-2-Learning-Core/P2-11-author-skill-dependency-graph.md](../../../user-stories/Phase-2-Learning-Core/P2-11-author-skill-dependency-graph.md)
> Phase 2 — Learning Core · Epic: Curriculum & Learning Core · SP: 5 · FR-AD-1, FR-QZ-3, FR-CI-3 (MVP slice) · Est. unit: hours
> **Backend/infra only — no Frontend tasks.** Barrier-to-entry BE1 launch bridge (full OCR pipeline stays in `BL-01..05`).

> **Status: ✅ Done** — `KnowledgeNode`/`KnowledgeEdge` entities + EF configs + `AddSkillGraphTables` migration, `SkillGraphValidator` acyclic check + tests, seeded prerequisite edges, prerequisites/unlocked-by query API, and `LearningPathEngine` reads edges from the graph. _(Audited 2026-06-07.)_

## Backend tasks
| ID | Task | Artifact | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| P2-11-BE-1 | Add relational `KnowledgeNode` + `KnowledgeEdge` entities to the **`learning`** module per SRS §6 (node = skill/concept; edge = prerequisite/related + `Strength`); forward-compatible superset of the future `BL-04` schema | `Domain/Entities/KnowledgeNode.cs`, `KnowledgeEdge.cs` | P2-01-BE-2 | 4 | ✅ |
| P2-11-BE-2 | EF configurations + Npgsql migration (self-referential edge FKs source/target, indexes on `(SourceNodeId)`/`(TargetNodeId)`, schema `learning`) | `Persistence/Configurations/*`, migration | P2-11-BE-1, P2-01-BE-4 | 4 | ✅ |
| P2-11-BE-3 | **Acyclic validation**: reject cycles at author/seed time (graph DFS) with a clear domain error; unit-test the cycle check | `Domain/Services/SkillGraphValidator.cs` + tests | P2-11-BE-1 | 5 | ✅ |
| P2-11-BE-4 | Seed/author path for MVP subjects & grades (extends P2-10 seed) — hand-authored prerequisite edges; **no OCR / Azure DI dependency** | seed data, `LearningSeeder` extension | P2-11-BE-2, P2-10-BE-* | 5 | ✅ |
| P2-11-BE-5 | Query API: `GetPrerequisitesOf(skillId)` and `GetUnlockedBy(skillId)` queries (CQRS), consumed by P2-04 unlock rules | `Application/Queries/*`, endpoints | P2-11-BE-2 | 4 | ✅ |
| P2-11-BE-6 | Wire P2-04 unlock rules + P3-08/P3-10 prerequisite-review to read edges from this graph (not ad-hoc ordering) | integration in `P2-04` engine | P2-11-BE-5, P2-04-BE-* | 3 | ✅ |

## Acceptance-criteria coverage
- Prerequisite edges within/across lessons, relational shape matching SRS §6 → **BE-1, BE-2**
- Hand-authored / seedable, no OCR → **BE-4**
- Validated acyclic, cycles rejected with clear error → **BE-3**
- P2-04 / P3-08 / P3-10 read prerequisites from the graph → **BE-5, BE-6**
- Queryable "prerequisites of X" / "unlocked by X" → **BE-5**

## Notes
- Blocked by P2-01 (learning module + entities) and P2-10 (seed). Forward-compatible with `BL-03`/`BL-04`.
- **Strategic decision:** full Curriculum Intelligence (BE1) stays post-MVP; this is the launch bridge. Record the deferral in `user-stories/README.md`.
