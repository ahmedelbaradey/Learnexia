# P9-11 (Backend) — Notification analytics sink

> Story: [../../../user-stories/Phase-9-Notifications/P9-11-notification-analytics-sink.md](../../../user-stories/Phase-9-Notifications/P9-11-notification-analytics-sink.md)
> Phase 9 — Notifications (post-MVP) · Epic: Notifications Module · SP: 3 · FR-GM-8; builds on P5-03 · Est. unit: hours
> Stack: .NET 10 — `notifications` module **emits** `Shared.Contracts` lifecycle events; the `analytics` module **consumes** into its `ActivityEvent` stream (cross-module via Shared.Contracts; module isolation rule 1).
> **Status: ✅ Built + gated 2026-06-20 (`feat/P9-11-notification-analytics-sink`).** 3 lifecycle events → Analytics `ActivityEvent` sink + admin aggregate endpoint. Suppression v1 = dispatcher push-reasons only (pre-dispatch deferred); Opened = inbox-read only (push-tap → P9-02 FE). Gates: build 0 · api 15/15 · security PASS · reviewer PASS. **Last Phase-9 backend story — Phase-9 BE complete.**

## Backend tasks
| ID | Task | Artifact | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| P9-11-BE-1 | Define notification lifecycle integration events (Dispatched / Suppressed / Opened) — opaque ids + `Code`/`Category`/`Reason`/`Channels` scalars, NO PII | `Shared.Contracts/Notifications/Notification*IntegrationEvent.cs` | — | 2 | 🔲 |
| P9-11-BE-2 | Emit **Dispatched** + per-channel result and **Suppressed** (with P9-07 reason) from the single choke point | extend `NudgeDispatcher` (fail-soft, off hot path) | P9-07, P9-11-BE-1 | 3 | 🔲 |
| P9-11-BE-3 | Emit **Opened** on inbox read | extend `InboxController` MarkRead / read handler | P9-11-BE-1 | 2 | 🔲 |
| P9-11-BE-4 | Analytics consumers map the 3 events → `ActivityEvent` append-only sink (fail-soft, idempotent) | `analytics` module `INotificationHandler`s + `AddCrossModuleMediatR` registration | P5-03, P9-11-BE-1 | 3 | 🔲 |
| P9-11-BE-5 | Admin-readable aggregate: send/suppress/open counts + open-rate **by code & category over a range** | extend `IPlatformAnalyticsQuery` + admin endpoint | P9-11-BE-4 | 3 | 🔲 |
| P9-11-BE-6 | Confirm `notifications` Application assembly is included in the analytics `AddCrossModuleMediatR` set (consumers fire) + integration test the relay end-to-end | DI wiring + api-tester | P9-11-BE-4 | 2 | 🔲 |

## Acceptance-criteria coverage
- Dispatched + per-channel result, fail-soft, off hot path → **BE-2**
- Suppressed with reason (P9-07) → **BE-2**
- Opened (inbox read now; push-tap later via P9-02) → **BE-3**
- Analytics module is the single sink via Shared.Contracts (no cross-module FK) → **BE-1, BE-4**
- Admin aggregate by code/category over a range (feeds P7-10/P7-11) → **BE-5**
- Never blocks/fails a dispatch → **BE-2** (fire-and-forget / fail-soft)

## Contract for Frontend (P9-11-FE — admin-dashboard)
- `GET /api/Admin/Analytics/notifications?from=&to=&category=` → `BaseResponse<NotificationAnalyticsDto>` (per-code/category send/suppress/open + open-rate). Admin-gated; read `Successed`.

## Notes
- **Sink home = Analytics module** (lead decision 2026-06-20) — reuse the P5-03 `ActivityEvent` stream, not a second metrics table in `notifications`. Mirror the fire-and-forget recorder shape from `AiUsageRecorder` (P7-11b) so capture never throws into the dispatch path. **Push-tap "Opened" is deferred** to when the P9-02 FE deep-link handler reports taps. **analyzer + planner first.**
