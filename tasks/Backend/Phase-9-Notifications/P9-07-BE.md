# P9-07 (Backend) — Many types, few sends (arbitration + global daily budget)

> Story: [../../../user-stories/Phase-9-Notifications/P9-07-nudge-arbitration-and-daily-budget.md](../../../user-stories/Phase-9-Notifications/P9-07-nudge-arbitration-and-daily-budget.md)
> Phase 9 — Notifications (post-MVP) · Epic: Notifications Module · SP: 5 · FR-GM-8 · Est. unit: hours
> Stack: .NET 10 `notifications` module — extends `Domain/Services/ReengagementEvaluator` (pure) + dispatcher path. Redis for budget/cooldown counters.
> **Status: ✅ BUILT + MERGED 2026-06-20 (PR #192, `feat/P9-nudge-arbitration`).** Global daily push budget + per-type cooldown arbiter at `NudgeDispatcher` (the single choke point for all 11 handlers); in-app always written, only push rationed; budget resolved per-child (`ChildReengagementPreference.GlobalDailyPushBudget` ?? config 4); cooldown via Redis SETNX (fail-open). Tested in `P9_07_NudgeArbitration_Tests` (TC-05..07, 12, 13). *(Prior "Not started" marker was stale planning-time.)*

## Backend tasks
| ID | Task | Artifact | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| P9-07-BE-1 | **Global per-child daily push budget** (parent-configurable, conservative default) across all categories, in addition to per-category caps | extend prefs + evaluator/arbiter | P4-09 (evaluator) | 4 | 🔲 |
| P9-07-BE-2 | **Priority ordering** when multiple nudges eligible in a window; config-driven, tunable without deploy | arbitration service over evaluator | P9-07-BE-1 | 5 | 🔲 |
| P9-07-BE-3 | **Per-type cooldowns** (e.g. league ≤1/day, recap ≤1/week) via Redis counters | cooldown check | P1-06 (Redis) | 3 | 🔲 |
| P9-07-BE-4 | In-app inbox row **always written** even when push loses arbitration / budget; only push is rationed | dispatcher guarantee | P4-09 (dispatcher) | 2 | 🔲 |
| P9-07-BE-5 | Suppression-reason logging (priority-lost / budget-exhausted / cooldown) + per-type send/open + suppression analytics (P5-03 seam) | structured logging + analytics emit | P5-03 seam | 3 | 🔲 |

## Acceptance-criteria coverage
- Global daily push budget + per-category caps → **BE-1**
- Config-driven priority ordering → **BE-2**
- Per-type cooldowns → **BE-3**
- Quiet hours + Redis dedupe still apply; inbox unrationed → **BE-4** (+ existing evaluator)
- Suppression reason logged + item still in inbox → **BE-4, BE-5**
- Per-type send/open + suppression analytics → **BE-5**

## Notes
- **Extend** the existing pure `ReengagementEvaluator` + `NudgeDispatcher` — do not fork. Likely a thin arbitration service that runs after per-category evaluation.
- This is the anti-spam guardrail for the whole phase. **analyzer + planner first.**
