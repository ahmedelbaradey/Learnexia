# P6-06 (backend) — Backend security hardening (stabilization)

> Story: [../../../user-stories/Phase-6-Stabilization/P6-06-backend-security-hardening.md](../../../user-stories/Phase-6-Stabilization/P6-06-backend-security-hardening.md)
> Phase 6 — Stabilization · Epic: Stabilization & Hardening · SP: 8 · Module: **Identity + Notifications + Host** (parallel-safe) · mirror existing shapes.
> Source: the per-PR security briefs — `docs/briefs/P1-12-password-reset-security-audit.md`, `P1-13a-security-audit.md`, `P1-13-security-audit.md`, `P1-13-captcha-security-audit.md`. **Relocated from P1-13b** (its BE-1 rate-limiting shipped in Phase-1, PR #50).

## Tasks
| ID | Task | Artifact | Deps | Est (h) |
|---|---|---|---|---|
| P6-06-BE-1 | **Close the forgot-password timing oracle**: dispatch the reset email **out-of-band** so the request doesn't await provider I/O (background path / fire-and-forget with isolated logging), making registered vs unknown emails latency-indistinguishable. Keep the generic 200 + anti-enumeration intact. | `ForgotPasswordCommandHandler` / event dispatch | P1-12-BE-6 | 4 |
| P6-06-BE-2 | **Localize transactional emails**: carry the recipient **locale** on the welcome (`UserRegisteredIntegrationEvent`) + reset (`PasswordResetRequestedIntegrationEvent`) events (or resolve via `IUserLookup`); render subject/body via the localizer (ar/en) in the Notifications consumers. | `Shared.Contracts` events + Notifications handlers + resx | P1-13a, P1-12-BE-6 | 5 |
| P6-06-BE-3 | **Transport/secret hygiene**: gate `RequireHttpsMetadata=false` to Development only; read the DB connection password from env in non-Development; document the required prod env vars in HANDOFF. | Identity Infrastructure JWT bearer config + `appsettings`/env + HANDOFF | — | 3 |
| P6-06-BE-4 | **Multi-instance rate-limit store**: promote the auth rate-limiting (shipped in-memory in P1-13b BE-1) to a **Redis-backed** `IRateLimitCounterStore`/`IIpPolicyStore` (add `AspNetCoreRateLimit.Redis` + a Redis connection) so limits hold across instances; keep the in-memory fallback for dev where Redis is absent. | `ServiceExtensions` rate-limit store + DI + `Directory.Packages.props` | P1-13b-BE-1 | 4 |
| P6-06-BE-5 | **security-auditor** pass for the timing-oracle, email-locale, transport/secret, and rate-limit-store changes | security-auditor | BE-1..BE-4 | — |

## Acceptance-criteria coverage
- Forgot-password latency indistinguishable (oracle closed), anti-enumeration intact → **BE-1**
- Welcome + reset emails localized (ar/en) → **BE-2**
- HTTPS metadata Dev-gated; DB secret from env; prod env vars documented → **BE-3**
- Auth rate-limit counters multi-instance-safe (Redis-backed) → **BE-4**
- Security review of the above → **BE-5**

## Open decisions (for the lead)
- **BE-2:** email-locale source — add a `Locale` field to the integration events (simplest) **vs** enrich via `IUserLookup`.
- **BE-4:** confirm a Redis connection is provisioned in staging/prod (dev keeps the in-memory store; HANDOFF notes Redis is optional in dev).

## Notes
- **Identity + Notifications + Host scoped**, parallel-safe; cross-module only via `Shared.Contracts` (BE-2 may add a `Locale` field to the existing reset/welcome events). No cross-module FK. No Unit of Work.
- **New package** only for BE-4 (`AspNetCoreRateLimit.Redis`, centrally versioned) — otherwise no new packages; BE-2 reuses the existing localizer; BE-1/BE-3 are config/wiring.
- **Already done (Phase-1, not here):** P1-13b BE-1 auth rate-limiting (PR #50); P1-13 lockout/sign-in-safety/admin-seed/CAPTCHA; P1-13a email; P1-12 BE-1..BE-9.
- Operational items (set prod env secrets, enable CAPTCHA, FE/legal Google consent) are tracked in HANDOFF, not code tasks here.
