# P10-17 (Backend) — Refund reconciliation (unused purchased energy)

> Story: [../../../user-stories/Phase-10-Payments-Billing/P10-17-refund-reconciliation-unused-purchased-energy.md](../../../user-stories/Phase-10-Payments-Billing/P10-17-refund-reconciliation-unused-purchased-energy.md)
> Phase 10 — Payment, Billing & Credits · Epic: Payment, Billing & Credits · SP: 5 · FR-PAY-5 · Est. unit: hours
> **Security-sensitive (money + child data) — mandatory `security-auditor` gate.** Refines the existing P10-09 `RefundService` / refund webhook branch for the LOCKED two-bucket family-energy model (P10-13). Billing module only.

> **Status: 🟡 Scope-confirmed (lead 2026-06-18), build starting** — OQ-1/OQ-4 resolved (refund = monetary value of UNUSED purchased energy at original price, partial, per-original-`Purchase`-payment FIFO); OQ-2 resolved = **PARENT + ADMIN** (added **BE-6** admin-refund action). Refunds NEVER touch bucket A (subscription/granted); settlement is webhook-driven.

## Architecture guardrails (read before coding)
- **Module isolation:** all work lives in the **Billing** module (`FamilyEnergyAccount`, buckets, ledger, packs, payments, refunds are Billing-owned per the locked model). No cross-module FK. Any cross-module need goes through `Shared.Contracts` only. Identity owns child profiles — do **not** reach into it.
- **Option C — service-only (CONVENTIONS §7):** the Application layer is **EF-free and depends on service interfaces ONLY**. Handlers inject **service interfaces** from `Billing.Application/Abstractions` — never a `DbContext`, `DbSet`, `IQueryable`, repository, or `Microsoft.EntityFrameworkCore`. Chain is **Handler → Service → Repository → EF**. Reconciliation logic + explicit transactions live in the **service impl in Infrastructure**.
- **No UoW (ADR 0001):** refund reconciliation (read ledger → compute refundable → append `Refund` row → decrement shared purchased balance) is a multi-write atomic operation → wrap in an **explicit transaction inside the service**, never in a handler.
- **No free-text string literals** — refund reasons / statuses / error keys are **enums or localized keys**, not hardcoded strings.
- **Refines, does not duplicate, P10-09:** extend the existing `RefundService` and the `refund.succeeded` webhook branch rather than adding a parallel refund path.

## Backend tasks

| ID | Task | Artifact | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| P10-17-BE-1 | Define the **refundable-balance reconciliation contract** in `Billing.Application/Abstractions`: extend (or add to) `IRefundService` with `ComputeRefundableAsync(familyAccountId, purchasePaymentId)` returning a `RefundableQuoteDto { purchasedTotal, consumedPurchased, refundable, currency }`. Refundable = `purchasedTotal − consumedPurchased`, **clamped ≥ 0**, computed by **reconciling the immutable family ledger** (sum of `Purchase` rows for the pack/account minus `Spend` rows that drew from the **shared family purchased row** — bucket B only; bucket A subscription/entitlement rows are **never** included). Service interface only — no EF here | `Billing.Application/Abstractions/IRefundService.cs` (extend); `Billing.Application/Features/Refunds/Dtos/RefundableQuoteDto.cs` | P10-13-BE (two-bucket ledger + `FamilyEnergyAccount` + shared purchased row + the bucket-B `Purchase` rows from the **P10-13-BE-12 pack-credit re-home**), P10-09-BE-3 (`RefundService` exists) | 4 | 🔲 |
| P10-17-BE-2 | Implement the reconciliation in the **`RefundService` impl (Infrastructure)**: query the immutable ledger via the Billing repository (materialized results only — no `IQueryable` across the boundary), sum purchased-bucket `Purchase` vs purchased-bucket `Spend` (spend rows that hit the **shared family purchased row** fallback, NOT per-child allocation/subscription rows), clamp ≥ 0 and **never exceed the current shared purchased balance**. Pure, idempotent read; no mutation. Unit-test the math (bought 10000 / used 3000 → 7000; fully spent → 0; subscription rows excluded) | `Billing.Infrastructure/Services/RefundService.cs` (extend); `Billing.Infrastructure/Repositories/...` (ledger read) | P10-17-BE-1 | 5 | 🔲 |
| P10-17-BE-3 | `RequestPurchasedEnergyRefundCommand(parentUserId, purchasePaymentId, reason)` — **parent-only JWT** (child JWTs rejected) + **owning-parent / family-scope authz** (parent owns the `FamilyEnergyAccount` behind the payment); FluentValidation validator (reason is a **`RefundReason` enum**, not free text). Handler calls `IRefundService.ComputeRefundableAsync` (rejects with a localized error key if refundable ≤ 0), then `IRefundService.InitiateProviderRefundAsync(...)` → `IPaymentProvider.Refund(paymentRef, refundableAmount)`. **The ledger/balance change is driven by the subsequent `refund.succeeded` webhook, NOT by this command** (consistent with P10-09-BE-5). Returns `BaseResponse<RefundRequestResultDto>` (`Successed`) | `Billing.Application/Features/Refunds/Commands/RequestPurchasedEnergyRefund/` (command + handler + validator); `Dtos/RefundRequestResultDto.cs` | P10-17-BE-1, P10-09-BE-5 (`IPaymentProvider`/refund seam) | 4 | 🔲 |
| P10-17-BE-4 | **Refine the `refund.succeeded` webhook branch (P10-09-BE-3) for the two-bucket model:** for a **purchased-pack** refund, inside an **explicit transaction** in the service — recompute refundable from the ledger (re-reconcile; never trust the request-time figure), append an **idempotent `Refund` ledger row** linked to the original `Purchase` payment, and **decrement the SHARED FAMILY purchased balance** (bucket B) by the refunded amount, **clamped so the purchased balance never goes negative**. Subscription-bucket refunds remain on the existing P10-09 policy path (untouched here). Idempotent on provider refund event id (`WebhookEvent` dedup) **and** on `LedgerEntry.IdempotencyKey` (guard double-refund / double-decrement). Concurrency guard against negative-balance race | `Billing.Infrastructure/Services/RefundService.cs` (extend); `Billing.Infrastructure/Services/WebhookEventService.cs` (`HandleRefundSucceededAsync` Pack branch — route purchased-refund to the shared-balance reconciliation, NOT `HandleProviderWebhookCommandHandler` which only dispatches by event type) | P10-17-BE-2, P10-09-BE-3 (webhook handler + `WebhookEvent` dedup), P10-13-BE (shared purchased row + BE-12 pack re-home) | 5 | 🔲 |
| P10-17-BE-5 | Expose the **refundable quote** for the parent UI: `GetRefundableQuoteQuery(parentUserId, purchasePaymentId)` → `RefundableQuoteDto` (reuses BE-1/BE-2 reconciliation; owning-parent authz; query, so no `ValidationBehavior`). REST controller `RefundController` with `[Authorize(Roles="Parent")]` (parent JWT only): `GET /api/Billing/Refunds/Quote/{purchasePaymentId}`, `POST /api/Billing/Refunds/Request`. Family-scope authz enforced in the handler/command | `Billing.Application/Features/Refunds/Queries/GetRefundableQuote/`; `Billing.Api/Controllers/RefundController.cs` | P10-17-BE-2, P10-17-BE-3 | 3 | 🔲 |
| P10-17-BE-6 | **Admin-side refund action (OQ-2 = parent+admin, lead 2026-06-18).** `AdminRequestPurchasedEnergyRefundCommand(adminUserId, purchasePaymentId, reason)` + validator — **reuses** `IRefundService.ComputeRefundableAsync` + `InitiateProviderRefundAsync` (same reconciliation + the `refund.succeeded` webhook settlement from BE-4; do NOT duplicate logic). Difference vs BE-3: authz is **admin-role** (`[Authorize(Roles="Admin,SuperAdmin")]`), NOT family-scope — an admin/support agent can refund any family's purchased pack; the `parentUserId`/`familyAccountId` is resolved from the target `purchasePaymentId`, never from the admin's JWT. Ledger the admin actor id on the `Refund` row for audit. Add admin route(s) to `RefundController` (or an `AdminRefundController`): `GET /api/Billing/Admin/Refunds/Quote/{purchasePaymentId}`, `POST /api/Billing/Admin/Refunds/Request`. **Settlement is still webhook-driven (BE-4) — admin request only initiates.** | `Billing.Application/Features/Refunds/Commands/AdminRequestPurchasedEnergyRefund/` (command + handler + validator); `Billing.Api/Controllers/RefundController.cs` (admin routes) or `AdminRefundController.cs` | P10-17-BE-2, P10-17-BE-3, P10-17-BE-4 | 3 | 🔲 |

## Acceptance-criteria coverage

- Refundable = purchased − consumed-purchased, reconciled from the immutable ledger (10000 − 3000 → 7000) → **BE-1** (contract) + **BE-2** (impl + math)
- Subscription/entitlement (bucket A) energy is **never** refundable → **BE-2** (bucket-B-only reconciliation excludes subscription/allocation rows)
- Already-consumed purchased energy can never be refunded; refundable clamped ≥ 0, never exceeds remaining purchased → **BE-2** (clamp) + **BE-4** (re-clamp at settlement, no negative balance)
- Successful refund = idempotent `Refund` ledger row linked to original `Purchase`, decrements shared family purchased balance → **BE-4**
- Reconciled against the immutable ledger as source of truth (not a mutable cached balance) → **BE-2** + **BE-4** (re-reconcile at settlement)
- Settled only via verified provider webhook (`refund.succeeded`); in-app request only initiates → **BE-3** (initiate-only) + **BE-4** (webhook-driven balance change)
- Parent-gated, owning-parent only; children can't request → **BE-3** + **BE-5** (parent-JWT-only `[Authorize(Roles="Parent")]` + family-scope authz)
- Admin/support can also initiate a refund (admin-role authz, any family, actor id ledgered) → **BE-6** (reuses the BE-2 reconciliation + BE-4 webhook settlement)
- Refundable balance + refund request exposed to parent UI → **BE-5** (`GET /Refunds/Quote`, `POST /Refunds/Request`)
- EN+AR error/reason keys → localized keys / `RefundReason` enum (no free text) → **BE-3** validator

## Notes

- **OQ-1 (LEAD GATE) — RESOLVED (lead 2026-06-18):** refundable unit = **energy reconciled to currency at the original purchase price** (refund the monetary value proportional to UNUSED purchased energy); **partial refunds (unused remainder only) are v1** — NOT all-or-nothing. Never refunds subscription/granted (bucket A). BE-1/BE-2 unblocked.
- **OQ-2 (refund trigger) — RESOLVED (lead 2026-06-18): PARENT + ADMIN.** Both an in-app **parent** request (BE-3/BE-5) AND an **admin-side** refund action (new **BE-6**, reuses the same `IRefundService.ComputeRefundableAsync`/`InitiateProviderRefundAsync` seam + the `refund.succeeded` webhook settlement) are in scope this story. Admin path = admin-role authz (not family-scope) + a `RefundController` admin route; same reconciliation + webhook-driven settlement.
- **OQ-3 (RESOLVED via P10-13):** purchased spend is identified by ledger rows that drew from the **shared family purchased row** (bucket B fallback), per the locked spend order (child allocation first → shared purchased fallback). The P10-13 ledger (`P10-13-BE-3`) tags each `Spend` row with `SourceBucket`, and the **P10-13-BE-12 pack-credit re-home** writes bucket-B `Purchase` rows against the shared reserve — so reconciliation is unambiguous. No further P10-13 flag needed.
- **OQ-4 (multi-pack accounting) — RESOLVED (lead 2026-06-18): per-original-`Purchase`-payment, FIFO consumption attribution.** Refundable for a given purchase payment = that payment's purchased amount minus its attributable consumed share (FIFO), capped at the current shared purchased balance. Partial allowed. (Confirms the earlier recommendation.)
- **Supersedes P10-09 clawback semantics:** P10-09 clawed back unused credits from a single per-child `PurchasedBalance`; here the purchased balance is a **shared family reserve** and reconciliation is ledger-driven. Refine the existing branch — do not add a parallel one.
- No UoW (ADR 0001) — reconciliation + ledger append + balance decrement run in an **explicit transaction inside the service** (Infrastructure), never a handler. Negative-balance race is the primary correctness risk.
- `ValidationBehavior` runs for `ICommand<>` only — `RequestPurchasedEnergyRefundCommand` gets a FluentValidation validator; the quote query does not.
- Blocked by **P10-13** (two-bucket family ledger + shared purchased row + the **P10-13-BE-12 pack-credit re-home** that lands purchased packs as the shared family reserve + bucket-B `Purchase` ledger rows) and **P10-09** (refund/webhook seam being refined — the `WebhookEventService.HandleRefundSucceededAsync` Pack branch). **Canonical mapping: 13 wallet · 14 seats · 15 lifecycle · 16 redistribution · 17 refunds (this story).**
