# P10-08 (Backend) — Billing history & receipts

> Story: [../../../user-stories/Phase-10-Payments-Billing/P10-08-billing-history-receipts.md](../../../user-stories/Phase-10-Payments-Billing/P10-08-billing-history-receipts.md)
> Phase 10 — Payment, Billing & Credits · Epic: Payment, Billing & Credits · SP: 3 · FR-PAY-4 · Est. unit: hours
> **Read-only reporting layer over P10-06/07/09 data. Parent-app surface only.**

> **Status: ✅ BUILT + MERGED.** Billing history + receipts (payment/transaction read API, parent-scoped); tested by `P10_01_12_Billing_IntegrationTests`. *(Prior "Not started" marker was stale planning-time.)*

## Backend tasks

| ID | Task | Artifact | Deps | Est (h) | Status |
|---|---|---|---|---|---|
| P10-08-BE-1 | (Conditional) migration: if finance confirms sequential invoice numbering or a persisted `Invoice` table is required, add `InvoiceNumber` (sequence) and/or `Invoice` entity + EF config to `BillingDbContext` (schema `billing`). Also add `NetAmount`, `VatAmount`, `GrossAmount` to `Payment` if VAT fields were not captured at charge time in P10-06 — **strongly prefer capturing these in P10-06 so receipts are exact**. If both are already present from P10-06, this task is a no-op (zero migration needed) | `Domain/Entities/Invoice.cs` (if needed); `Persistence/Configurations/InvoiceConfig.cs`; migration `AddBillingReceiptFields` if needed | FINANCE GATE (OQ-1); P10-06-BE-2 (Payment exists) | 3 | 🔲 |
| P10-08-BE-2 | `GetBillingHistoryQuery(parentUserId, paging)` — returns chronological list of `Payment` + refund entries belonging to the parent (DTO: date, kind, amount, currency, status, targetChildId for packs, providerRef, linkedRefundId). Family-scope: filter strictly to `parentUserId` from JWT — no cross-account access. Paginated. `BaseResponse<T>`/`Successed`; `ILoggerManager` | `Application/Features/BillingHistory/Queries/GetBillingHistory/`; `BillingHistoryItemDto.cs` | P10-06-BE-2; P10-07-BE-1 (Pack payments exist); P10-09 (refund entries, coordinate timing) | 3 | 🔲 |
| P10-08-BE-3 | `GetReceiptQuery(parentUserId, paymentId)` — returns a receipt DTO with legally required fields (seller legal entity from config, amount, VAT/tax breakdown, date, provider reference). Defense-in-depth: re-check `Payment.ParentUserId == parentUserId` inside the handler before returning (prevents IDOR). **v1 recommendation: printable HTML receipt** (FE renders + browser print-to-PDF) — avoid introducing a PDF library without lead approval (CLAUDE.md rule 8). Seller/VAT fields from P10-11/billing-config, never hard-coded | `Application/Features/BillingHistory/Queries/GetReceipt/`; `ReceiptDto.cs` | P10-08-BE-1 (if VAT fields exist); P10-08-BE-2 | 3 | 🔲 |
| P10-08-BE-4 | REST controller `BillingHistoryController`: `GET /api/Billing/History` (paginated list) + `GET /api/Billing/History/Receipt/{paymentId}` (receipt). Both `[Authorize]` with parent-JWT; family-scope enforced at command/query level | `Api/Controllers/BillingHistoryController.cs` | P10-08-BE-3 | 1 | 🔲 |

## Acceptance-criteria coverage

- Chronological list of charges with date/amount/status/child for packs → **BE-2** (`GetBillingHistoryQuery`)
- Downloadable receipt with legally required fields (seller, amount, VAT, date, reference) → **BE-3** (`GetReceiptQuery`)
- Refunds appear as linked negative entries → **BE-2** (refund rows from P10-09)
- Owning-parent-only access (no cross-account/IDOR) → **BE-3** (re-check ownership) + **BE-4** (`[Authorize]`)
- EN+AR, empty state → frontend story (P10-08-FE)

## Notes

- **FINANCE GATE (OQ-1):** exact Egypt EGP/VAT required fields, sequential invoice numbering, VAT rate — must be confirmed before finalizing BE-3 receipt DTO. BE-1 is conditional on this answer.
- **OQ-2 (PDF vs printable HTML):** strongly recommend printable HTML for v1 (browser print-to-PDF) — no new library, no design-pattern decision. Confirm with lead. If true PDF is required, stop and ask before adding a PDF library (CLAUDE.md rule 8).
- **OQ-3 (VAT at charge time):** coordinate with P10-06 implementer to capture `NetAmount`, `VatAmount`, `GrossAmount` on `Payment` at charge time — do not let this fall to P10-08 reconstruction.
- **OQ-4 (child name in history DTO):** `targetChildId` is returned; FE maps it to the child name from the parent's already-loaded children list (P1-04/P5-05). No cross-module child-directory query needed in BE.
- Queries are not auto-validated by `ValidationBehavior` — validate parentUserId/paymentId inside handlers.
- Blocked by **P10-06** (Payment records) and **P10-07** (pack payments); refund entries from **P10-09** extend the history.
