{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "f=\"${CLAUDE_PROJECT_DIR:-.}/docs/dev/HANDOFF.md\"; [ -f \"$f\" ] && { printf '=== Shared dev memory (docs/dev/HANDOFF.md) — read before starting work; update it before opening your PR ===\\n\\n'; cat \"$f\"; } || true",
            "statusMessage": "Loading shared dev memory (HANDOFF.md)"
          }
        ]
      }
    ]
  }
}
