# Pipeline Brief — P10-05 Manage subscription plan (Free / Premium)

> Phase 10 — Payment, Billing & Credits. Parent-side plan management. First **non-foundational** billing story after the credit-core enablers; the source of truth for the monthly-grant tier (P10-02). **Parent-app surface only — never shown in the student app.**

---

## Summary & traceability

| Field | Value |
|---|---|
| **Task (one line)** | A parent can view their current plan, choose a billing period (Monthly 199 EGP / Annual 1990 EGP), compare Free vs Premium benefits, and upgrade / downgrade; the active plan and billing period drive recurring payment timing but not the monthly energy grant amount. |
| **Story (source of truth)** | `user-stories/Phase-10-Payments-Billing/P10-05-manage-subscription-plan.md` |
| **FR-ID** | FR-PAY-1 *(new — Phase 10)* |
| **BRD goal** | Monetization / business viability (the 199 EGP plan funds the AI tutor — see `docs/briefs/ai-cost-routing.md` §1). |
| **Phase / Epic / SP** | Phase 10 — Payment, Billing & Credits · Epic: Payment, Billing & Credits · SP 5 |
| **Task file** | **None yet** — `tasks/Backend/Phase-10-*/P10-05-BE.md` and FE task file do not exist. Flag to planner: tasks must be authored, or planner derives the task inventory from this brief. |

**Product-decision context:** parent-driven monetization (parent registers + adds children; children never see plan/payment UI), 4 subjects, no teacher role. **Family plan** — one plan per parent applies to **all** their children; per-child plans are explicitly out of scope.

**Upstream dependencies (must land first):**
- **P10-01** (credit account & ledger — `CreditAccount` with `GrantedBalance`/`PurchasedBalance`, append-only `CreditTransaction`) — foundation for the whole phase. **Blocks P10-05.**
- **P10-06** (payment provider) — Premium *activation* requires a successful payment. P10-05 ships the plan model + change rules; the paid-activation path is completed by P10-06. The two are tightly coupled (the story Notes mark P10-06 as blocked by P10-05).
- **P10-11** (admin config) — plan price (199 EGP) and grant amounts (Free 100 / Premium 5000) are config-driven, owned by P10-11.

---

## Business context & value

- **Who benefits:** the **parent** (controls how much AI help each child gets, at a predictable monthly price) and the **business** (recurring revenue that funds the AI tutor runtime — the 199 EGP plan is the unit economics anchor in `ai-cost-routing.md`).
- **Value:** the plan is the **single source of truth** for the monthly energy allotment (P10-02 reads it). Without a plan model there is no way to decide how much energy a child gets each cycle. Free = 100, Premium = 5000 (config-driven via P10-11).
- **Family plan** keeps the model simple: a parent on Premium upgrades *all* their children at once. Per-child plans would multiply billing complexity and are deferred.
- **Success measured by:** a parent can read their current plan + compare benefits; upgrade takes effect per the agreed rule (immediate, pending payment) and downgrade at next cycle; P10-02's grant job reads the active plan and grants the right amount.

---

## Acceptance criteria (testable)

1. A parent can **view the current plan** (Free or Premium) and its status (active / pending / canceled / cycle-end date).
2. A parent can **compare Free vs Premium** benefits side by side — at minimum **monthly energy** (100 vs 5000) and **daily soft cap** (10 vs 250) — values read from config (P10-11), never hard-coded in the FE.
3. A parent can **upgrade** to Premium. Upgrade **takes effect immediately on successful payment** (P10-06); **without a successful payment the parent stays on Free** (no Premium entitlement is granted on an unpaid/declined attempt).
4. A parent can **downgrade** to Free; the downgrade **takes effect at the next billing cycle** (Premium access persists until cycle end).
5. The **active plan is the source of truth for the monthly grant** — P10-02's grant job resolves the allotment from the parent's active plan for each of their children.
6. The plan applies to **all of the parent's children** (family plan). Per-child plans are not modeled.
7. Plan and plan-change state are **persisted, family-scoped, and only the owning parent can read/modify their own plan** (no cross-account access).
8. Renders in **Arabic (RTL) and English (LTR)**; the surface lives in the parent area (the existing P2-12 "Plan & billing" settings tab, which today is a read-only stub — see Affected surface).

---

## Affected modules & data

### Module — **NEW `Billing` module** (schema `billing`) — REQUIRES LEAD SIGN-OFF
- Per CLAUDE.md "ask before new modules" and the cluster constraints, the **`Billing` module is new** and owns subscriptions/plans/payments/invoices. Mirror an existing module's structure (Learning/Parent). **Flag for explicit lead sign-off before scaffolding** — this is the first time the module is created (shared by P10-01..P10-11).
- Schema-per-module: `billing`. Existing schemas are `identity`, `catalog`(learning), `notifications`, `gamification`, `parent`, `moderation`. `billing` is net-new.
- **Module isolation:** no cross-module FKs. The plan references the **parent** (an Identity user) by **loose id**, not an FK. P10-02 (the grant job) consumes the active plan via `Shared.Contracts` (integration event or a contract interface) — it does NOT reference `Billing` projects directly across the read boundary if it lives in another module. *(Open Q: does the P10-02 grant job live inside `Billing` or in another module? Recommend it lives in `Billing` since it operates on `CreditAccount` which is also in `Billing` per P10-01 — confirm.)*

### New entities (this story)
- **`Plan`** — the plan catalog: `Id`, `Code` (`Free` / `Premium`), `Name`, `MonthlyPriceEgp` (decimal, from P10-11 config), `MonthlyGrant` (int), `DailySoftCap` (int), `IsActive`. *(Open Q: is Plan a seeded reference table owned by Billing, or are the values pulled live from the P10-11 config store? Recommend a small seeded `Plan` table whose tunable fields are sourced from / overridden by P10-11 config — confirm with planner.)*
- **`Subscription`** — per-parent plan assignment: `Id`, `ParentUserId` (loose id, **no FK** to Identity), `PlanCode`, `Status` (enum `Active` / `PendingPayment` / `Canceled` / `Downgrading` / `Expired`), `CurrentCycleStart`, `CurrentCycleEnd`, `PendingPlanCode` (for scheduled downgrades), `CreatedAt`, audit fields. One active subscription per parent.

### Existing surface touched
- **P2-12 "Plan & billing" tab** (`P2-12-account-settings-tabs.md` child story P2-12d): today a **read-only/stub** tab in the parent settings page (`design-system/screenshots/web/07-settings.png`). P10-05 turns it into the real plan-management surface. The frontend agent extends that tab; it is the canonical home for this UI.

---

## Handoff → db-migration

- **New `Billing` module scaffolding** (Domain / Application / Infrastructure / Api projects mirroring Learning or Parent) — **only after lead sign-off**. New `BillingDbContext`, schema `billing`, `MigrationsHistoryTable("__EFMigrationsHistory", "billing")`, `UseNpgsql`, auto-migrate at startup via the module's `InitializeAsync` (mirror `LearningModule`).
- **Entities:** `Plan`, `Subscription` (fields above). All derive from the module's audited base (mirror an existing `AggregateRoot`/`FullAuditedEntity`). `ParentUserId` is a plain int/Guid loose id — **no navigation to Identity**.
- **EF config:** unique index on `Subscription.ParentUserId` (one active subscription per parent — confirm whether you keep history rows; if so the unique should be a filtered index on `Status = Active`). Seed the two `Plan` rows (Free, Premium) idempotently (mirror `LearningSeeder`).
- **No Unit of Work** (ADR 0001) — `GenericRepository` commits per call; for any atomic multi-write (e.g. activate Premium + write subscription) open an explicit transaction.
- This migration is the **foundation for P10-06/07/08/09 tables too** — coordinate with the P10-06 db-migration so the `BillingDbContext` + initial migration are created **once** (serialize the shared-file / module-scaffold edit per PARALLELISM).

## Handoff → backend-feature

- **Queries:** `GetCurrentPlan(parentUserId)` → current `Subscription` + resolved `Plan` benefits DTO; `GetPlanComparison()` → Free vs Premium benefit DTO (values from P10-11 config). Mirror existing CQRS query shape (`record XQuery : IQuery<BaseResponse<T>>`, handler inherits `BaseResponseHandler`, returns `BaseResponse<T>`, try/catch → `ServerError<T>`, inject `ILoggerManager`).
- **Commands:** `RequestUpgrade(parentUserId, planCode=Premium)` — creates/transitions a `Subscription` to `PendingPayment` and hands off to P10-06's checkout (returns a checkout session/redirect ref); on the P10-06 webhook success the subscription flips to `Active`. `RequestDowngrade(parentUserId)` — sets `PendingPlanCode = Free` + `Status = Downgrading`, effective at `CurrentCycleEnd`. `CancelSubscription` — stops future renewals, access to cycle end (coordinate with P10-06's recurring-cancel; may be the same command).
- **Commands are auto-validated** (`ValidationBehavior` runs for `ICommand<>`) — add FluentValidation validators. **Queries are not** — validate the parent id inside the handler.
- **Authz:** `[Authorize]` with a **family-scope / owning-parent** check — a parent may only read/modify their own subscription. Mirror P1-04/P1-05 family-scope semantics referenced by P2-12. **No teacher role.**
- **Contract for P10-02:** expose the active plan + allotment via `Shared.Contracts` (an `IPlanQuery`-style contract or a `PlanActivated` / `PlanChanged` integration event) so the grant job resolves the right amount without referencing Billing internals across a module boundary.

## Handoff → designer + frontend + frontend-e2e-tester

- **Surface:** the parent **"Plan & billing" settings tab** (P2-12d) in the Expo universal app's **parent area** (web PWA primary). This is the same tab shell P2-12 built; P10-05 fills in the real content. **Never rendered in the child area.**
- **Designer** (light — extends an existing tab, not a net-new flow; planner decides if a full design spec is warranted): current-plan card, Free-vs-Premium comparison table, Upgrade CTA (routes into P10-06 checkout), Downgrade/Cancel with confirmation + "effective at cycle end" copy, status badges (Active / Pending payment / Canceled). EN + AR, RTL mirrors, tokens only.
- **Frontend:** add api-client hooks `useCurrentPlan`, `usePlanComparison`, `useRequestUpgrade`, `useRequestDowngrade`/`useCancelSubscription`. Upgrade CTA hands off to the P10-06 checkout surface. i18n EN+AR. **Never show purchase/payment forms to a child route.**
- **frontend-e2e-tester:** drive the parent web PWA — view plan, compare benefits, attempt upgrade (mocked/stub provider), downgrade confirmation, RTL/ar+en, owning-parent-only access (a parent cannot see another parent's plan).

## Handoff → api-tester

- Endpoints exist (queries + commands) → api-tester runs after backend-feature against the running API + real PostgreSQL.
- Cases: get current plan (Free default), get comparison, request upgrade → subscription is `PendingPayment` (not yet Active without payment), request downgrade → effective next cycle / `Downgrading`, cancel → access to cycle end, **cross-account access denied** (parent A cannot read parent B's plan), `BaseResponse<T>` envelope with `Successed`, camelCase errors.

## Open questions / assumptions / risks

1. **NEW `Billing` module — needs lead sign-off** (CLAUDE.md "ask before new modules"). All of P10-05..11 share it. Resolve before any scaffolding.
2. **Proration on upgrade** (story AC + Notes mark it an open question). Does an immediate mid-cycle upgrade grant a prorated top-up of energy now, or only affect the next cycle? P10-02 says "a plan change mid-cycle affects the **next** grant." **Recommendation/assumption:** upgrade activates Premium *access* immediately on payment but the **energy grant difference is applied at the next cycle** (no mid-cycle proration of credits) — simplest and consistent with P10-02. **Confirm with lead.**
3. **Plan source of truth split:** is `Plan` a seeded Billing table or purely the P10-11 config store? Recommend a seeded `Plan` table whose tunable numbers come from P10-11 config. Confirm.
4. **Family plan vs per-child** — story confirms family plan; per-child is out of scope. Flag if product later wants per-child (would change the `Subscription` grain).
5. **Where does the P10-02 grant job live** (inside Billing vs another module)? Affects the `Shared.Contracts` seam. Recommend inside Billing.
6. **Upgrade↔P10-06 ordering:** P10-05 is "blocked by P10-01" and P10-06 is "blocked by P10-05" — they must be built as a tight pair (plan model first, then the paid-activation webhook completes it). Planner should sequence P10-05 → P10-06 and treat the Premium-activation AC as jointly delivered.

## Recommended pipeline order (first cut — planner finalizes)

1. **LEAD GATE:** sign off the new `Billing` module; resolve proration (OQ2) + plan-source (OQ3).
2. **db-migration:** scaffold `Billing` module + `BillingDbContext` + `Plan`/`Subscription` tables + seed plans. (Shared module-scaffold edit — coordinate/serialize with the P10-06 migration so the context is created once.)
3. **backend-feature:** plan queries + upgrade/downgrade/cancel commands + `Shared.Contracts` plan seam for P10-02.
4. **api-tester:** subscription endpoints against the running API.
5. **designer (light) → frontend → frontend-e2e-tester:** parent "Plan & billing" tab (P2-12d), EN+AR.
6. **security-auditor:** low surface here (no card data; it's plan state + family-scope authz). Planner may fold the authz check into the P10-06 audit. The Upgrade CTA's handoff to P10-06 is where card data appears — audited there.
7. **reviewer → committer** (`feat/P10-05-manage-subscription-plan`, PR to main, update HANDOFF.md). Never merge.

*Brief author: analyzer subagent · Date: 2026-06-12 · Phase 10 billing cluster.*
