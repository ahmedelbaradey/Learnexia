# Execution Plan — W10-P2-12-FE Parent account settings tabs

## Source files

| File | Role |
|---|---|
| `docs/briefs/W10-P2-12-FE.md` | Analyzer brief — adopted as spec; all best-guess answers are decided |
| `user-stories/Phase-2-Learning-Core/P2-12-account-settings-tabs.md` | User story + child stories P2-12a/b/c/d |
| `tasks/Frontend/student-app/Phase-2-Learning-Core/P2-12-FE.md` | FE task file (FE-1…FE-5) |
| `docs/dev/HANDOFF.md` | Load-bearing config; how to regen api-client |
| `apps/student-app/app/(parent)/_components/SettingsWeb.tsx` | Existing 6-tab shell — shell preserved; four `ComingSoonPanel` stubs replaced |
| `packages/api-client/src/generated/nswag-client.ts` | Currently stale (MUST be regenerated in B0) |
| `packages/api-client/src/query/queryKeys.ts` | Extended in B1 |
| `packages/api-client/src/hooks/` | 8 new hook files added in B1 |
| `packages/ui/src/components/CheckboxField/index.tsx` | Pattern reference for Switch primitive |

---

## Task inventory

| ID | Stack | Summary | Est (h) | Depends on |
|---|---|---|---|---|
| B0-regen | frontend (lead/agent) | Regenerate `@learnexia/api-client` against running BE (HTTP :5080). Verify 8 new methods + `myChildren` route move to `/api/Parent/My-Children`. Commit `chore: regen api-client for P2-12 endpoints`. | 0.5 | BE running on main |
| B1-switch | frontend | Add `Switch` primitive at `packages/ui/src/components/Switch/index.tsx`; export from `packages/ui/src/index.ts`. Mirrors `CheckboxField` prop shape. No abstraction beyond a simple wrapper. | 1 | B0-regen |
| B1-hooks | frontend | Add 8 hooks in `packages/api-client/src/hooks/`: `useNotificationPreferences`, `useUpdateNotificationPreferences`, `useUpdateChild`, `useUnlinkChild`, `useChangePassword` (new, targeting `/api/Users/Account/ChangePassword`), `useMySessions`, `useSignOutOtherSessions`, `useMyPlan`. Extend `queryKeys.ts` with `notifications.preferences()`, `account.sessions()`, `account.plan()`. Export all from `hooks/index.ts`. | 2 | B0-regen |
| B1-i18n | frontend | Add all new i18n keys (EN + AR) to `packages/shared/src/i18n/resources.ts` under `parent.settings.notifications.*`, `parent.settings.linkedChildren.*`, `parent.settings.security.*`, `parent.settings.billing.*`. See brief §Per-tab spec for full key lists. | 1 | — (independent of B0) |
| B2-design | designer | Write `design-system/ui_kits/parent-dashboard/W10-P2-12-tabs.md`: per-tab spec (Notifications, LinkedChildren, Security, Plan), Switch visual spec (track/thumb/disabled/focus/RTL), inline-confirm-strip UX for Unlink, Plan status pill, EN + AR appendices. Reference `web/07-settings.png`. | 2 | — (independent of B0/B1) |
| B3-notifications | frontend | Build `apps/student-app/app/(parent)/_components/settings/NotificationsPanel.tsx` (4-category × 2-switch grid, optimistic toggle, full-array PUT, success/error feedback). | 3 | B1-switch, B1-hooks, B1-i18n, B2-design |
| B3-linked | frontend | Build `apps/student-app/app/(parent)/_components/settings/LinkedChildrenPanel.tsx` (`ChildCard` list, inline Edit form, inline Unlink confirm strip, Add-child CTA, empty state). | 4 | B1-hooks, B1-i18n, B2-design |
| B3-security | frontend | Build `apps/student-app/app/(parent)/_components/settings/SecurityPanel.tsx` (change-password form + `PasswordStrengthMeter`, sessions list, Sign-out-others). | 3 | B1-hooks, B1-i18n, B2-design |
| B3-plan | frontend | Build `apps/student-app/app/(parent)/_components/settings/PlanPanel.tsx` (plan name, status `Badge`, disabled Manage CTA, `TODO(P2-12-PAYMENTS)` comment). | 1.5 | B1-hooks, B1-i18n, B2-design |
| B3-wire | frontend | Edit `SettingsWeb.tsx`: replace the `ComingSoonPanel` else-branch with a `switch(activeTab)` routing each of the 4 tabs to its dedicated panel. `direction` + `rowDir` props threaded through. `ComingSoonPanel` kept as a fallback for unknown keys. | 0.5 | B3-notifications, B3-linked, B3-security, B3-plan |
| B4-security-audit | security-auditor | Audit B3 output: password field `secureTextEntry`/no logging, no `console.log` of session IDs in production, `autocomplete` attributes correct on password fields, no plaintext password in state/query-keys, BE error messages not leaked raw. Block reviewer if Critical/High found. | 1 | B3-wire |
| B5-reviewer | reviewer | Gate all batches against brief ACs + CONVENTIONS + design spec + security-auditor results. Run `pnpm --filter student-app typecheck`, `pnpm lint`, `pnpm --filter student-app build:web`. Check api-client was regenerated (new types present). Check RTL + dark/light parity. | 1.5 | B4-security-audit |
| B6-committer | committer | Stage changed files, commit on `feat/W10-P2-12-FE-settings-tabs` with conventional message, push, open PR with full description. Update `docs/dev/HANDOFF.md` with what changed + decisions in the same commit. | 0.5 | B5-reviewer PASS |

**Total estimate:** ~21.5 h (implementation) + gates

---

## Dependency graph

```
B0-regen (blocks all implementation)
    |
    +---> B1-switch ---+
    |                  |
    +---> B1-hooks ----+---> B3-notifications ---+
    |                  |                          |
    |                  +---> B3-linked -----------+
    |                  |                          |
    |                  +---> B3-security ---------+---> B3-wire ---> B4-security-audit ---> B5-reviewer ---> B6-committer
    |                  |                          |
    |                  +---> B3-plan ------------+
    |
    +---> B1-i18n -----+
    |                  (feeds all B3 panels)
    |
B2-design (independent of B0/B1 — can run in parallel with B0+B1)
    |
    +---> B3-* panels (all B3 panels consume the design spec)
```

**Parallel opportunities:**
- B0 is sequential (needs running BE; blocks B1-switch and B1-hooks).
- B1-i18n and B2-design are **independent of B0** and each other — they may start in parallel with B0 if the agent knows the key names from the brief.
- B1-switch, B1-hooks, B1-i18n are **parallel-safe within one agent** once B0 is done.
- B3 panels (NotificationsPanel, LinkedChildrenPanel, SecurityPanel, PlanPanel) are logically parallel — they share no code — but because they must be built by the same `frontend` agent (single worktree, no parallel subagents), they run **sequentially within one agent invocation**, ordered by complexity: PlanPanel (simplest) → NotificationsPanel → SecurityPanel → LinkedChildrenPanel.
- B3-wire runs after all four panels exist.
- B4, B5, B6 are fully sequential.

---

## Execution batches

### Batch 0 — Pre-flight (sequential, blocks all FE implementation)
**Agent:** lead manual step OR `frontend` agent if BE already running at `:5080`.

**Prerequisite check (lead must verify before dispatching):**
- Backend is on `main` (P2-12 BE is confirmed merged per HANDOFF.md: `feat/P2-12-account-settings-apis` committed).
- `.NET 9 runtime` side-by-side is installed (HANDOFF.md: `dotnet-install.sh --runtime dotnet --channel 9.0` + `--runtime aspnetcore --channel 9.0`). The `nswag` 14.x tool requires Net90 binary.
- Backend is running: `ASPNETCORE_ENVIRONMENT=Development ASPNETCORE_URLS=http://localhost:5080 dotnet run --no-launch-profile` from `backend/src/Host/Learnexia.Host`.

**Steps:**
1. `SWAGGER_URL=http://localhost:5080/swagger/v2/swagger.json pnpm --filter @learnexia/api-client refresh:swagger`
2. `pnpm --filter @learnexia/api-client gen:api`
3. Verify presence of 8 new generated methods (names after regen — confirm exact names from NSwag output): one for each of the 9 endpoints in the brief tech-inventory table.
4. Verify `myChildren()` method now hits `/api/Parent/My-Children` (not the stale `/api/Users/Parent/My-Children`).
5. Check that the stale `changePasswordForUser` (targeting `/api/Users/UserManagement/ChangePasswordForUser`) still exists separately — the new `changePassword` method must target `/api/Users/Account/ChangePassword`. Do NOT conflate them.
6. Commit on `feat/W10-P2-12-FE-settings-tabs`: `chore: regen api-client for P2-12 endpoints`.

**Files touched:** `packages/api-client/src/generated/nswag-client.ts`, `packages/api-client/src/generated/swagger.json` (or equivalent).

**Review gate (before proceeding to B1):** 8 new methods present, `myChildren` route corrected, build compiles.

---

### Batch 1 — Foundations (parallel-safe within one agent invocation)
**Agent:** `frontend`
**Runs after:** B0 complete.
**B1-i18n may start independently** of B0 (keys are known from the brief) and B2 may also start independently — see note below.

**Tasks (run in parallel within the agent):**

**B1-switch — `packages/ui/src/components/Switch/index.tsx`**
- Wrap Tamagui's native `Switch` component with token-driven styling.
- Props (mirror `CheckboxField`): `{ value: boolean, onValueChange: (v: boolean) => void, label?: string, hideLabel?: boolean, disabled?: boolean, direction?: 'ltr'|'rtl', accessibilityLabel: string }`.
- Visual: track uses `$primary` when `value=true`, `$borderStrong` when false. Thumb is `$fg1`. Disabled: `opacity 0.5`. Focus: `$primary` ring (`outlineColor`). Track radius: pill (large `$modal` or explicit 100). No new design pattern — plain wrapper only. If anything fancier is needed, STOP and ask the lead.
- Export from `packages/ui/src/index.ts`.

**B1-hooks — 8 new hook files + queryKeys extension**
- `packages/api-client/src/query/queryKeys.ts`: add `notifications: { all: ['notifications'], preferences: () => [..., 'preferences'] }` and extend `account` with `sessions: ()` and `plan: ()`.
- `useNotificationPreferences.ts` — `useApiQuery` wrapping the regenerated GET preferences method; key `queryKeys.notifications.preferences()`.
- `useUpdateNotificationPreferences.ts` — `useApiMutation`; on success: invalidate `queryKeys.notifications.preferences()`.
- `useUpdateChild.ts` — `useApiMutation` over regenerated `updateChild` (PUT); on success: invalidate `queryKeys.family.myChildren()`.
- `useUnlinkChild.ts` — `useApiMutation` over regenerated `unlinkChild` (DELETE); on success: invalidate `queryKeys.family.myChildren()`.
- `useChangePassword.ts` — NEW file, `useApiMutation` over regenerated `changePassword` method targeting `/api/Users/Account/ChangePassword`. Do NOT reuse the stale `changePasswordForUser` path.
- `useMySessions.ts` — `useApiQuery`; key `queryKeys.account.sessions()`.
- `useSignOutOtherSessions.ts` — `useApiMutation`; on success: invalidate `queryKeys.account.sessions()`.
- `useMyPlan.ts` — `useApiQuery`; key `queryKeys.account.plan()`.
- Export all 8 from `packages/api-client/src/hooks/index.ts`.

**B1-i18n — `packages/shared/src/i18n/resources.ts`**
Add all new keys for both `en` and `ar` locales. Full key lists from the brief:

*Notifications:* `parent.settings.notifications.{title, subtitle, category.weeklyReport.{label,helper}, category.streakAtRisk.{label,helper}, category.productAnnouncement.{label,helper}, category.achievement.{label,helper}, channel.email, channel.push, saveSuccess, saveError}`

*Linked children:* `parent.settings.linkedChildren.{title, subtitle, addChild, emptyTitle, emptyBody, edit, unlink, unlinkConfirmTitle, unlinkConfirmBody, unlinkLastParentError, updateSuccess, updateError, unlinkSuccess, unlinkError, grade, language, country, fullName}`

*Security:* `parent.settings.security.{title, subtitle, currentPassword, newPassword, confirmPassword, save, saveSuccess, saveErrorPolicy, saveErrorCurrent, mismatchError, sameAsCurrent, sessionsTitle, sessionsSubtitle, signOutOthers, signOutOthersSuccess, sessionActive, sessionExpired, expiresAt}`

*Billing:* `parent.settings.billing.{title, subtitle, planLabel, statusActive, statusInactive, manage, managePending, upgradeComingSoon}`

AR values must be genuine Arabic (not transliterated). RTL copy should use right-aligned sentence flow naturally.

**Review gate (B1 internal — before handing off to B3):** `pnpm --filter @learnexia/ui build` (or typecheck) passes with Switch exported; all 8 hooks compile; i18n keys present for both locales.

---

### Batch 2 — Design spec (parallel with B0+B1 — independent)
**Agent:** `designer`
**Runs:** in parallel with B0/B1 (no code dependency; the designer reads the brief + existing screenshots).

**Output:** `design-system/ui_kits/parent-dashboard/W10-P2-12-tabs.md`

**Must cover:**
- Pixel reference: `design-system/screenshots/web/07-settings.png` — one section per tab.
- `PanelSurface` reuse spec: border `rgba(255,255,255,0.06)`, `$card` bg, `padding 22`, `gap 18`, `$modal` radius — unchanged from Profile panel.
- Per-tab field grid: `flexDirection={rowDir}` rows, `gap={14}` between fields, `flex={1} minWidth={240}` per field column (matches Profile panel's 2-col layout).
- Button row: `justifyContent="flex-end"`, `gap={10}`, `paddingTop={6}`.
- **Switch primitive visual spec:** track dimensions (~44×24px), thumb (white circle), `$primary` track when active, `$borderStrong` when inactive, disabled state (`opacity 0.5`), focus ring (`outlineColor $primary`), RTL behavior (thumb position reversal via `direction` prop — Tamagui handles it natively).
- **Unlink confirm strip:** inline inside `ChildCard` row (NOT a modal overlay). A second row below the child name: warning text ("Unlink {name}? This cannot be undone."), Cancel button (`ghost sm`), Unlink button (`ghost sm`, `$danger` label color). Toggled by `onRemove`. No shared Dialog primitive introduced.
- Plan tab: `Badge` status pill — `$success` variant when `Active`, `$fg3`/neutral variant otherwise. `planName` in a large heading (`fontSize 20, fontWeight 800`). Disabled Manage button with helper text below.
- Sessions row: `sessionId` truncated to first 8 chars + "…", status `Badge` (`Active` = `$success`, `Expired` = `$danger`), `expiresAt` formatted per locale.
- Both EN (LTR) and AR (RTL) appendices — mirror the structure of `align-settings.md`.
- Call out which sub-rows use `rowDir` reversal for RTL.

**Review gate (B2 internal):** spec references the screenshot, calls out the inline confirm strip, specifies Switch in tokens only, EN + AR appendices present.

---

### Batch 3 — Frontend panels (sequential within one agent, after B1+B2)
**Agent:** `frontend`
**Runs after:** B1 complete AND B2 complete.
**Implementation order within the agent** (simplest first, reduces ramp-up risk): PlanPanel → NotificationsPanel → SecurityPanel → LinkedChildrenPanel → SettingsWeb routing wire.

**Working directory:** `apps/student-app/app/(parent)/_components/settings/` (create directory).

**B3-plan — `settings/PlanPanel.tsx`**
- Import `useMyPlan` from `@learnexia/api-client`; import `PanelSurface`, `PanelHeader` (already defined in `SettingsWeb.tsx` — extract or re-import; see Extract Note below).
- Loading state: centered "Loading…" caption per Profile panel pattern.
- Render: `PanelHeader` (billing.title / billing.subtitle), large plan name heading, `Badge` status pill, body copy (`upgradeComingSoon`), disabled `<Button variant="primary" size="md" disabled>` with helper line below.
- `TODO(P2-12-PAYMENTS)` comment on the CTA.
- Props: `{ direction, rowDir }`.

**B3-notifications — `settings/NotificationsPanel.tsx`**
- Import `useNotificationPreferences`, `useUpdateNotificationPreferences` from `@learnexia/api-client`; import `Switch` from `@learnexia/ui`.
- Local enum constant mapping numeric category to i18n key (0=WeeklyReport, 1=StreakAtRisk, 2=ProductAnnouncement, 3=Achievement).
- State: keep a local `preferences` array; seed from query data. On toggle: clone array → mutate item → set state optimistically → fire mutation with full 4-item array. On error: rollback to previous state + `ServerErrorBanner`. On success: show `$successSoft` strip.
- 4 rows, each row: label + helper text on left (`writingDirection={direction}`), two `Switch` components on the right (`email`, `push`), row reversed via `rowDir`.
- `accessibilityLabel` composed from `t()`: `{categoryLabel} – {channelLabel}`.
- Skeleton/loading: centered text while `isPending`.

**B3-security — `settings/SecurityPanel.tsx`**
- Two sub-sections inside one `PanelSurface`, separated by a 1px divider (`borderBottomWidth={1}`, `borderBottomColor="rgba(255,255,255,0.06)"`).
- **Change-password section:**
  - 3 `TextField` components (`secureTextEntry`, show/hide toggle, `forceLtr` for all password fields per existing Login pattern).
  - `<PasswordStrengthMeter value={newPassword} />` below `newPassword` field.
  - Client-side guards: all 3 non-empty, `newPassword === confirmPassword`, `newPassword !== currentPassword`. Save button disabled until all guards pass.
  - On success: clear form, show `$successSoft` strip with `saveSuccess` copy ("Password updated — other sessions have been signed out."). On 400: `ServerErrorBanner`.
  - IMPORTANT: never `console.log` or expose password value outside the mutation call; no plaintext password in state beyond the controlled `useState`.
- **Active sessions section:**
  - `useMySessions()` — render each `SessionInfo` row: truncated `sessionId` (first 8 chars + "…"), `Badge` status, `expiresAt` formatted via `new Date(expiresAt).toLocaleDateString(locale)`.
  - **Sign out other sessions:** `<Button variant="ghost" size="md">` calls `useSignOutOtherSessions()`; on success: re-fetch sessions (query already invalidated by the mutation hook).
  - IMPORTANT: never `console.log` session IDs.

**B3-linked — `settings/LinkedChildrenPanel.tsx`**
- Import `useMyChildren`, `useUpdateChild`, `useUnlinkChild` from `@learnexia/api-client`; import `ChildCard` from `@learnexia/ui`.
- **Add child CTA** at top-right: `router.push('/(onboarding)/add-child')` (confirmed route from `MyChildrenWeb.tsx` — same pattern).
- **Child list:** `<ChildCard variant="editable">` per child. `meta` line = `child.email` only (grade absent from `LinkedChildResponse` — see Carry-forward log).
  - `onEdit`: toggle `editingId === child.id`. Shows inline Edit form below the card (a `Stack` expanded conditionally, not a modal).
  - `onRemove`: toggle `unlinkingId === child.id`. Shows inline confirm strip (not a modal dialog per designer decision).
- **Edit form** (inline, expandable below the child card): `TextField` for fullName, `Select` for grade (options 1..6 with localized labels), `Select` for language (`ar`/`en`), `Select` for country (reuse `COUNTRIES`). Submit calls `useUpdateChild()`; on success: close form, invalidate `useMyChildren`, show `$successSoft` strip. On error: `ServerErrorBanner` inline inside the form.
  - Grade/language/country default to empty — the parent must choose (Q L-1 decision: `LinkedChildResponse` lacks these fields; no separate GET by child ID exists).
- **Unlink confirm strip** (inline below child card, controlled by `unlinkingId`): warning text with child name, Cancel button, Unlink button (`ghost sm`, label in `$danger` color). Confirm: `useUnlinkChild({ childId })`. On 400 (last-parent guard): keep strip open, show inline error text from BE message mapped to `unlinkLastParentError` key. On success: close strip, invalidate, show success.
- **Empty state:** centered `emptyTitle` + `emptyBody` + Add child CTA.
- Props: `{ direction, rowDir }`.

**B3-wire — edit `SettingsWeb.tsx`**
- Replace the trailing `else { return <ComingSoonPanel ... /> }` block (lines 174–178 in the current file) with a `switch (activeTab)`:
  ```
  case SETTINGS_TAB.Notifications: return <NotificationsPanel direction={direction} rowDir={rowDir} />;
  case SETTINGS_TAB.LinkedChildren: return <LinkedChildrenPanel direction={direction} rowDir={rowDir} />;
  case SETTINGS_TAB.Security: return <SecurityPanel direction={direction} rowDir={rowDir} />;
  case SETTINGS_TAB.Billing: return <PlanPanel direction={direction} rowDir={rowDir} />;
  default: return <ComingSoonPanel direction={direction} />;
  ```
- Import all 4 new panels at the top of `SettingsWeb.tsx`.
- Keep `ComingSoonPanel` as the `default` fallback (do not delete it).
- No change to the tab list, `SETTINGS_TAB` const, `TAB_ICON`, `TAB_LABEL_KEY`, or the rail render logic.

**Extract Note — `PanelSurface` / `PanelHeader`:** These are currently local functions inside `SettingsWeb.tsx`. The new panels import from the same file or accept JSX children that already live within `SettingsWeb.tsx`. Option A (preferred, simpler): keep `PanelSurface` / `PanelHeader` in `SettingsWeb.tsx` and accept them as imports (or just replicate the tiny JSX inline in each new panel file since they are trivial). Option B: extract to `settings/shared.tsx`. Per CLAUDE.md rule #8, do not introduce a new pattern — choose the simplest approach (inline or sibling file) consistent with what the designer specifies.

**Review gate (B3 internal — before handing to B4):** `pnpm --filter student-app typecheck` green; no raw hex/px violations beyond sibling-consistent layout numbers; no `console.log` of sensitive values.

---

### Batch 4 — Security audit (mandatory gate)
**Agent:** `security-auditor`
**Runs after:** B3 complete.

**Scope — FE-side checks only (no backend audit needed; P2-12 BE security audit already PASS on main):**
- Password fields: `secureTextEntry` set on all 3 fields; show/hide toggle uses a boolean state flag, not string mutation; `autoComplete="new-password"` / `"current-password"` as appropriate (web `autocomplete` attribute).
- No `console.log`, `console.warn`, or `console.error` containing a password value, session ID, or token.
- `useChangePassword` mutation does not include the password in the query key or any publicly readable TanStack Query cache entry.
- Error messages from BE surfaced via `useServerError` / `ServerErrorBanner` only — never raw `error.message` injected into the DOM or state that could be read by other components.
- Session IDs in the sessions list: rendered truncated (first 8 chars) — not full UUIDs exposed unnecessarily.
- No `TODO(P2-12-PAYMENTS)` comment left missing on the Manage CTA.
- Accessibility: password fields have `accessibilityLabel` (important for screen readers that must not announce the value).
- RTL: no hard-coded `textAlign="left"` without `direction` guard.

**Blocking criteria:** Critical or High findings block the reviewer gate. The lead must be notified; implementer must fix before B5 starts.

---

### Batch 5 — Reviewer gate
**Agent:** `reviewer`
**Runs after:** B4 security-auditor PASS (no Critical/High).

**Checklist:**
- All 4 `ComingSoonPanel` stubs replaced; `SettingsWeb.tsx` routes correctly.
- `packages/api-client/src/generated/` was regenerated — new types/methods present; `myChildren()` hits `/api/Parent/My-Children`.
- 8 new hooks exist, exported from `hooks/index.ts`; `queryKeys.ts` extended.
- `Switch` primitive in `@learnexia/ui` — exported, tokens-only, no design pattern.
- All i18n keys present in both `en` and `ar`; all panel copy goes through `t()`.
- `ServerErrorBanner` + `useServerError` used for errors (not raw message strings); `$successSoft` strip for success — matches Profile panel's pattern.
- `BaseResponse.successed` (not `succeeded`) checked where applicable via the hook's error/success detection.
- No raw hex (unless sibling-consistent layout numbers — e.g. the `rgba(255,255,255,0.06)` border already in the shell), no raw `px` outside existing sibling usage.
- RTL: `direction` + `rowDir` threaded into all panels; `writingDirection` on text elements; `forceLtr` on password/email/phone fields.
- Dark + light: tokens only — no theme-specific hard-codes.
- Pixel-perfect to `design-system/screenshots/web/07-settings.png` per the design spec.
- Unlink confirm is an inline strip (no modal/Dialog primitive introduced).
- Plan tab Manage CTA is disabled; `TODO(P2-12-PAYMENTS)` comment present.
- `pnpm --filter student-app typecheck` — green.
- `pnpm lint` — green.
- `pnpm --filter student-app build:web` — green.
- No secrets or hard-coded API URLs.
- Security-auditor B4 report: PASS (no Critical/High).

---

### Batch 6 — Committer (only after B5 PASS)
**Agent:** `committer`

**Steps:**
1. Stage all changed files (enumerate explicitly — do NOT `git add -A`):
   - `packages/api-client/src/generated/nswag-client.ts` (regenerated)
   - `packages/api-client/src/generated/swagger.json` (if exists)
   - `packages/api-client/src/query/queryKeys.ts`
   - `packages/api-client/src/hooks/useNotificationPreferences.ts` (new)
   - `packages/api-client/src/hooks/useUpdateNotificationPreferences.ts` (new)
   - `packages/api-client/src/hooks/useUpdateChild.ts` (new)
   - `packages/api-client/src/hooks/useUnlinkChild.ts` (new)
   - `packages/api-client/src/hooks/useChangePassword.ts` (new)
   - `packages/api-client/src/hooks/useMySessions.ts` (new)
   - `packages/api-client/src/hooks/useSignOutOtherSessions.ts` (new)
   - `packages/api-client/src/hooks/useMyPlan.ts` (new)
   - `packages/api-client/src/hooks/index.ts`
   - `packages/shared/src/i18n/resources.ts`
   - `packages/ui/src/components/Switch/index.tsx` (new)
   - `packages/ui/src/index.ts`
   - `design-system/ui_kits/parent-dashboard/W10-P2-12-tabs.md` (new)
   - `apps/student-app/app/(parent)/_components/settings/NotificationsPanel.tsx` (new)
   - `apps/student-app/app/(parent)/_components/settings/LinkedChildrenPanel.tsx` (new)
   - `apps/student-app/app/(parent)/_components/settings/SecurityPanel.tsx` (new)
   - `apps/student-app/app/(parent)/_components/settings/PlanPanel.tsx` (new)
   - `apps/student-app/app/(parent)/_components/SettingsWeb.tsx`
   - `docs/dev/HANDOFF.md`
2. Commit message (conventional): `feat(settings): build P2-12 settings tabs — Notifications, Linked children, Security, Plan`
3. Branch: `feat/W10-P2-12-FE-settings-tabs` (already created per constraints).
4. Push branch and open PR with full description (tabs built, design spec, api-client regen, security audit PASS).
5. Update `docs/dev/HANDOFF.md` in the same commit:
   - Mark Wave 10 P2-12-FE complete.
   - Note the `Switch` primitive added to `@learnexia/ui`.
   - Note api-client regenerated; `myChildren` route now `/api/Parent/My-Children`.
   - Note stale `changePasswordForUser` (old admin route) still in generated client — do NOT remove it (may have admin consumers); the new `changePassword` is the account-security route.
   - Note `LinkedChildResponse` lacks grade/language/country (carry-forward P2-12 item).
   - Note payments stub on Plan tab (`TODO P2-12-PAYMENTS`).

---

## Review gates

| Gate | Condition | Who clears |
|---|---|---|
| B0 exit | 8 new methods in generated client; `myChildren` route corrected; build compiles. | Lead / frontend agent |
| B1 exit | Switch exported + tokens-only; 8 hooks compile; i18n keys in both locales. | Frontend agent self-check before B3 |
| B2 exit | Design spec covers all 4 tabs + Switch + Unlink strip + EN/AR appendices. | Designer self-check |
| B3 exit | Typecheck green; no raw hex/px violations; no sensitive `console.log`. | Frontend agent self-check |
| B4 gate | Security-auditor PASS — zero Critical/High findings. BLOCKS B5 if any found. | security-auditor |
| B5 gate | All reviewer checklist items green. BLOCKS B6 if any fail. | reviewer |
| Final | PR open, HANDOFF.md updated, branch pushed. | committer |

---

## Blockers / prerequisites

| # | Blocker | Status | Mitigation |
|---|---|---|---|
| P1 | Backend running for api-client regen | Must be started by lead | HANDOFF.md documents the exact `dotnet run` command. .NET 9 runtime side-by-side must be installed first. |
| P2 | .NET 9 runtime side-by-side | Must be present on the build machine | `dotnet-install.sh --runtime dotnet --channel 9.0` + `--runtime aspnetcore --channel 9.0` (one-time; already documented in HANDOFF.md). |
| P3 | P2-12 BE merged to main | Confirmed merged per HANDOFF.md § "P2-12 — Account settings" | No action needed. |
| P4 | `Switch` primitive missing from `@learnexia/ui` | Built in B1 (confirmed absent from `packages/ui/src/components/` glob) | B1-switch task covers it. |
| P5 | Stale `useChangePassword` targeting admin route | Confirmed stale (targets `/api/Users/UserManagement/ChangePasswordForUser`) | B1-hooks adds a new `useChangePassword.ts`; the old one is a different method entirely (`changePasswordForUser`) — no naming conflict. |
| P6 | `queryKeys.ts` missing `notifications`, `account.sessions`, `account.plan` | Extended in B1-hooks | B1-hooks task covers it. |
| P7 | `LinkedChildResponse` lacks grade/language/country | Architectural limitation of current BE (carry-forward per brief Q L-1) | Edit form opens with empty defaults; parent must select. Documented in carry-forward log. |
| P8 | No `GET /api/Parent/Child/{id}` endpoint | BE limitation — no per-child GET | Mitigated by empty-defaults Edit form per Q L-1 decision. |
| P10 | `SessionInfo` has no device/IP/UA metadata | BE limitation (only `{sessionId, isActive, isExpired, expiresAt, remainingSeconds}`) | Render truncated sessionId + expiresAt + status badge only. Richer metadata deferred to P6-06. |
| P10 | No shared `Dialog` primitive in `@learnexia/ui` | Confirmed absent | Inline confirm strip inside `ChildCard` row per designer call (brief Q UI-2). Do NOT introduce a Dialog. |
| P11 | No payments BE | BE returns stub `{planName:"Free",status:"Active"}` | Plan tab is read-only; Manage CTA disabled with `TODO(P2-12-PAYMENTS)`. |
| P12 | `ChangePasswordCommand.currentPassword` is nullable on BE | Brief Q S-2 decision | FE requires `currentPassword` for W10; social-login users with no local password are out of scope. |

---

## Out-of-scope carry-forward log

The following items were explicitly decided as out of scope for W10 and must NOT be implemented here. Each has a `TODO` comment or a follow-up story reference.

| Item | Why deferred | Follow-up |
|---|---|---|
| Payment processing / checkout flow | No payments BE exists; stub only | `TODO(P2-12-PAYMENTS)` — dedicated payments story |
| Apple / Microsoft OAuth sign-in linking on Security tab | Only Google landed in P1-12; settings does not surface linked identity providers in W10 | Future payments/identity story |
| Native (iOS/Android) OS push-permission prompt | The push toggle persists the preference server-side; wiring the OS permission prompt is native-only | Future native story |
| Session device / IP / user-agent metadata in sessions list | BE's `SessionInfo` has no device fields | P6-06 BE extension |
| `LinkedChildResponse.grade` / `.language` / `.country` | BE doesn't return these fields on the list endpoint; no per-child GET exists | P2-12 carry-forward — extend `LinkedChildResponse` or add a per-child GET endpoint |
| Grade-transition control (full flow) | History-preserving; complex authz | P5-06 |
| Child grade in Edit form pre-filled | Depends on missing `LinkedChildResponse` fields | Blocked by carry-forward above |
| Avatar upload / removal on Profile tab | `P1-12 BE-4` not yet wired (MinIO flow exists on BE; FE wiring deferred) | P1-12 avatar upload story |
| Email change on Profile tab | `AccountProfileResponse` doesn't expose email yet | Future BE extension |
| Marketing-site changes | W10 is student-app only | Not applicable |
| `ChangePasswordCommand.currentPassword` optional path for OAuth-only users | No "Set a password" UX for W10 | Future security story |
| Session count display ("N other sessions") without a list | Alternative UX for Q S-1 if designer prefers | Designer call — if spec chooses this over the minimal list, it can replace B3-security's sessions section |

---

## Definition of done

### Per batch
- **B0:** Generated client contains all 8 new methods; `myChildren()` uses `/api/Parent/My-Children`; TypeScript builds without errors.
- **B1:** `Switch` exported from `@learnexia/ui` with token-only styling; 8 hooks compile and export correctly; all i18n keys present in `en` and `ar`.
- **B2:** `design-system/ui_kits/parent-dashboard/W10-P2-12-tabs.md` exists and covers all 4 tabs, Switch visual spec, inline Unlink strip UX, EN + AR appendices.
- **B3:** All 4 panel files exist under `settings/`; `SettingsWeb.tsx` routes correctly; no `ComingSoonPanel` rendered for any of the 4 tabs; typecheck green.
- **B4:** Security-auditor PASS — zero Critical/High on FE-side checks.
- **B5:** All reviewer checklist items green; `pnpm --filter student-app typecheck`, `pnpm lint`, `pnpm --filter student-app build:web` all green.
- **B6:** Branch pushed, PR open, `docs/dev/HANDOFF.md` updated in the same commit.

### Overall story acceptance criteria mapping
| AC | Satisfied by |
|---|---|
| AC-FE-0a (panels swap ComingSoonPanel) | B3-wire |
| AC-FE-0b (all copy via `t()`, en + ar) | B1-i18n + B3 panels |
| AC-FE-0c (tokens only, no raw hex/px) | B3 panels (verified by B5 reviewer) |
| AC-FE-0d (ServerErrorBanner + useServerError) | B3 panels |
| AC-FE-N1–N4 (Notifications tab) | B3-notifications |
| AC-FE-L1–L6 (Linked children tab) | B3-linked |
| AC-FE-S1–S5 (Security tab) | B3-security |
| AC-FE-P1–P3 (Plan & billing tab) | B3-plan |
| AC-FE-Q1 (pixel-perfect) | B5 reviewer cross-check vs design spec + screenshot |
| AC-FE-Q2 (RTL parity) | B3 panels + B5 reviewer |
| AC-FE-Q3 (dark/light parity) | B3 panels (tokens only) + B5 reviewer |
| AC-FE-Q4 (typecheck/lint/build green) | B5 reviewer running CI commands |

---

Plan ready — dispatch Batch 0.
