# Python service local artifacts — never committed.
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/
.env
# Benchmark report is regenerated by the runner (devops flip); keep the dir via .gitkeep.
benchmarks/results/*.md
!benchmarks/results/.gitkeep
