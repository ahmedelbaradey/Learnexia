namespace Learnexia.Modules.Notifications.Application;

// Marker type so the Host can reference this Application assembly for the unified MediatR scan
// (ADR 0002 §4 / P4-01-BE-4). Mirrors the AssemblyReference markers in the Identity & Catalog
// Application projects.
public class AssemblyReference
{
}
