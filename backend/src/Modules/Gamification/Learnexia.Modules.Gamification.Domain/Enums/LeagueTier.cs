namespace Learnexia.Modules.Gamification.Domain.Enums;

// Ordered weekly league tiers (Bronze -> Diamond). FR-GM-6. The numeric value encodes promotion order.
public enum LeagueTier
{
    Bronze = 1,
    Silver = 2,
    Gold = 3,
    Diamond = 4,
}
