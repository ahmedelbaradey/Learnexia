namespace Learnexia.Modules.Analytics.Infrastructure;

/// <summary>
/// Marker type for the Analytics Infrastructure assembly.
/// Used if the Host ever needs to register Infrastructure-resident handlers via MediatR scanning
/// (mirrors the Curriculum.Infrastructure / Learning.Infrastructure pattern).
/// </summary>
public class AssemblyReference
{
}
